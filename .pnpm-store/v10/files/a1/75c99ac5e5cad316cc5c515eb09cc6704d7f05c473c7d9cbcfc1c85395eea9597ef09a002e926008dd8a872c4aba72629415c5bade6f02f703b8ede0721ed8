/**
 * Adapted from https://github.com/purescript/purescript-prelude/blob/master/src/Data/Field.purs
 *
 * @since 2.0.0
 */ // -------------------------------------------------------------------------------------
// utils
// -------------------------------------------------------------------------------------
/**
 * The *greatest common divisor* of two values
 *
 * @since 2.0.0
 */export let gcd = (E, field) => {
  let zero = field.zero;
  let f = (x, y) => E.equals(y, zero) ? x : f(y, field.mod(x, y));
  return f;
}; /**
   * The *least common multiple* of two values
   *
   * @since 2.0.0
   */
export let lcm = (E, F) => {
  let zero = F.zero;
  let gcdSF = gcd(E, F);
  return (x, y) => E.equals(x, zero) || E.equals(y, zero) ? zero : F.div(F.mul(x, y), gcdSF(x, y));
}; // -------------------------------------------------------------------------------------
// deprecated
// -------------------------------------------------------------------------------------
/**
 * Use [`Field`](./number.ts.html#field) instead.
 *
 * @category zone of death
 * @since 2.0.0
 * @deprecated
 */
export let fieldNumber = {
  add: (x, y) => x + y,
  zero: 0,
  mul: (x, y) => x * y,
  one: 1,
  sub: (x, y) => x - y,
  degree: _ => 1,
  div: (x, y) => x / y,
  mod: (x, y) => x % y
};
//# sourceMappingURL=Field.js.map