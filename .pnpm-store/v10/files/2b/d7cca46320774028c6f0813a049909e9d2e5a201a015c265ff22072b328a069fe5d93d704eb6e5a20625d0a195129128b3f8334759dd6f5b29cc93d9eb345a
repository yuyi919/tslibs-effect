// Generated by ReScript, PLEASE EDIT WITH CARE
import * as Primitive_curry from "rescript/lib/es6/Primitive_curry.js";
let constant = a => param => a;
let mapTo = (p, a) => a;
let tap = (t, f) => (Primitive_curry._1(f, t), t);
let constThis = function () {
  return this;
};
let $$throw = e => {
  throw e;
};
let id = (a, id$1) => a;
let make = (a, b) => a(b);
let from = __x => make(__x, undefined);
let fromUnsafe = f => {
  let cache = make(f, undefined);
  return {
    _: () => cache
  };
};
let Lazy_get = prim => prim;
let Lazy_getUnsafe = prim => prim;
let Lazy = {
  from: from,
  fromUnsafe: fromUnsafe,
  get: Lazy_get,
  getUnsafe: Lazy_getUnsafe
};
let Idx2 = Id => ({
  Id: {},
  get: prim => add()
});
let Id = M => {
  let make = (f, a) => f(a);
  let wrap = f => ({
    _: () => f,
    f: f$1 => f$1(f)
  });
  let from = f => {
    let cache = make(f, undefined);
    return {
      _: () => cache,
      f: f => f(cache)
    };
  };
  return {
    wrap: wrap,
    from: from
  };
};
let Id2 = M => {
  let make = (f, a) => f(a);
  let from = f => {
    let cache = make(f, undefined);
    return {
      _: () => cache,
      f: f => f(cache)
    };
  };
  return {
    from: from
  };
};
export { constant, mapTo, tap, constThis, $$throw, id, Lazy, Idx2, Id, Id2 }; /* No side effect */
//# sourceMappingURL=Identity.bs.js.map