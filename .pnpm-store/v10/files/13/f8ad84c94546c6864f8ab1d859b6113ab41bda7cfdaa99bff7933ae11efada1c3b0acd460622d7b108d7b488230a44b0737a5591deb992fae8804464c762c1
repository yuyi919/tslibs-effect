/**
 * @since 0.6.0
 */ // import { Char } from './char'
let lineTerminatorRegex = /[\n\r]$/m;
export let getLocation = (source, cursor) => {
  let line = 1;
  let column = 1;
  let i = 0;
  while (i < cursor) {
    i++;
    let c = source.charAt(i);
    if (lineTerminatorRegex.test(c)) {
      line++;
      column = 1;
    } else {
      column++;
    }
  }
  return {
    start: {
      line,
      column,
    },
  };
};
//# sourceMappingURL=code-frame.js.map
