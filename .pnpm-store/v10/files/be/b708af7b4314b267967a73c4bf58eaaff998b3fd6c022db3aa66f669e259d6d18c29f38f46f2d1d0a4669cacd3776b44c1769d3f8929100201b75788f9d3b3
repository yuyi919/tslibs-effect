import { OBJECT_PROPOTYPE } from "./additional"
import { NULL } from "./consts"
import { ARRAY, FUNCTION, OBJECT } from "./generated"

/**
 * @internal
 */
export const ENTRIES: typeof Object.entries = OBJECT.entries

/**
 * @internal
 * @see Object.is
 */
export const OBJECT_IS: typeof Object.is = OBJECT.is

/**
 * @internal
 */
export const IS_ARR: typeof Array.isArray = ARRAY.isArray

/**
 * @internal
 */
export const CALLER: typeof Function.call = FUNCTION.call

/**
 * @internal
 * @see parseFloat
 */
export const PARSE_FLOAT: typeof parseFloat = parseFloat

/**
 * @internal
 * @see parseInt
 */
export const PARSE_INT: typeof parseInt = parseInt

/**
 * ```ts
 * Object.keys
 * ```
 * @internal
 * */
export const OBJ_KEYS: typeof Object.keys = OBJECT.keys
/**
 * ```ts
 * Object.freeze
 * ```
 * @internal
 * */
export const OBJ_FREEZE: typeof Object.freeze = OBJECT.freeze
/**
 * ```ts
 * Object.seal
 * ```
 * @internal
 * */
export const OBJ_SEAL: typeof Object.seal = OBJECT.seal
/**
 * ```ts
 * Object.create
 * ```
 * @internal
 * */
export const OBJ_CREATE: typeof Object.create = OBJECT.create
/**
 * ```ts
 * Object.assign
 * ```
 * @internal
 * */
export const OBJ_ASSIGN: typeof Object.assign = OBJECT.assign
/**
 * ```ts
 * Object.getOwnPropertyDescriptors
 * ```
 * @internal
 * */
export const OBJ_getOwnPropertyDescriptors$: typeof Object.getOwnPropertyDescriptors =
  OBJECT.getOwnPropertyDescriptors
/**
 * ```ts
 * Object.defineProperty
 * ```
 * @internal
 * */
export const OBJ_defineProperty$: typeof Object.defineProperty =
  OBJECT.defineProperty
/**
 * ```ts
 * Object.defineProperties
 * ```
 * @internal
 * */
export const OBJ_defineProperties$: typeof Object.defineProperties =
  OBJECT.defineProperties

/**
 * ```ts
 * Object.getPrototypeOf
 * ```
 * @internal
 * */
export const OBJ_getPrototypeOf$: typeof Object.getPrototypeOf =
  OBJECT.getPrototypeOf

/**
 * ```ts
 * Object.setPrototypeOf
 * ```
 * @internal
 * */
export const OBJ_setPrototypeOf$: typeof Object.setPrototypeOf =
  OBJECT.setPrototypeOf

/**
 * ```ts
 * Object.getOwnPropertyDescriptor
 * ```
 * @internal
 * */
export const OBJ_getOwnPropertyDescriptor$: typeof Object.getOwnPropertyDescriptor =
  OBJECT.getOwnPropertyDescriptor

/**
 * ```ts
 * Object.getOwnPropertyNames
 * ```
 * @internal
 * */
export const OBJ_getOwnPropertyNames$: {
  (o: number): []
  (o: boolean): []
  (o: string): string[]
  (o: any[]): string[]
  <T extends Record<any, any>>(o: T): Exclude<keyof T, symbol>[]
  (o: any): string[]
} = OBJECT.getOwnPropertyNames as any

/**
 * ```ts
 * Object.getOwnPropertyNames
 * ```
 * @internal
 * */
export const OBJ_getOwnPropertySymbols$: {
  (o: number | boolean): []
  (o: string): symbol[]
  (o: any[]): symbol[]
  <T extends Record<any, any>>(o: T): Extract<keyof T, symbol>[]
  (o: any): symbol[]
} = OBJECT.getOwnPropertySymbols as any

/**
 * @internal
 */
export const PROMISE: typeof Promise = Promise

/**
 * @internal
 */
export const CREATE_NEW: typeof Reflect.construct = Reflect.construct

/**
 * @internal
 */
export const RESOLVE: typeof Promise.resolve = PROMISE.resolve

/**
 * @internal
 */
export const ALL: typeof Promise.all = PROMISE.all

/**
 * noop return
 * @internal
 */
export const NOOP: () => void = () => {}

/**
 * 强制隐式转换类型
 * @internal
 */
/* @__INLINE__ */
export const CAST: <T>(target: any) => T = <T>(target: any) => target as T

/**
 * ```ts
 * Object.prototype.hasOwnProperty
 * ```
 * @internal
 */
export const OBJECT_HAS_OWNPROPERTY: typeof Object.prototype.hasOwnProperty =
  OBJECT_PROPOTYPE.hasOwnProperty

/**
 * Determines whether an object has a property with the specified name.
 * @param o An object.
 * @param v A property name.
 * ```ts
 * Object.hasOwn
 * ```
 * @internal
 */
export const OBJECT_HAS_OWN: (o: object, v: PropertyKey) => boolean =
  OBJECT.hasOwn

/**
 * Creates an array from an iterable object.
 * @param iterable An iterable object to convert to an array.
 * @param mapfn A mapping function to call on every element of the array.
 * @param thisArg Value of 'this' used to invoke the mapfn.
 */
export const ARRAY_FROM: typeof Array.from = ARRAY.from

export function BIND_NULL<F extends Function | undefined>(f: F): F {
  return f?.bind(NULL)
}
