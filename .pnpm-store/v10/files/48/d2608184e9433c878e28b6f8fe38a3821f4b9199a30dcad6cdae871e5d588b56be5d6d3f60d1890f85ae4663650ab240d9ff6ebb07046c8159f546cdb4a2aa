import { dual } from "effect/Function";
import { isLeft, isRight } from "fp-ts/Either";

import * as Option from "./Option.js";

export * from "fp-ts/Either";
/**
 * Extracts the value of an `Either` or throws if the `Either` is `Left`.
 *
 * If a default error is sufficient for your use case and you don't need to configure the thrown error, see {@link getOrThrow}.
 *
 * @param self - The `Either` to extract the value from.
 * @param onLeft - A function that will be called if the `Either` is `Left`. It returns the error to be thrown.
 *
 * @example
 *
 * assert.deepStrictEqual(
 *   Either.getOrThrowWith(Either.right(1), () => new Error('Unexpected Left')),
 *   1
 * )
 * assert.throws(() => Either.getOrThrowWith(Either.left("error"), () => new Error('Unexpected Left')))
 *
 * @category getters
 * @since 2.0.0
 */
export let getOrThrowWith = /*#__PURE__*/ dual(2, (self, onLeft) => {
  if (isRight(self)) {
    return self.right;
  }
  throw onLeft(self.left);
});
export let toOption = (self) =>
  isRight(self) ? Option.some(self.right) : Option.none();
/**
 * @category pattern matching
 * @since 0.0.0
 */
export let match = (ma, onRight, onLeft) =>
  isLeft(ma) ? onLeft(ma.left) : onRight(ma.right);
//# sourceMappingURL=Either.js.map
