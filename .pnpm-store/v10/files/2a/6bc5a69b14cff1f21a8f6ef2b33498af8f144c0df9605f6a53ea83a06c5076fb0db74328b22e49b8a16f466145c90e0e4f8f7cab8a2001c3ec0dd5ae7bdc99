// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_curry from "rescript/lib/es6/Primitive_curry.js";

function constant(a) {
  return param => a;
}

function mapTo(p, a) {
  return a;
}

function tap(t, f) {
  Primitive_curry._1(f, t);
  return t;
}

let constThis = (function () {return this});

let $$throw = (e => { throw e });

function id(a, id$1) {
  return a;
}

let make = ((a, b) => a(b));

function from(__x) {
  return make(__x, undefined);
}

function fromUnsafe(f) {
  let cache = make(f, undefined);
  return {
    _: () => cache
  };
}

function Lazy_get(prim) {
  return prim;
}

function Lazy_getUnsafe(prim) {
  return prim;
}

let Lazy = {
  from: from,
  fromUnsafe: fromUnsafe,
  get: Lazy_get,
  getUnsafe: Lazy_getUnsafe
};

function Idx2(Id) {
  return {
    Id: {},
    get: prim => add()
  };
}

function Id(M) {
  let make = ((f, a) => f(a));
  let wrap = f => ({
    _: () => f,
    f: f$1 => f$1(f)
  });
  let from = f => {
    let cache = make(f, undefined);
    return {
      _: () => cache,
      f: f => f(cache)
    };
  };
  return {
    wrap: wrap,
    from: from
  };
}

function Id2(M) {
  let make = ((f, a) => f(a));
  let from = f => {
    let cache = make(f, undefined);
    return {
      _: () => cache,
      f: f => f(cache)
    };
  };
  return {
    from: from
  };
}

export {
  constant,
  mapTo,
  tap,
  constThis,
  $$throw,
  id,
  Lazy,
  Idx2,
  Id,
  Id2,
}
/* No side effect */
