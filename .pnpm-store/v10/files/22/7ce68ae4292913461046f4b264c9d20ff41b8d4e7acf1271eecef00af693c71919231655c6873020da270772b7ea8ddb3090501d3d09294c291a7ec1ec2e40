// Generated by ReScript, PLEASE EDIT WITH CARE
let Make = $star => {
  let fromLength = prim => new Int32Array(prim);
  let fromBufferRange = (prim0, prim1, prim2) => new Int32Array(prim0, prim1, prim2);
  let get = (array, index) => array[index];
  let set = (array, index, v) => {
    array[index] = v;
  };
  let plusplus = (array, index) => {
    array[index] = array[index] + 1 | 0;
  };
  let minusminus = (array, index) => {
    array[index] = array[index] - 1 | 0;
  };
  let plus = (array, index, v) => {
    array[index] = array[index] + v | 0;
  };
  let grow_to = (array, index, v) => {
    if (v > array[index]) {
      array[index] = v;
      return;
    }
  };
  let minus = (array, index, v) => {
    array[index] = array[index] - v | 0;
  };
  return {
    fromLength: fromLength,
    fromBufferRange: fromBufferRange,
    get: get,
    set: set,
    plusplus: plusplus,
    minusminus: minusminus,
    plus: plus,
    grow_to: grow_to,
    minus: minus
  };
};
let fromLength = prim => new Int32Array(prim);
let fromBufferRange = (prim0, prim1, prim2) => new Int32Array(prim0, prim1, prim2);
let get = (array, index) => array[index];
let set = (array, index, v) => {
  array[index] = v;
};
let plusplus = (array, index) => {
  array[index] = array[index] + 1 | 0;
};
let minusminus = (array, index) => {
  array[index] = array[index] - 1 | 0;
};
let plus = (array, index, v) => {
  array[index] = array[index] + v | 0;
};
let grow_to = (array, index, v) => {
  if (v > array[index]) {
    array[index] = v;
    return;
  }
};
let minus = (array, index, v) => {
  array[index] = array[index] - v | 0;
};
export { Make, fromLength, fromBufferRange, get, set, plusplus, minusminus, plus, grow_to, minus }; /* No side effect */
//# sourceMappingURL=I32Array.bs.js.map