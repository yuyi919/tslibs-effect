import type { Types } from "..";
/**
 * @beta
 */
export type Peaces<K extends string> = K extends `${infer Pre}${infer D}`
  ? [Pre, ...Peaces<D>]
  : [];
/**
 * @beta
 */
export type Length<
  K extends string,
  Deep extends number = 0,
> = K extends `${string}${infer D}` ? Length<D, Types.Number.Plus<Deep>> : Deep;
export * from "./case";
export * from "./split";
/**
 * @beta
 */
export type JoinWith<K, P, STR extends string = ""> = K extends string | number
  ? P extends string | number
    ? `${K}${"" extends P ? "" : STR}${P}`
    : never
  : never;
//# sourceMappingURL=index.d.ts.map
