/**
 * @since 2.4.0
 */import { getApplySemigroup } from "./Apply.js";
import { fromOption as fromOption_, fromOptionK as fromOptionK_, fromPredicate as fromPredicate_ } from "./FromEither.js";
import { fromIOK as fromIOK_ } from "./FromIO.js";
import { fromTaskK as fromTaskK_ } from "./FromTask.js";
import { fromTheseK as fromTheseK_ } from "./FromThese.js";
import { flow, pipe, SK } from "./function.js";
import { flap as flap_ } from "./Functor.js";
import * as _ from "./internal.js";
import * as T from "./Task.js";
import * as TH from "./These.js";
import * as TT from "./TheseT.js"; /**
                                   * @category constructors
                                   * @since 2.4.0
                                   */
export let left = /*#__PURE__*/TT.left(T.Pointed); /**
                                                   * @category constructors
                                                   * @since 2.4.0
                                                   */
export let right = /*#__PURE__*/TT.right(T.Pointed); /**
                                                     * @category constructors
                                                     * @since 2.4.0
                                                     */
export let both = /*#__PURE__*/TT.both(T.Pointed); /**
                                                   * @category constructors
                                                   * @since 2.4.0
                                                   */
export let rightTask = /*#__PURE__*/TT.rightF(T.Functor); /**
                                                          * @category constructors
                                                          * @since 2.4.0
                                                          */
export let leftTask = /*#__PURE__*/TT.leftF(T.Functor); /**
                                                        * @category constructors
                                                        * @since 2.4.0
                                                        */
export let rightIO = /*#__PURE__*/flow(T.fromIO, rightTask); /**
                                                             * @category constructors
                                                             * @since 2.4.0
                                                             */
export let leftIO = /*#__PURE__*/flow(T.fromIO, leftTask); // -------------------------------------------------------------------------------------
// conversions
// -------------------------------------------------------------------------------------
/**
 * @category conversions
 * @since 2.10.0
 */
export let fromEither = T.of; /**
                              * @category conversions
                              * @since 2.11.0
                              */
export let fromThese = T.of; /**
                             * @category conversions
                             * @since 2.7.0
                             */
export let fromIO = rightIO; /**
                             * @category conversions
                             * @since 2.4.0
                             */
export let fromIOEither = T.fromIO; /**
                                    * @category conversions
                                    * @since 2.7.0
                                    */
export let fromTask = rightTask; /**
                                 * @category pattern matching
                                 * @since 2.10.0
                                 */
export let match = /*#__PURE__*/TT.match(T.Functor); /**
                                                     * Less strict version of [`match`](#match).
                                                     *
                                                     * The `W` suffix (short for **W**idening) means that the handler return types will be merged.
                                                     *
                                                     * @category pattern matching
                                                     * @since 2.10.0
                                                     */
export let matchW = match; /**
                           * The `E` suffix (short for **E**ffect) means that the handlers return an effect (`Task`).
                           *
                           * @category pattern matching
                           * @since 2.10.0
                           */
export let matchE = /*#__PURE__*/TT.matchE(T.Monad); /**
                                                     * Alias of [`matchE`](#matche).
                                                     *
                                                     * @category pattern matching
                                                     * @since 2.4.0
                                                     */
export let fold = matchE; /**
                          * Less strict version of [`matchE`](#matche).
                          *
                          * The `W` suffix (short for **W**idening) means that the handler return types will be merged.
                          *
                          * @category pattern matching
                          * @since 2.10.0
                          */
export let matchEW = fold; /**
                           * Alias of [`matchEW`](#matchew).
                           *
                           * @category pattern matching
                           * @since 2.10.0
                           */
export let foldW = matchEW; /**
                            * @since 2.4.0
                            */
export let swap = /*#__PURE__*/TT.swap(T.Functor);
let _map = (fa, f) => map(f)(fa); /* istanbul ignore next */
let _bimap = (fa, f, g) => bimap(f, g)(fa); /* istanbul ignore next */
let _mapLeft = (fa, f) => mapLeft(f)(fa); /**
                                          * `map` can be used to turn functions `(a: A) => B` into functions `(fa: F<A>) => F<B>` whose argument and return types
                                          * use the type constructor `F` to represent some computational context.
                                          *
                                          * @category mapping
                                          * @since 2.4.0
                                          */
export let map = /*#__PURE__*/TT.map(T.Functor); /**
                                                 * Map a pair of functions over the two type arguments of the bifunctor.
                                                 *
                                                 * @category mapping
                                                 * @since 2.4.0
                                                 */
export let bimap = /*#__PURE__*/TT.bimap(T.Functor); /**
                                                     * Map a function over the first type argument of a bifunctor.
                                                     *
                                                     * @category error handling
                                                     * @since 2.4.0
                                                     */
export let mapLeft = /*#__PURE__*/TT.mapLeft(T.Functor); /**
                                                         * @category constructors
                                                         * @since 2.7.0
                                                         */
export let of = right; /**
                       * @category type lambdas
                       * @since 2.4.0
                       */
export let URI = "TaskThese"; /**
                              * @category instances
                              * @since 2.10.0
                              */
export let getApply = (A, S) => {
  let ap = TT.ap(A, S);
  return {
    URI,
    _E: undefined,
    map: _map,
    ap: (fab, fa) => ap(fa)(fab)
  };
}; /**
   * @category instances
   * @since 2.7.0
   */
export let getApplicative = (A, S) => {
  let {
    ap
  } = getApply(A, S);
  return {
    URI,
    _E: undefined,
    map: _map,
    ap,
    of
  };
}; /**
   * @category instances
   * @since 2.10.0
   */
export let getChain = S => {
  let A = getApply(T.ApplicativePar, S);
  let chain = TT.chain(T.Monad, S);
  return {
    URI,
    _E: undefined,
    map: _map,
    ap: A.ap,
    chain: (ma, f) => chain(f)(ma)
  };
}; /**
   * @category instances
   * @since 2.4.0
   */
export let getMonad = S => {
  let A = getApplicative(T.ApplicativePar, S);
  let C = getChain(S);
  return {
    URI,
    _E: undefined,
    map: _map,
    ap: A.ap,
    of,
    chain: C.chain,
    fromIO,
    fromTask
  };
}; /**
   * @category instances
   * @since 2.10.0
   */
export let Functor = {
  URI,
  map: _map
}; /**
   * @category mapping
   * @since 2.10.0
   */
export let flap = /*#__PURE__*/flap_(Functor); /**
                                               * @category instances
                                               * @since 2.10.0
                                               */
export let Pointed = {
  URI,
  of
}; /**
   * @category instances
   * @since 2.10.0
   */
export let Bifunctor = {
  URI,
  bimap: _bimap,
  mapLeft: _mapLeft
}; /**
   * @category instances
   * @since 2.10.0
   */
export let FromEither = {
  URI,
  fromEither
}; /**
   * @category conversions
   * @since 2.10.0
   */
export let fromOption = /*#__PURE__*/fromOption_(FromEither); /**
                                                              * @category lifting
                                                              * @since 2.10.0
                                                              */
export let fromOptionK = /*#__PURE__*/fromOptionK_(FromEither); /**
                                                                * @category lifting
                                                                * @since 2.10.0
                                                                */
export let fromPredicate = /*#__PURE__*/fromPredicate_(FromEither); /**
                                                                    * @category instances
                                                                    * @since 2.11.0
                                                                    */
export let FromThese = {
  URI,
  fromThese
}; /**
   * @category lifting
   * @since 2.11.0
   */
export let fromTheseK = /*#__PURE__*/fromTheseK_(FromThese); /**
                                                             * @category instances
                                                             * @since 2.10.0
                                                             */
export let FromIO = {
  URI,
  fromIO
}; /**
   * @category lifting
   * @since 2.10.0
   */
export let fromIOK = /*#__PURE__*/fromIOK_(FromIO); /**
                                                    * @category instances
                                                    * @since 2.10.0
                                                    */
export let FromTask = {
  URI,
  fromIO,
  fromTask
}; /**
   * @category lifting
   * @since 2.10.0
   */
export let fromTaskK = /*#__PURE__*/fromTaskK_(FromTask); // -------------------------------------------------------------------------------------
// utils
// -------------------------------------------------------------------------------------
/**
 * @since 2.10.0
 */
export let toTuple2 = /*#__PURE__*/TT.toTuple2(T.Functor); /**
                                                           * @since 2.11.0
                                                           */
export let ApT = /*#__PURE__*/of(_.emptyReadonlyArray); // -------------------------------------------------------------------------------------
// array utils
// -------------------------------------------------------------------------------------
/**
 * Equivalent to `ReadonlyNonEmptyArray#traverseWithIndex(getApplicative(T.ApplicativePar, S))`.
 *
 * @category traversing
 * @since 2.11.0
 */
export let traverseReadonlyNonEmptyArrayWithIndex = S => {
  let g = TH.traverseReadonlyNonEmptyArrayWithIndex(S);
  return f => flow(T.traverseReadonlyNonEmptyArrayWithIndex(f), T.map(g(SK)));
}; /**
   * Equivalent to `ReadonlyArray#traverseWithIndex(getApplicative(T.ApplicativePar, S))`.
   *
   * @category traversing
   * @since 2.11.0
   */
export let traverseReadonlyArrayWithIndex = S => f => {
  let g = traverseReadonlyNonEmptyArrayWithIndex(S)(f);
  return as => _.isNonEmpty(as) ? g(as) : ApT;
}; /**
   * Equivalent to `ReadonlyNonEmptyArray#traverseWithIndex(getApplicative(T.ApplicativeSeq, S))`.
   *
   * @category traversing
   * @since 2.11.0
   */
export let traverseReadonlyNonEmptyArrayWithIndexSeq = S => f => as => () => _.tail(as).reduce((acc, a, i) => acc.then(ebs => TH.isLeft(ebs) ? acc : f(i + 1, a)().then(eb => {
  if (TH.isLeft(eb)) {
    return eb;
  }
  if (TH.isBoth(eb)) {
    let right = ebs.right;
    right.push(eb.right);
    return TH.isBoth(ebs) ? TH.both(S.concat(ebs.left, eb.left), right) : TH.both(eb.left, right);
  }
  ebs.right.push(eb.right);
  return ebs;
})), f(0, _.head(as))().then(TH.map(_.singleton))); /**
                                                    * Equivalent to `ReadonlyArray#traverseWithIndex(getApplicative(T.ApplicativeSeq, S))`.
                                                    *
                                                    * @category traversing
                                                    * @since 2.11.0
                                                    */
export let traverseReadonlyArrayWithIndexSeq = S => f => {
  let g = traverseReadonlyNonEmptyArrayWithIndexSeq(S)(f);
  return as => _.isNonEmpty(as) ? g(as) : ApT;
}; // -------------------------------------------------------------------------------------
// deprecated
// -------------------------------------------------------------------------------------
/**
 * Use [`Functor`](#functor) instead.
 *
 * @category zone of death
 * @since 2.7.0
 * @deprecated
 */
export let functorTaskThese = {
  URI,
  map: _map
}; /**
   * Use [`Bifunctor`](#bifunctor) instead.
   *
   * @category zone of death
   * @since 2.7.0
   * @deprecated
   */
export let bifunctorTaskThese = {
  URI,
  bimap: _bimap,
  mapLeft: _mapLeft
}; /**
   * Use [`toTuple2`](#totuple2) instead.
   *
   * @category zone of death
   * @since 2.4.0
   * @deprecated
   */
export let toTuple = (e, a) => toTuple2(() => e, () => a); /**
                                                           * This instance is deprecated, use small, specific instances instead.
                                                           * For example if a function needs a `Functor` instance, pass `TT.Functor` instead of `TT.taskThese`
                                                           * (where `TT` is from `import TT from 'fp-ts/TaskThese'`)
                                                           *
                                                           * @category zone of death
                                                           * @since 2.4.0
                                                           * @deprecated
                                                           */
export let taskThese = {
  URI,
  map: _map,
  bimap: _bimap,
  mapLeft: _mapLeft
}; /**
   * Use [`getApplySemigroup`](./Apply.ts.html#getapplysemigroup) instead.
   *
   * @category zone of death
   * @since 2.4.0
   * @deprecated
   */
export let getSemigroup = (SE, SA) => getApplySemigroup(T.ApplySeq)(TH.getSemigroup(SE, SA));
//# sourceMappingURL=TaskThese.js.map