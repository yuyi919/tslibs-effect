import { MAP } from "./internal/constant/index.js";

/**
 *
 * @remark PURE
 */
export let empty = () => new MAP();
/**
 *
 * @remark PURE
 */
export let get = (map, k) => map.get(k);
/**
 *
 * @remark PURE
 */
export let unsafeGet = get;
/**
 *
 * @remark PURE
 */
export let set = (map, key, value) => (map.set(key, value), map);
/**
 *
 * @remark PURE
 */
export let getOrInit = (map, key, value) => {
  let initial;
  return map.has(key)
    ? map.get(key)
    : (map.set(key, (initial = value())), initial);
};
/**
 *
 * @remark PURE
 */
export let remove = (map, key) => (map.delete(key), map);
/**
 *
 * @remark PURE
 */
export let clear = (map) => (map.clear(), map);
//# sourceMappingURL=JsMap.js.map
