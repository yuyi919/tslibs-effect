/**
 * 进行迭代，遇到第一个map结果不为null的元素时不的场合，将结果返回
 *
 * 未满足上述条件则继续迭代直到最后返回undefined
 * @param array
 * @param mapFn
 * @returns
 */ export let findIterableMap = (array, mapFn) => {
  let i = 0;
  let o;
  for (let key of array) {
    o = mapFn(key, i);
    if (o !== null) return o;
    i++;
  }
};
export let findArrayMap = (array, mapFn) => {
  let i = 0;
  let o;
  for (let index = 0; index < array.length; index++) {
    o = mapFn(array[index], i);
    if (o !== null) return o;
  }
};
export let nullElse = (a, e) => (a === e ? a : null);
export let nullWhen = (a, be) => (a === be ? null : a);
/**
 *
 * @param a
 * @param b
 * @param otherwise
 * @remark
 * ```ts
 * a === b ? otherwise : a
 * ```
 */
export let nullishWhen = (a, be, otherwise) => (a === be ? otherwise() : a);
//# sourceMappingURL=NonNullable.js.map
