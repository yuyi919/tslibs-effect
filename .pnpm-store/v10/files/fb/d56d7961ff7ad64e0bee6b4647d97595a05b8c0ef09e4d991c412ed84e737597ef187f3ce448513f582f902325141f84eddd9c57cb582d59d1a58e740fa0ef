export type TGen<TNext, Perform, A> = Generator<Perform, A, TNext>;
export type TGenerator<Perform, TNext, A> = Generator<Perform, A, TNext>;
export type TGenFiber<A, TNext = unknown, Perform = never> = Generator<Perform, A, TNext>;
/**
 * @internal
 * @remark
 **/
export declare const perform: <Perform, TNext = unknown>(a: Perform) => TGenFiber<TNext, TNext, Perform>;
/**
 * @internal
 * @remark
 **/
export declare const lift: <Perform, TNext = unknown>(a: Perform) => TGenFiber<void, TNext, Perform>;
export declare function map<TNext, Perform, A, B>(fa: TGen<TNext, Perform, A>, f: (a: A) => B): TGen<TNext, Perform, B>;
export declare function mapTo<TNext, Perform, A, B>(fa: TGen<TNext, Perform, A>, b: B): TGen<TNext, Perform, B>;
/** @internal */
export declare function ap<TNext, Perform, A, B>(gab: TGen<TNext, Perform, (a: A) => B>, ga: TGen<TNext, Perform, A>): TGen<TNext, Perform, B>;
export declare const chain: <TNext, Perform, A, B>(fa: TGen<TNext, Perform, A>, f: (a: A) => TGen<TNext, Perform, B>) => TGen<TNext, Perform, B>;
export declare const chainW: <TNext, PerformA, PerformB, A, B>(fa: TGen<TNext, PerformA, A>, f: (a: A) => TGen<TNext, PerformB, B>) => TGen<TNext, PerformA | PerformB, B>;
export declare const tap: <A, TNext, Perform>(ma: TGenFiber<A, TNext, Perform>, handle: (arg: A) => any) => TGenFiber<A, TNext, Perform>;
export declare function tapE<C, A, B>(ma: TGenFiber<C, A, B>, handle: (arg: C) => TGenFiber<any, A, B>): TGenFiber<C, A, B>;
/**
 * 同时迭代当前项和下一项
 * @param stream
 */
export declare function mapToWithNext<T>(stream: Iterable<T>): Generator<{
    readonly item: NonNullable<T>;
    readonly next: T;
    readonly proccessing: true;
    readonly end: false;
} | {
    readonly item: NonNullable<T>;
    readonly next: NonNullable<T>;
    readonly end: true;
    readonly proccessing?: undefined;
}, void, unknown>;
export declare enum StreamStage {
    Begin = 0,
    Content = 1,
    End = 2
}
/**
 * 同时迭代当前项和前一项和下一项
 * @param stream
 * @example
 * for (const { prev, curr, next, _tag } of iterateWithPrevAndNext([])) {
 *   console.log({ prev, curr, next, _tag });
 * }
 * => 'nothing'
 * for (const { prev, curr, next, _tag } of iterateWithPrevAndNext([1])) {
 *   console.log({ prev, curr, next, _tag });
 * }
 * => ({prev: 1, curr: 1, next: 1, _tag: IteratorStage.End})
 * @example
 * for (const { prev, curr, next, _tag } of iterateWithPrevAndNext([1, 2])) {
 *   console.log({ prev, curr, next, _tag });
 * }
 * => ({prev: 1, curr: 1, next: 2, _tag: IteratorStage.Begin})
 * => ({prev: 1, curr: 2, next: 2, _tag: IteratorStage.End})
 * @example
 * for (const { prev, curr, next, _tag } of iterateWithPrevAndNext([1, 2, 3])) {
 *   console.log({ prev, curr, next, _tag });
 * }
 * => ({prev: 1, curr: 1, next: 2, _tag: IteratorStage.Begin})
 * => ({prev: 1, curr: 2, next: 3, _tag: IteratorStage.Content})
 * => ({prev: 2, curr: 3, next: 3, _tag: IteratorStage.End})
 * @example
 * for (const { prev, curr, next, _tag } of iterateWithPrevAndNext([1, 2, 3, 4, 5])) {
 *   console.log({ prev, curr, next, _tag });
 * }
 * => ({prev: 1, curr: 1, next: 2, _tag: IteratorStage.Begin})
 * => ({prev: 1, curr: 2, next: 3, _tag: IteratorStage.Content})
 * => ({prev: 2, curr: 3, next: 4, _tag: IteratorStage.Content})
 * => ({prev: 3, curr: 4, next: 5, _tag: IteratorStage.Content})
 * => ({prev: 4, curr: 5, next: 5, _tag: IteratorStage.End})
 */
export declare function mapToWithPrevAndNext<T extends NonNullable<{}>>(stream: Iterable<T>): Iterable<{
    readonly prev: T;
    readonly curr: T;
    readonly next: T;
    readonly _tag: StreamStage;
}>;
//# sourceMappingURL=Generator.base.d.ts.map