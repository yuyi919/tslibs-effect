// Generated by ReScript, PLEASE EDIT WITH CARE
import * as Constant from "./constant/index.js";
import * as Primitive_option from "rescript/lib/es6/Primitive_option.js";
let fromIterator = Constant.ARRAY_FROM;
let fromArrayLike = Constant.ARRAY_FROM;
let fromArrayLikeWithMap = Constant.ARRAY_FROM;
let isArray = Constant.IS_ARR;
let get = (prim0, prim1) => prim0[prim1];
let set = (prim0, prim1, prim2) => {
  prim0[prim1] = prim2;
};
let length = prim => prim.length;
let makeUnsafe = Array;
let indexOfOpt = (arr, item) => {
  let index = arr.indexOf(item);
  if (index !== -1) {
    return index;
  }
};
let lastIndexOfOpt = (arr, item) => {
  let index = arr.lastIndexOf(item);
  if (index !== -1) {
    return index;
  }
};
let findIndexOpt = (fa, finder) => {
  let index = fa.findIndex(finder);
  if (index !== -1) {
    return index;
  }
};
let reduceI = (arr, init, f) => arr.reduce(f, init);
let reduce = (arr, init, f) => arr.reduce(f, init);
let reduceRight = (arr, init, f) => arr.reduceRight(f, init);
let reduceRightI = (arr, init, f) => arr.reduceRight(f, init);
let make = (length, x) => {
  if (length <= 0) {
    return [];
  } else {
    return Array(length).fill(x);
  }
};
let unsafe_fromInitializer = (length, f) => {
  let arr = Array(length);
  for (let i = 0; i < length; ++i) {
    arr[i] = f(i);
  }
  return arr;
};
let fromInitializer = (length, f) => {
  if (length <= 0) {
    return [];
  } else {
    return unsafe_fromInitializer(length, f);
  }
};
let lastIndex = a => a.length - 1 | 0;
let equalFromIndex = (fa, fb, _i, eq, len) => {
  while (true) {
    let i = _i;
    if (i === len) {
      return true;
    }
    if (!eq(fa[i], fb[i])) {
      return false;
    }
    _i = i + 1 | 0;
    continue;
  }
  ;
};
let equal = (fa, fb, eq) => {
  let len = fa.length;
  if (len === fb.length) {
    return equalFromIndex(fa, fb, 0, eq, len);
  } else {
    return false;
  }
};
let equalFromIndex$1 = (fa, fb, eq, start) => {
  let len = fa.length;
  if (len === fb.length) {
    return equalFromIndex(fa, fb, start, eq, len);
  } else {
    return false;
  }
};
let compare = (a, b, cmp) => {
  let lenA = a.length;
  let b$1 = b.length;
  let param_1 = lenA === b$1 ? 0 : lenA > b$1 ? 1 : -1;
  if (param_1 === 0) {
    let _i = 0;
    let len = lenA;
    while (true) {
      let i = _i;
      if (i === len) {
        return 0;
      }
      let c = cmp(a[i], b[i]);
      if (c !== 0) {
        return c;
      }
      _i = i + 1 | 0;
      continue;
    }
    ;
  } else {
    return 0;
  }
};
let shuffle = xs => {
  let len = length(xs);
  for (let i = 0; i < len; ++i) {
    let f = Math.random() * (len - i | 0);
    let j = (Math.floor(f) | 0) + i | 0;
    let tmp = get(xs, i);
    set(xs, i, get(xs, j));
    set(xs, j, tmp);
  }
};
let Shuffle = {
  shuffle: shuffle
};
let toShuffled = xs => {
  let result = makeUnsafe(length(xs));
  let len = length(xs);
  for (let i = 0; i < len; ++i) {
    set(result, i, get(xs, i));
  }
  for (let i$1 = 0; i$1 < len; ++i$1) {
    let f = Math.random() * (len - i$1 | 0);
    let j = (Math.floor(f) | 0) + i$1 | 0;
    let tmp = get(result, i$1);
    set(result, i$1, get(result, j));
    set(result, j, tmp);
  }
  return result;
};
let ToShuffled = {
  toShuffled: toShuffled
};
let filterMap = (arr, f) => {
  let l = arr.length;
  let r = Array(l);
  let j = 0;
  for (let i = 0; i < l; ++i) {
    let v = f(arr[i]);
    if (v !== undefined) {
      r[j] = Primitive_option.valFromOption(v);
      j = j + 1 | 0;
    }
  }
  r.length = j;
  return r;
};
let keepSome = __x => filterMap(__x, x => x);
let findMap = (arr, f) => {
  let _i = 0;
  while (true) {
    let i = _i;
    if (i === arr.length) {
      return;
    }
    let r = f(arr[i]);
    if (r !== undefined) {
      return r;
    }
    _i = i + 1 | 0;
    continue;
  }
  ;
};
let atN = (arr, i) => {
  if (i < 0) {
    return arr[arr.length + i | 0];
  } else {
    return arr[i];
  }
};
let atCyclic = (xs, i) => {
  let len = xs.length;
  if (len === 0) {
    throw {
      RE_EXN_ID: "Invalid_argument",
      _1: "StdArray.atCyclic: arrays.length == 0",
      Error: new Error()
    };
  }
  return xs[i !== 0 ? i < 0 ? (len + i % len | 0) % len : i % len : 0];
};
let atLast = (arr, i) => arr[(arr.length - i | 0) + 1 | 0];
let last = a => a[a.length - 1 | 0];
let copyAllWithin = (prim0, prim1) => prim0.copyWithin(prim1);
let copyWithinToEnd = (prim0, prim1, prim2) => prim0.copyWithin(prim1, prim2);
let copyWithin = (prim0, prim1, prim2, prim3) => prim0.copyWithin(prim1, prim2, prim3);
let fillAll = (prim0, prim1) => prim0.fill(prim1);
let fillToEnd = (prim0, prim1, prim2) => prim0.fill(prim1, prim2);
let fill = (prim0, prim1, prim2, prim3) => prim0.fill(prim1, prim2, prim3);
let pop = prim => prim.pop();
let push = (prim0, prim1) => {
  prim0.push(prim1);
};
let reverse = prim => {
  prim.reverse();
};
let shift = prim => prim.shift();
let sort = (prim0, prim1) => {
  prim0.sort(prim1);
};
let withAt = (prim0, prim1, prim2) => prim0.with(prim1, prim2);
let unshift = (prim0, prim1) => {
  prim0.unshift(prim1);
};
let unshiftMany = (prim0, prim1) => {
  prim0.unshift(...prim1);
};
let concat = (prim0, prim1) => prim0.concat(prim1);
let flat = prim => prim.flat();
let includes = (prim0, prim1) => prim0.includes(prim1);
let indexOf = (prim0, prim1) => prim0.indexOf(prim1);
let indexOfOpt$1 = indexOfOpt;
let indexOfFrom = (prim0, prim1, prim2) => prim0.indexOf(prim1, prim2);
let joinWith = (prim0, prim1) => prim0.join(prim1);
let joinWithUnsafe = (prim0, prim1) => prim0.join(prim1);
let join = prim => prim.join();
let joinUnsafe = prim => prim.join();
let lastIndexOf = (prim0, prim1) => prim0.lastIndexOf(prim1);
let lastIndexOfOpt$1 = lastIndexOfOpt;
let lastIndexOfFrom = (prim0, prim1, prim2) => prim0.lastIndexOf(prim1, prim2);
let slice = (prim0, prim1, prim2) => prim0.slice(prim1, prim2);
let sliceToEnd = (prim0, prim1) => prim0.slice(prim1);
let copy = prim => prim.slice(0);
let toString = prim => prim.toString();
let toLocaleString = prim => prim.toLocaleString();
let every = (prim0, prim1) => prim0.every(prim1);
let everyI = (prim0, prim1) => prim0.every(prim1);
let filter = (prim0, prim1) => prim0.filter(prim1);
let filterI = (prim0, prim1) => prim0.filter(prim1);
let find = (prim0, prim1) => prim0.find(prim1);
let findI = (prim0, prim1) => prim0.find(prim1);
let findIndex = (prim0, prim1) => prim0.findIndex(prim1);
let findIndexI = (prim0, prim1) => prim0.findIndex(prim1);
let forEach = (prim0, prim1) => {
  prim0.forEach(prim1);
};
let forEachI = (prim0, prim1) => {
  prim0.forEach(prim1);
};
let map = (prim0, prim1) => prim0.map(prim1);
let mapI = (prim0, prim1) => prim0.map(prim1);
let reduce$1 = reduce;
let reduceI$1 = reduceI;
let reduceRight$1 = reduceRight;
let reduceRightI$1 = reduceRightI;
let some = (prim0, prim1) => prim0.some(prim1);
let someI = (prim0, prim1) => prim0.some(prim1);
let findIndexOpt$1 = findIndexOpt;
let toReversed = prim => prim.toReversed();
let toSorted = (prim0, prim1) => prim0.toSorted(prim1);
let toShuffled$1 = ToShuffled.toShuffled;
let shuffle$1 = Shuffle.shuffle;
let flatMap = (prim0, prim1) => prim0.flatMap(prim1);
let at = (prim0, prim1) => prim0.at(prim1);
export { fromIterator, fromArrayLike, fromArrayLikeWithMap, make, unsafe_fromInitializer, fromInitializer, equal, equalFromIndex$1 as equalFromIndex, compare, isArray, lastIndex, copyAllWithin, copyWithinToEnd, copyWithin, fillAll, fillToEnd, fill, pop, push, reverse, shift, sort, withAt, unshift, unshiftMany, concat, flat, includes, indexOf, indexOfOpt$1 as indexOfOpt, indexOfFrom, joinWith, joinWithUnsafe, join, joinUnsafe, lastIndexOf, lastIndexOfOpt$1 as lastIndexOfOpt, lastIndexOfFrom, slice, sliceToEnd, copy, toString, toLocaleString, every, everyI, filter, filterI, find, findI, findIndex, findIndexI, forEach, forEachI, map, mapI, reduce$1 as reduce, reduceI$1 as reduceI, reduceRight$1 as reduceRight, reduceRightI$1 as reduceRightI, some, someI, findIndexOpt$1 as findIndexOpt, toReversed, toSorted, filterMap, keepSome, toShuffled$1 as toShuffled, shuffle$1 as shuffle, flatMap, findMap, at, atN, atLast, atCyclic, last }; /*  Not a pure module */
//# sourceMappingURL=Array.bs.js.map