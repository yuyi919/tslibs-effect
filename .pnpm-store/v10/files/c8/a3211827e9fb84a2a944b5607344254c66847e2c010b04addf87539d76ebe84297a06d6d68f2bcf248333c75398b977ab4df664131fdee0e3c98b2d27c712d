// Generated by ReScript, PLEASE EDIT WITH CARE
let emoji = {
  log: "🗯️",
  success: "✔️",
  info: "ℹ️",
  error: "❌",
  warn: "⚠️",
  debug: "⚙️",
  trace: "🔎",
  begin: "🔥",
  end: "😊",
  time: "⏳",
  cost: "⌛",
  count: "⏲️"
};
let fill = color => "background:" + color + ";border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;";
let text = color => "color:" + color + ";font-weight:bold;margin-right:4px;";
let dict = {
  fill: fill,
  text: text
};
let NameStyles = {
  fill: fill,
  text: text,
  dict: dict
};
let logMany = (logger, args) => {
  logger.log.apply(null, args);
};
let MakeLogger = ILogger => {
  let toArgs = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return ["%c%s%c " + match, "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args.slice(1));
    }
    return ["%c%s%c %s", "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args);
  };
  let logger = ILogger.logger;
  let enable = ILogger.enable;
  let debuggerGroupCollapsed = ILogger.groupCollapsed;
  let debug = a => {
    if (enable) {
      logger.debug(a);
      return;
    }
  };
  let debug1 = (a, a2) => {
    if (enable) {
      logger.debug(a, a2);
      return;
    }
  };
  let debug2 = (a, a2, a3) => {
    if (enable) {
      logger.debug(a, a2, a3);
      return;
    }
  };
  let debug3 = (a, a2, a3, a4) => {
    if (enable) {
      logger.debug(a, a2, a3, a4);
      return;
    }
  };
  let debug4 = (a, a2, a3, a4, a5) => {
    if (enable) {
      logger.debug(a, a2, a3, a4, a5);
      return;
    }
  };
  let debug5 = (a, a2, a3, a4, a5, a6) => {
    if (enable) {
      logger.debug(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let debug6 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable) {
      logger.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
  };
  let banner = a => {
    if (enable) {
      logger.banner(a);
      return;
    }
  };
  let banner1 = (a, a2) => {
    if (enable) {
      logger.banner(a, a2);
      return;
    }
  };
  let banner2 = (a, a2, a3) => {
    if (enable) {
      logger.banner(a, a2, a3);
      return;
    }
  };
  let banner3 = (a, a2, a3, a4) => {
    if (enable) {
      logger.banner(a, a2, a3, a4);
      return;
    }
  };
  let banner4 = (a, a2, a3, a4, a5) => {
    if (enable) {
      logger.banner(a, a2, a3, a4, a5);
      return;
    }
  };
  let banner5 = (a, a2, a3, a4, a5, a6) => {
    if (enable) {
      logger.banner(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let log = a => {
    if (enable) {
      logger.log(a);
      return;
    }
  };
  let log1 = (a, a2) => {
    if (enable) {
      logger.log(a, a2);
      return;
    }
  };
  let log2 = (a, a2, a3) => {
    if (!enable) {
      return;
    }
    let args = toArgs(ILogger.label, [a, a2, a3]);
    logger.log.apply(null, args);
  };
  let log3 = (a, a2, a3, a4) => {
    if (enable) {
      logger.log(a, a2, a3, a4);
      return;
    }
  };
  let log4 = (a, a2, a3, a4, a5) => {
    if (enable) {
      logger.log(a, a2, a3, a4, a5);
      return;
    }
  };
  let log5 = (a, a2, a3, a4, a5, a6) => {
    if (enable) {
      logger.log(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let groupCollapsed = a => {
    if (enable) {
      if (debuggerGroupCollapsed) {
        logger.groupCollapsed(a);
      } else {
        logger.group(a);
      }
      return;
    }
  };
  let groupCollapsed1 = (a, a1) => {
    if (enable) {
      if (debuggerGroupCollapsed) {
        logger.groupCollapsed(a, a1);
      } else {
        logger.group(a, a1);
      }
      return;
    }
  };
  let groupCollapsed2 = (a, a1, a2) => {
    if (enable) {
      if (debuggerGroupCollapsed) {
        logger.groupCollapsed(a, a1, a2);
      } else {
        logger.group(a, a1, a2);
      }
      return;
    }
  };
  let groupCollapsed3 = (a, a1, a2, a3) => {
    if (enable) {
      if (debuggerGroupCollapsed) {
        logger.groupCollapsed(a, a1, a2, a3);
      } else {
        logger.group(a, a1, a2, a3);
      }
      return;
    }
  };
  let groupCollapsed4 = (a, a1, a2, a3, a4) => {
    if (enable) {
      if (debuggerGroupCollapsed) {
        logger.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger.group(a, a1, a2, a3, a4);
      }
      return;
    }
  };
  let groupCollapsed5 = (a, a1, a2, a3, a4, a5) => {
    if (enable) {
      if (debuggerGroupCollapsed) {
        logger.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
  };
  let groupCollapsed6 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable) {
      if (debuggerGroupCollapsed) {
        logger.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
  };
  let groupCollapsed7 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable) {
      if (debuggerGroupCollapsed) {
        logger.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
  };
  let groupEnd = () => {
    if (enable) {
      logger.groupEnd();
      return;
    }
  };
  let groupEnd1 = a => {
    if (enable) {
      logger.groupEnd(a);
      return;
    }
  };
  let groupEnd2 = (a, a1) => {
    if (enable) {
      logger.groupEnd(a, a1);
      return;
    }
  };
  let groupEnd3 = (a, a1, a2) => {
    if (enable) {
      logger.groupEnd(a, a1, a2);
      return;
    }
  };
  return {
    toArgs: toArgs,
    logger: logger,
    enable: enable,
    debuggerGroupCollapsed: debuggerGroupCollapsed,
    debug: debug,
    debug1: debug1,
    debug2: debug2,
    debug3: debug3,
    debug4: debug4,
    debug5: debug5,
    debug6: debug6,
    banner: banner,
    banner1: banner1,
    banner2: banner2,
    banner3: banner3,
    banner4: banner4,
    banner5: banner5,
    log: log,
    log1: log1,
    log2: log2,
    log3: log3,
    log4: log4,
    log5: log5,
    groupCollapsed: groupCollapsed,
    groupCollapsed1: groupCollapsed1,
    groupCollapsed2: groupCollapsed2,
    groupCollapsed3: groupCollapsed3,
    groupCollapsed4: groupCollapsed4,
    groupCollapsed5: groupCollapsed5,
    groupCollapsed6: groupCollapsed6,
    groupCollapsed7: groupCollapsed7,
    groupEnd: groupEnd,
    groupEnd1: groupEnd1,
    groupEnd2: groupEnd2,
    groupEnd3: groupEnd3
  };
};
let colorMap = {
  green: "mediumseagreen",
  blue: "#00bbee",
  red: "#c0392b",
  yellow: "#f39c12",
  grey: "#999",
  default: "#999"
};
export { emoji, NameStyles, colorMap, logMany, MakeLogger }; /* Array-Std Not a pure module */
//# sourceMappingURL=Logger.bs.js.map