import * as PR from "./parser-ts/ParseResult"

/**
 * @category combinators
 * @since 0.6.0
 */
export const escalateMutable: <I>(err: PR.ParseError<I>) => PR.ParseError<I> = (
  e,
) => (console.log("escalate", PR.getInput(e)), PR.setFatal(e, true), e)

export * from "./parser-ts/ParseResult"
