import { isFn } from "@yuyi919/shared-proto/JsTypes";

import { hasWindow } from "./Browser.js";

export let addEventListener = (target, name, listener, options) =>
  target.addEventListener(name, listener, options);
export let removeEventListener = (target, name, listener, options) =>
  target.removeEventListener(name, listener, options);
export let getTargetElement = (target, defaultElement) => {
  if (!hasWindow) {
    return undefined;
  }
  if (!target) {
    return defaultElement;
  }
  let targetElement;
  if (isFn(target)) {
    targetElement = target();
  } else if ("current" in target) {
    targetElement = target.current;
  } else {
    targetElement = target;
  }
  return targetElement;
};
//# sourceMappingURL=dom.js.map
