/**
 * ```ts
 * interface Separated<E, A> {
 *    readonly left: E
 *    readonly right: A
 * }
 * ```
 *
 * Represents a result of separating a whole into two parts.
 *
 * @since 2.10.0
 */import { pipe } from "./function.js";
import { flap as flap_ } from "./Functor.js"; // -------------------------------------------------------------------------------------
// constructors
// -------------------------------------------------------------------------------------
/**
 * @category constructors
 * @since 2.10.0
 */
export let separated = (left, right) => ({
  left,
  right
});
let _map = (fa, f) => map(f)(fa);
let _mapLeft = (fa, f) => mapLeft(f)(fa);
let _bimap = (fa, g, f) => bimap(g, f)(fa); /**
                                            * `map` can be used to turn functions `(a: A) => B` into functions `(fa: F<A>) => F<B>` whose argument and return types
                                            * use the type constructor `F` to represent some computational context.
                                            *
                                            * @category mapping
                                            * @since 2.10.0
                                            */
export let map = f => fa => separated(left(fa), f(right(fa))); /**
                                                               * Map a function over the first type argument of a bifunctor.
                                                               *
                                                               * @category error handling
                                                               * @since 2.10.0
                                                               */
export let mapLeft = f => fa => separated(f(left(fa)), right(fa)); /**
                                                                   * Map a pair of functions over the two type arguments of the bifunctor.
                                                                   *
                                                                   * @category mapping
                                                                   * @since 2.10.0
                                                                   */
export let bimap = (f, g) => fa => separated(f(left(fa)), g(right(fa))); /**
                                                                         * @category type lambdas
                                                                         * @since 2.10.0
                                                                         */
export let URI = "Separated"; /**
                              * @category instances
                              * @since 2.10.0
                              */
export let Bifunctor = {
  URI,
  mapLeft: _mapLeft,
  bimap: _bimap
}; /**
   * @category instances
   * @since 2.10.0
   */
export let Functor = {
  URI,
  map: _map
}; /**
   * @category mapping
   * @since 2.10.0
   */
export let flap = /*#__PURE__*/flap_(Functor); // -------------------------------------------------------------------------------------
// utils
// -------------------------------------------------------------------------------------
/**
 * @since 2.10.0
 */
export let left = s => s.left; /**
                               * @since 2.10.0
                               */
export let right = s => s.right;
//# sourceMappingURL=Separated.js.map