// Generated by ReScript, PLEASE EDIT WITH CARE
let Copy = M => {
  let copy = xs => {
    let result = M.makeUnsafe(M.length(xs));
    let len = M.length(xs);
    for (let i = 0; i < len; ++i) {
      M.set(result, i, M.get(xs, i));
    }
    return result;
  };
  return {
    copy: copy
  };
};
let Swap = M => {
  let swap = (fa, i, j) => {
    let tmp = M.get(fa, i);
    M.set(fa, i, M.get(fa, j));
    M.set(fa, j, tmp);
  };
  return {
    swap: swap
  };
};
let Shuffle = M => {
  let shuffle = xs => {
    let len = M.length(xs);
    for (let i = 0; i < len; ++i) {
      let f = Math.random() * (len - i | 0);
      let j = (Math.floor(f) | 0) + i | 0;
      let tmp = M.get(xs, i);
      M.set(xs, i, M.get(xs, j));
      M.set(xs, j, tmp);
    }
  };
  return {
    shuffle: shuffle
  };
};
let ToShuffled = M => {
  let toShuffled = xs => {
    let result = M.makeUnsafe(M.length(xs));
    let len = M.length(xs);
    for (let i = 0; i < len; ++i) {
      M.set(result, i, M.get(xs, i));
    }
    for (let i$1 = 0; i$1 < len; ++i$1) {
      let f = Math.random() * (len - i$1 | 0);
      let j = (Math.floor(f) | 0) + i$1 | 0;
      let tmp = M.get(result, i$1);
      M.set(result, i$1, M.get(result, j));
      M.set(result, j, tmp);
    }
    return result;
  };
  return {
    toShuffled: toShuffled
  };
};
export { Copy, Swap, Shuffle, ToShuffled }; /* No side effect */
//# sourceMappingURL=ArrayLike.bs.js.map