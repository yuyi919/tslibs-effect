/**
 * @since 0.6.0
 */ import { not } from "fp-ts/Predicate";
import { Monoid } from "fp-ts/string";

import * as P from "./Parser.js";

let maybe = /*#__PURE__*/ P.maybe(Monoid);
// -------------------------------------------------------------------------------------
// model
// -------------------------------------------------------------------------------------
/**
 * @category model
 * @since 0.6.0
 */ //`${DynamicString}${string}` & {}
// -------------------------------------------------------------------------------------
// constructors
// -------------------------------------------------------------------------------------
/**
 * The `char` parser constructor returns a parser which matches only the
 * specified single character
 *
 * @category constructors
 * @since 0.6.0
 */
export let char = (c) =>
  P.expected(
    P.sat((s) => s === c),
    `"${c}"`,
  );
/**
 * The `notChar` parser constructor makes a parser which will match any
 * single character other than the one provided.
 *
 * @category constructors
 * @since 0.6.0
 */
export let notChar = (c) =>
  P.expected(
    P.sat((c1) => c1 !== c),
    `anything but "${c}"`,
  );
let isOneOf = (s, c) => s.includes(c);
/**
 * Matches any one character from the provided string.
 *
 * @category constructors
 * @since 0.6.0
 */
export let oneOf = (s) =>
  P.expected(
    P.sat((c) => isOneOf(s, c)),
    `One of "${s}"`,
  );
/**
 * Matches a single character which isn't a character from the provided string.
 *
 * @category constructors
 * @since 0.6.0
 */
export let notOneOf = (s) =>
  P.expected(
    P.sat((c) => !isOneOf(s, c)),
    `Not one of ${JSON.stringify(s)}`,
  );
// -------------------------------------------------------------------------------------
// combinators
// -------------------------------------------------------------------------------------
/**
 * Takes a `Parser<Char, Char>` and matches it zero or more times, returning
 * a `string` of what was matched.
 *
 * @category combinators
 * @since 0.6.0
 */
export let many = (parser) => maybe(many1(parser));
/**
 * Takes a `Parser<Char, Char>` and matches it one or more times, returning
 * a `string` of what was matched.
 *
 * @category combinators
 * @since 0.6.0
 */
export let many1 = /*#__PURE__*/ P.getMany1C(Monoid);
let isDigit = (c) => /* LAZY */ "0123456789".includes(c);
/**
 * Matches a single digit.
 *
 * @category combinators
 * @since 0.6.0
 */
export let digit = /*#__PURE__*/ P.expected(
  /*#__PURE__*/ P.sat(isDigit),
  "a digit",
);
let spaceRe = /^\s$/;
let isSpace = (c) => /* LAZY */ spaceRe.test(c);
/**
 * Matches a single whitespace character.
 *
 * @category combinators
 * @since 0.6.0
 */
export let space = /*#__PURE__*/ P.expected(
  /*#__PURE__*/ P.sat(isSpace),
  "a whitespace",
);
let isUnderscore = (c) => c === "_";
let isLetter = (c) => /[a-z]/.test(c.toLowerCase());
let isAlphanum = (c) => isLetter(c) || isDigit(c) || isUnderscore(c);
/**
 * Matches a single letter, digit or underscore character.
 *
 * @category combinators
 * @since 0.6.0
 */
export let alphanum = /*#__PURE__*/ P.expected(
  /*#__PURE__*/ P.sat(isAlphanum),
  "a word character",
);
/**
 * Matches a single ASCII letter.
 *
 * @since 0.6.0
 */
export let letter = /*#__PURE__*/ P.expected(
  /*#__PURE__*/ P.sat(isLetter),
  "a letter",
);
let isUnicodeLetter = (c) => c.toLowerCase() !== c.toUpperCase();
/**
 * Matches a single Unicode letter.
 * Works for scripts which have a notion of an upper case and lower case letters
 * (Latin-based scripts, Greek, Russian etc).
 *
 * @category combinators
 * @since 0.6.16
 */
export let unicodeLetter = /*#__PURE__*/ P.expected(
  /*#__PURE__*/ P.sat(isUnicodeLetter),
  "an unicode letter",
);
let isUpper = (c) => isLetter(c) && c === c.toUpperCase();
/**
 * Matches a single upper case ASCII letter.
 *
 * @category combinators
 * @since 0.6.0
 */
export let upper = /*#__PURE__*/ P.expected(
  /*#__PURE__*/ P.sat(isUpper),
  "an upper case letter",
);
let isLower = (c) => isLetter(c) && c === c.toLowerCase();
/**
 * Matches a single lower case ASCII letter.
 *
 * @category combinators
 * @since 0.6.0
 */
export let lower = /*#__PURE__*/ P.expected(
  /*#__PURE__*/ P.sat(isLower),
  "a lower case letter",
);
/**
 * Matches a single character which isn't a digit.
 *
 * @category combinators
 * @since 0.6.0
 */
export let notDigit = /*#__PURE__*/ P.expected(
  /*#__PURE__*/ P.sat(/*#__PURE__*/ not(isDigit)),
  "a non-digit",
);
/**
 * Matches a single character which isn't whitespace.
 *
 * @category combinators
 * @since 0.6.0
 */
export let notSpace = /*#__PURE__*/ P.expected(
  /*#__PURE__*/ P.sat(/*#__PURE__*/ not(isSpace)),
  "a non-whitespace character",
);
/**
 * Matches a single character which isn't a letter, digit or underscore.
 *
 * @category combinators
 * @since 0.6.0
 */
export let notAlphanum = /*#__PURE__*/ P.expected(
  /*#__PURE__*/ P.sat(/*#__PURE__*/ not(isAlphanum)),
  "a non-word character",
);
/**
 * Matches a single character which isn't an ASCII letter.
 *
 * @category combinators
 * @since 0.6.0
 */
export let notLetter = /*#__PURE__*/ P.expected(
  /*#__PURE__*/ P.sat(/*#__PURE__*/ not(isLetter)),
  "a non-letter character",
);
/**
 * Matches a single character which isn't an upper case ASCII letter.
 *
 * @category combinators
 * @since 0.6.0
 */
export let notUpper = /*#__PURE__*/ P.expected(
  /*#__PURE__*/ P.sat(/*#__PURE__*/ not(isUpper)),
  "anything but an upper case letter",
);
/**
 * Matches a single character which isn't a lower case ASCII letter.
 *
 * @category combinators
 * @since 0.6.0
 */
export let notLower = /*#__PURE__*/ P.expected(
  /*#__PURE__*/ P.sat(/*#__PURE__*/ not(isLower)),
  "anything but a lower case letter",
);
//# sourceMappingURL=char.js.map
