// Generated by ReScript, PLEASE EDIT WITH CARE
let toString = o => o;
let $$return = (param, t) => t;
let switchTo = (param, t) => t;
let compute = (a, cb) => cb(a);
let switchWith = (param, cb) => cb();
let tap = (a, cb) => (cb(), a);
let effect = (a, cb) => (cb(a), a);
let charCodeAt = (str, int) => str.codePointAt(int);
let charAt = (str, int) => str.codePointAt(int);
let charStrAt = (str, int) => str[int];
let is32Bit = (str, int) => str.codePointAt(int) > 65535;
let test = (prim0, prim1) => prim0.test(prim1);
let getStringLength = str => {
  let realLength = 0;
  let charCode = 0;
  for (let i = 0, i_finish = str.length; i < i_finish; ++i) {
    charCode = str.codePointAt(i);
    realLength = charCode >= 0 && charCode <= 128 ? realLength + 1 | 0 : realLength + 2 | 0;
  }
  return realLength;
};
export { toString, $$return, switchTo, compute, switchWith, tap, effect, charCodeAt, charAt, charStrAt, is32Bit, test, getStringLength }; /* No side effect */
//# sourceMappingURL=Shared.bs.js.map