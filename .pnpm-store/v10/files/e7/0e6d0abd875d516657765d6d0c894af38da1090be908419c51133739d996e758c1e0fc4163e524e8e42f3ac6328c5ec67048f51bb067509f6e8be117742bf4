import {
  isArguments,
  isArr,
  isBigInt,
  isBool,
  isDate,
  isEmpty,
  isError,
  isFn,
  isNaN,
  isNil,
  isNull,
  isNum,
  isObj,
  isPrimitive,
  isPromise,
  isRegExp,
  isStr,
  isSymbol,
  isThenable,
  isUndefined,
  isEsModule,
} from "./checker";
import type { ToUpperCaseFirst, ToLowerCaseFirst } from "../namespaces/string";
declare const checker: {
  isArguments: typeof isArguments;
  isArr: typeof isArr;
  isBigInt: typeof isBigInt;
  isBool: typeof isBool;
  isDate: typeof isDate;
  isEmpty: typeof isEmpty;
  isError: typeof isError;
  isFn: typeof isFn;
  isNaN: typeof isNaN;
  isNil: typeof isNil;
  isNull: typeof isNull;
  isNum: typeof isNum;
  isObj: typeof isObj;
  isPrimitive: typeof isPrimitive;
  isPromise: typeof isPromise;
  isRegExp: typeof isRegExp;
  isStr: typeof isStr;
  isSymbol: typeof isSymbol;
  isThenable: typeof isThenable;
  isUndefined: typeof isUndefined;
  isEsModule: typeof isEsModule;
};
/**
 * @public
 */
type ComputedCheckerKey<K extends string> = K extends `${infer A}${infer B}`
  ? `is${ToUpperCaseFirst<A>}${B}`
  : K;
/**
 * @public
 */
export type CheckerHandles = {
  [K in Extract<keyof typeof checker, `is${string}`>]: (typeof checker)[K];
};
/**
 * @public
 */
export type CheckerHandleFactory<
  CheckerHandles extends Record<string, any>,
  Keys extends keyof CheckerHandles = keyof CheckerHandles,
> = {
  [K in Extract<Keys, `is${string}`> extends `is${infer type}`
    ? type extends "NaN"
      ? "NaN"
      : ToLowerCaseFirst<type>
    : never]: CheckerHandles[ComputedCheckerKey<K>];
};
/**
 * @public
 */
export type Checker = CheckerHandleFactory<CheckerHandles>;
/**
 * @public
 * see {@link Checker}
 */
export declare const is: Checker;
export * from "./checker";
/**
 * 强制类型转换
 *
 * I sure hope you know what you are doing
 * @public
 * @param target -
 */
export declare function as<T>(target: any): T;
//# sourceMappingURL=index.d.ts.map
