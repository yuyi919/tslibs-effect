// Generated by ReScript, PLEASE EDIT WITH CARE
import * as TypedArrays$Std from "./TypedArrays.bs.js";
let Float64Array_Constants = TypedArrays$Std.$$Float64Array.Constants;
let Float64Array_fromTypedArray = TypedArrays$Std.$$Float64Array.fromTypedArray;
let $$Float64Array = {
  Constants: Float64Array_Constants,
  fromTypedArray: Float64Array_fromTypedArray
};
let Make = $star => {
  let fromLength = prim => new Float64Array(prim);
  let fromBufferRange = (buf, offset, length) => new Float64Array(buf, offset, length);
  let get = (array, index) => array[index];
  let set = (array, index, v) => {
    array[index] = v;
  };
  let plusplus = (array, index) => {
    array[index] = array[index] + 1;
  };
  let minusminus = (array, index) => {
    array[index] = array[index] - 1;
  };
  let plus = (array, index, v) => {
    array[index] = array[index] + v;
  };
  let grow_to = (array, index, v) => {
    if (index > array.length) {
      let r = () => {};
      console.log(r);
      throw new RangeError("");
    }
    if (v > array[index]) {
      array[index] = v;
      return;
    }
  };
  let minus = (array, index, v) => {
    array[index] = array[index] - v;
  };
  return {
    fromLength: fromLength,
    fromBufferRange: fromBufferRange,
    get: get,
    set: set,
    plusplus: plusplus,
    minusminus: minusminus,
    plus: plus,
    grow_to: grow_to,
    minus: minus
  };
};
let fromLength = prim => new Float64Array(prim);
let fromBufferRange = (buf, offset, length) => new Float64Array(buf, offset, length);
let get = (array, index) => array[index];
let set = (array, index, v) => {
  array[index] = v;
};
let plusplus = (array, index) => {
  array[index] = array[index] + 1;
};
let minusminus = (array, index) => {
  array[index] = array[index] - 1;
};
let plus = (array, index, v) => {
  array[index] = array[index] + v;
};
let grow_to = (array, index, v) => {
  if (index > array.length) {
    let r = () => {};
    console.log(r);
    throw new RangeError("");
  }
  if (v > array[index]) {
    array[index] = v;
    return;
  }
};
let minus = (array, index, v) => {
  array[index] = array[index] - v;
};
export { $$Float64Array, Make, fromLength, fromBufferRange, get, set, plusplus, minusminus, plus, grow_to, minus }; /* TypedArrays-Std Not a pure module */
//# sourceMappingURL=F64Array.bs.js.map