// Generated by ReScript, PLEASE EDIT WITH CARE
import * as ElementType$Std from "./ElementType.bs.js";
import * as Primitive_curry from "rescript/lib/es6/Primitive_curry.js";
let MakeProcess = Opts => {
  let CompressOptions = Opts.CompressOptions;
  let Config = {
    $$debugger: CompressOptions.$$debugger
  };
  let $$return = (param, t) => t;
  let switchTo = (param, t) => t;
  let tapIf = (context, exp, fn) => {
    if (exp(context)) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let tapIfConst = (context, exp, fn) => {
    if (exp) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let boolean = (context, exp) => [exp(context) ? "True" : "False", context];
  let onTrue = (context, fn) => {
    if (context[0] === "False") {
      return context;
    } else {
      return ["True", fn(context[1])];
    }
  };
  let onFalse = (context, fn) => {
    if (context[0] === "False") {
      return ["False", fn(context[1])];
    } else {
      return context;
    }
  };
  let unwrap = context => context[1];
  let computeIfConst = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIf = (context, exp, fn) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIfElse = (context, exp, fn, else$p) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return else$p(context);
    }
  };
  let pipeIfConst = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let switchIf = (context, exp, else$p, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let check = (context, if$p, else$p, fn) => {
    if (if$p(context)) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let pipe = (context, fn) => fn(context);
  let computeIfElse = (context, exp, fn, fn2) => {
    if (exp) {
      return fn(context);
    } else {
      return fn2(context);
    }
  };
  let action = (context, fn) => {
    fn(context);
    return context;
  };
  let actionU = (context, fn) => {
    fn(context);
    return context;
  };
  let actionWith = (context, withCtx, fn) => {
    fn(withCtx);
    return context;
  };
  let tap = (context, fn) => {
    fn();
    return context;
  };
  let debugTap = (context, fn) => {
    if (Config.$$debugger) {
      fn();
      return context;
    } else {
      return context;
    }
  };
  let debug = (context, fn) => {
    if (Config.$$debugger) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let ignore = (context, withCtx) => context;
  let call = (context, arg) => context(arg);
  let rev = (b, fn, a) => fn(a, b);
  let Pipe = {
    $$return: $$return,
    switchTo: switchTo,
    tapIf: tapIf,
    tapIfConst: tapIfConst,
    boolean: boolean,
    onTrue: onTrue,
    onFalse: onFalse,
    unwrap: unwrap,
    computeIfConst: computeIfConst,
    pipeIf: pipeIf,
    pipeIfElse: pipeIfElse,
    pipeIfConst: pipeIfConst,
    switchIf: switchIf,
    check: check,
    pipe: pipe,
    computeIfElse: computeIfElse,
    action: action,
    actionU: actionU,
    actionWith: actionWith,
    tap: tap,
    debugTap: debugTap,
    debug: debug,
    ignore: ignore,
    call: call,
    rev: rev
  };
  let env = {
    _gridSize: 0.0,
    _yInterval: 0.0,
    _xInterval: 0.0,
    _spaceWidth: 0.0,
    _maxRow: 0,
    _maxCol: 0.0,
    _lastCol: 0.0,
    _letterSpacing: 0.0,
    _forceSpace: 0.0,
    _maxWidth: 0.0,
    _paddingLeft: 0.0,
    _hasLeftQuote: false,
    _firstChar: 0,
    _flag_STDWIDTH: false,
    _gridWidth: 0.0
  };
  let compressOptions_inlineCompression = CompressOptions.inlineCompression;
  let compressOptions_forceGridAlignment = CompressOptions.forceGridAlignment;
  let compressOptions_westernCharacterFirst = CompressOptions.westernCharacterFirst;
  let compressOptions_forceSpaceBetweenCJKAndWestern = CompressOptions.forceSpaceBetweenCJKAndWestern;
  let compressOptions_fixCJKLeftQuote = CompressOptions.fixCJKLeftQuote;
  let compressOptions = {
    inlineCompression: compressOptions_inlineCompression,
    forceGridAlignment: compressOptions_forceGridAlignment,
    westernCharacterFirst: compressOptions_westernCharacterFirst,
    forceSpaceBetweenCJKAndWestern: compressOptions_forceSpaceBetweenCJKAndWestern,
    fixCJKLeftQuote: compressOptions_fixCJKLeftQuote
  };
  let initlize = (env, options, firstChar, hasLeftQuote, paddingLeft) => {
    env._firstChar = firstChar;
    env._hasLeftQuote = hasLeftQuote;
    env._paddingLeft = paddingLeft;
    env._gridSize = options.gridSize;
    env._forceSpace = CompressOptions.forceSpaceBetweenCJKAndWestern ? env._gridSize / 4.0 : 0.0;
    env._spaceWidth = env._gridSize * 0.35;
    env._xInterval = options.xInterval;
    env._yInterval = options.yInterval;
    env._maxCol = options.column;
    env._lastCol = env._maxCol - 1.0;
    env._maxRow = options.row;
    let value = options.wrapWidth;
    env._maxWidth = value !== undefined ? value : env._maxCol * env._gridSize;
    env._letterSpacing = options.letterSpacing;
    env._gridWidth = env._gridSize + env._xInterval;
    return env;
  };
  let Env = {
    env: env,
    compressOptions: compressOptions,
    initlize: initlize
  };
  let context = {
    _END: 0,
    _currentX: 0.0,
    _currentY: 0.0,
    _currentCol: 0.0,
    _currentRow: 1,
    _lastIsPunctuation: false,
    _lastPunctuations: 0.0,
    _isNeedForceWrap: false,
    _lastCharWidth: 0.0,
    _contentWidth: 0.0,
    _contentHeight: 0.0,
    _currentRowHeight: 0.0,
    _compactWindowStart: 0,
    _compactWindowEnd: 0,
    _lastIsWesternChar: false,
    _source: [],
    _buf_offsetX: 0.0,
    _prevChar: 0,
    _cursor: 0,
    rows: [0, 0],
    _lineHeights: [],
    _status: "flushing"
  };
  let Store = {
    context: context
  };
  let Ecs = Opts.Ecs;
  let create = Ecs.create;
  let getCharacter = Ecs.getCharacter;
  let setCharacter = Ecs.setCharacter;
  let getFont = Ecs.getFont;
  let setFont = Ecs.setFont;
  let getCharCode = Ecs.getCharCode;
  let getElementType = Ecs.getElementType;
  let setElementType = Ecs.setElementType;
  let getWidth = Ecs.getWidth;
  let getHeight = Ecs.getHeight;
  let getX = Ecs.getX;
  let getY = Ecs.getY;
  let getFontSize = Ecs.getFontSize;
  let setW = Ecs.setW;
  let setH = Ecs.setH;
  let setX = Ecs.setX;
  let setY = Ecs.setY;
  let setFontSize = Ecs.setFontSize;
  let addX = Ecs.addX;
  let setPosition = Ecs.setPosition;
  let setSize = Ecs.setSize;
  let unwrap$1 = Ecs.unwrap;
  let map = Ecs.map;
  let out = Ecs.out;
  let input = Ecs.input;
  let cursor = Ecs.cursor;
  let output = Ecs.output;
  let grow = Ecs.grow;
  let initlize$1 = Ecs.initlize;
  let env$1 = env;
  let Runtime = {
    create: create,
    getCharacter: getCharacter,
    setCharacter: setCharacter,
    getFont: getFont,
    setFont: setFont,
    getCharCode: getCharCode,
    getElementType: getElementType,
    setElementType: setElementType,
    getWidth: getWidth,
    getHeight: getHeight,
    getX: getX,
    getY: getY,
    getFontSize: getFontSize,
    setW: setW,
    setH: setH,
    setX: setX,
    setY: setY,
    setFontSize: setFontSize,
    addX: addX,
    setPosition: setPosition,
    setSize: setSize,
    unwrap: unwrap$1,
    map: map,
    out: out,
    input: input,
    cursor: cursor,
    output: output,
    grow: grow,
    initlize: initlize$1,
    CompressOptions: undefined,
    Store: undefined,
    env: env$1
  };
  let Runtime$1 = {
    env: env$1,
    create: create,
    getCharacter: getCharacter,
    setCharacter: setCharacter,
    getFont: getFont,
    setFont: setFont,
    getCharCode: getCharCode,
    getElementType: getElementType,
    setElementType: setElementType,
    getWidth: getWidth,
    getHeight: getHeight,
    getX: getX,
    getY: getY,
    getFontSize: getFontSize,
    setW: setW,
    setH: setH,
    setX: setX,
    setY: setY,
    setFontSize: setFontSize,
    addX: addX,
    setPosition: setPosition,
    setSize: setSize,
    unwrap: unwrap$1,
    map: map,
    out: out,
    input: input,
    cursor: cursor,
    output: output,
    grow: grow,
    initlize: initlize$1,
    CompressOptions: CompressOptions,
    Store: Store
  };
  let logger = console;
  let $$debugger = CompressOptions.$$debugger;
  let groupCollapsed = CompressOptions.groupCollapsed;
  let enable = process.env.NODE_ENV !== "production";
  let label = "main";
  let LoggerOption = {
    logger: logger,
    $$debugger: $$debugger,
    groupCollapsed: groupCollapsed,
    enable: enable,
    label: label
  };
  let toArgs = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return ["%c%s%c " + match, "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args.slice(1));
    }
    return ["%c%s%c %s", "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args);
  };
  let logger$1 = logger;
  let enable$1 = enable;
  let debuggerGroupCollapsed = groupCollapsed;
  let debug$1 = a => {
    if (enable$1) {
      logger$1.debug(a);
      return;
    }
  };
  let debug1 = (a, a2) => {
    if (enable$1) {
      logger$1.debug(a, a2);
      return;
    }
  };
  let debug2 = (a, a2, a3) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3);
      return;
    }
  };
  let debug3 = (a, a2, a3, a4) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3, a4);
      return;
    }
  };
  let debug4 = (a, a2, a3, a4, a5) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3, a4, a5);
      return;
    }
  };
  let debug5 = (a, a2, a3, a4, a5, a6) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let debug6 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
  };
  let banner = a => {
    if (enable$1) {
      logger$1.banner(a);
      return;
    }
  };
  let banner1 = (a, a2) => {
    if (enable$1) {
      logger$1.banner(a, a2);
      return;
    }
  };
  let banner2 = (a, a2, a3) => {
    if (enable$1) {
      logger$1.banner(a, a2, a3);
      return;
    }
  };
  let banner3 = (a, a2, a3, a4) => {
    if (enable$1) {
      logger$1.banner(a, a2, a3, a4);
      return;
    }
  };
  let banner4 = (a, a2, a3, a4, a5) => {
    if (enable$1) {
      logger$1.banner(a, a2, a3, a4, a5);
      return;
    }
  };
  let banner5 = (a, a2, a3, a4, a5, a6) => {
    if (enable$1) {
      logger$1.banner(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let log = a => {
    if (enable$1) {
      logger$1.log(a);
      return;
    }
  };
  let log1 = (a, a2) => {
    if (enable$1) {
      logger$1.log(a, a2);
      return;
    }
  };
  let log2 = (a, a2, a3) => {
    if (!enable$1) {
      return;
    }
    let args = toArgs(label, [a, a2, a3]);
    logger$1.log.apply(null, args);
  };
  let log3 = (a, a2, a3, a4) => {
    if (enable$1) {
      logger$1.log(a, a2, a3, a4);
      return;
    }
  };
  let log4 = (a, a2, a3, a4, a5) => {
    if (enable$1) {
      logger$1.log(a, a2, a3, a4, a5);
      return;
    }
  };
  let log5 = (a, a2, a3, a4, a5, a6) => {
    if (enable$1) {
      logger$1.log(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let groupCollapsed$1 = a => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a);
      } else {
        logger$1.group(a);
      }
      return;
    }
  };
  let groupCollapsed1 = (a, a1) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1);
      } else {
        logger$1.group(a, a1);
      }
      return;
    }
  };
  let groupCollapsed2 = (a, a1, a2) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2);
      } else {
        logger$1.group(a, a1, a2);
      }
      return;
    }
  };
  let groupCollapsed3 = (a, a1, a2, a3) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3);
      } else {
        logger$1.group(a, a1, a2, a3);
      }
      return;
    }
  };
  let groupCollapsed4 = (a, a1, a2, a3, a4) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger$1.group(a, a1, a2, a3, a4);
      }
      return;
    }
  };
  let groupCollapsed5 = (a, a1, a2, a3, a4, a5) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger$1.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
  };
  let groupCollapsed6 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger$1.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
  };
  let groupCollapsed7 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger$1.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
  };
  let groupEnd = () => {
    if (enable$1) {
      logger$1.groupEnd();
      return;
    }
  };
  let groupEnd1 = a => {
    if (enable$1) {
      logger$1.groupEnd(a);
      return;
    }
  };
  let groupEnd2 = (a, a1) => {
    if (enable$1) {
      logger$1.groupEnd(a, a1);
      return;
    }
  };
  let groupEnd3 = (a, a1, a2) => {
    if (enable$1) {
      logger$1.groupEnd(a, a1, a2);
      return;
    }
  };
  let Logger = {
    toArgs: toArgs,
    logger: logger$1,
    enable: enable$1,
    debuggerGroupCollapsed: debuggerGroupCollapsed,
    debug: debug$1,
    debug1: debug1,
    debug2: debug2,
    debug3: debug3,
    debug4: debug4,
    debug5: debug5,
    debug6: debug6,
    banner: banner,
    banner1: banner1,
    banner2: banner2,
    banner3: banner3,
    banner4: banner4,
    banner5: banner5,
    log: log,
    log1: log1,
    log2: log2,
    log3: log3,
    log4: log4,
    log5: log5,
    groupCollapsed: groupCollapsed$1,
    groupCollapsed1: groupCollapsed1,
    groupCollapsed2: groupCollapsed2,
    groupCollapsed3: groupCollapsed3,
    groupCollapsed4: groupCollapsed4,
    groupCollapsed5: groupCollapsed5,
    groupCollapsed6: groupCollapsed6,
    groupCollapsed7: groupCollapsed7,
    groupEnd: groupEnd,
    groupEnd1: groupEnd1,
    groupEnd2: groupEnd2,
    groupEnd3: groupEnd3
  };
  let Runtime_env = env$1;
  let Runtime_CompressOptions = CompressOptions;
  let CompressOptions$1 = Runtime_CompressOptions;
  let compressOptions_inlineCompression$1 = CompressOptions$1.inlineCompression;
  let compressOptions_forceGridAlignment$1 = CompressOptions$1.forceGridAlignment;
  let compressOptions_westernCharacterFirst$1 = CompressOptions$1.westernCharacterFirst;
  let compressOptions_forceSpaceBetweenCJKAndWestern$1 = CompressOptions$1.forceSpaceBetweenCJKAndWestern;
  let compressOptions_fixCJKLeftQuote$1 = CompressOptions$1.fixCJKLeftQuote;
  let compressOptions$1 = {
    inlineCompression: compressOptions_inlineCompression$1,
    forceGridAlignment: compressOptions_forceGridAlignment$1,
    westernCharacterFirst: compressOptions_westernCharacterFirst$1,
    forceSpaceBetweenCJKAndWestern: compressOptions_forceSpaceBetweenCJKAndWestern$1,
    fixCJKLeftQuote: compressOptions_fixCJKLeftQuote$1
  };
  let initlize$2 = (env, options, firstChar, hasLeftQuote, paddingLeft) => {
    env._firstChar = firstChar;
    env._hasLeftQuote = hasLeftQuote;
    env._paddingLeft = paddingLeft;
    env._gridSize = options.gridSize;
    env._forceSpace = CompressOptions$1.forceSpaceBetweenCJKAndWestern ? env._gridSize / 4.0 : 0.0;
    env._spaceWidth = env._gridSize * 0.35;
    env._xInterval = options.xInterval;
    env._yInterval = options.yInterval;
    env._maxCol = options.column;
    env._lastCol = env._maxCol - 1.0;
    env._maxRow = options.row;
    let value = options.wrapWidth;
    env._maxWidth = value !== undefined ? value : env._maxCol * env._gridSize;
    env._letterSpacing = options.letterSpacing;
    env._gridWidth = env._gridSize + env._xInterval;
    return env;
  };
  let IEnv_env = Runtime_env;
  let IEnv = {
    CompressOptions: CompressOptions$1,
    env: IEnv_env,
    compressOptions: compressOptions$1,
    initlize: initlize$2
  };
  let CompressOptions$2 = CompressOptions;
  let Config$1 = {
    $$debugger: CompressOptions$2.$$debugger
  };
  let $$return$1 = (param, t) => t;
  let switchTo$1 = (param, t) => t;
  let tapIf$1 = (context, exp, fn) => {
    if (exp(context)) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let tapIfConst$1 = (context, exp, fn) => {
    if (exp) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let boolean$1 = (context, exp) => [exp(context) ? "True" : "False", context];
  let onTrue$1 = (context, fn) => {
    if (context[0] === "False") {
      return context;
    } else {
      return ["True", fn(context[1])];
    }
  };
  let onFalse$1 = (context, fn) => {
    if (context[0] === "False") {
      return ["False", fn(context[1])];
    } else {
      return context;
    }
  };
  let unwrap$2 = context => context[1];
  let computeIfConst$1 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIf$1 = (context, exp, fn) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIfElse$1 = (context, exp, fn, else$p) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return else$p(context);
    }
  };
  let pipeIfConst$1 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let switchIf$1 = (context, exp, else$p, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let check$1 = (context, if$p, else$p, fn) => {
    if (if$p(context)) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let pipe$1 = (context, fn) => fn(context);
  let computeIfElse$1 = (context, exp, fn, fn2) => {
    if (exp) {
      return fn(context);
    } else {
      return fn2(context);
    }
  };
  let action$1 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionU$1 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionWith$1 = (context, withCtx, fn) => {
    fn(withCtx);
    return context;
  };
  let tap$1 = (context, fn) => {
    fn();
    return context;
  };
  let debugTap$1 = (context, fn) => {
    if (Config$1.$$debugger) {
      fn();
      return context;
    } else {
      return context;
    }
  };
  let debug$2 = (context, fn) => {
    if (Config$1.$$debugger) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let ignore$1 = (context, withCtx) => context;
  let call$1 = (context, arg) => context(arg);
  let rev$1 = (b, fn, a) => fn(a, b);
  let Pipe$1 = {
    $$return: $$return$1,
    switchTo: switchTo$1,
    tapIf: tapIf$1,
    tapIfConst: tapIfConst$1,
    boolean: boolean$1,
    onTrue: onTrue$1,
    onFalse: onFalse$1,
    unwrap: unwrap$2,
    computeIfConst: computeIfConst$1,
    pipeIf: pipeIf$1,
    pipeIfElse: pipeIfElse$1,
    pipeIfConst: pipeIfConst$1,
    switchIf: switchIf$1,
    check: check$1,
    pipe: pipe$1,
    computeIfElse: computeIfElse$1,
    action: action$1,
    actionU: actionU$1,
    actionWith: actionWith$1,
    tap: tap$1,
    debugTap: debugTap$1,
    debug: debug$2,
    ignore: ignore$1,
    call: call$1,
    rev: rev$1
  };
  let env$2 = env$1;
  let context$1 = Store.context;
  let _buf = {
    seqs: [],
    areaWidth: 0.0,
    areaHeight: 0.0,
    width: 0.0,
    height: 0.0,
    lastLineIndex: 0,
    lineHeights: context$1._lineHeights,
    status: context$1._status
  };
  let setContentWidth = (context, width) => {
    context._contentWidth = width;
    return context;
  };
  let getContentWidth = context => context._contentWidth;
  let setCurrentX = (context, x) => {
    context._currentX = x;
    return context;
  };
  let getCurrentX = context => context._currentX;
  let setCurrentY = (context, y) => {
    context._currentY = y;
    return context;
  };
  let getCurrentY = context => context._currentY;
  let setRowHeight = (context, rowHeight) => {
    context._currentRowHeight = rowHeight;
    return context;
  };
  let getRowHeight = context => context._currentRowHeight;
  let storeLineHeights = context => {
    context._lineHeights.push(context._currentRowHeight);
    return context;
  };
  let setCurrentCol = (context, col) => {
    context._currentCol = col;
    return context;
  };
  let setCurrentColU = (context, col) => {
    context._currentCol = col;
  };
  let getCurrentCol = context => context._currentCol;
  let setCurrentRow = (context, row) => {
    context._currentRow = row;
    return context;
  };
  let getCurrentRow = context => context._currentRow;
  let finish = param => {};
  let syncContentWidth = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    return context;
  };
  let growRowHeight = (context, elementHeight) => {
    if (elementHeight > context._currentRowHeight) {
      context._currentRowHeight = elementHeight;
    }
    return context;
  };
  let resetCurrent = context => {
    context._currentX = 0.0;
    let context$1 = context;
    context$1._currentY = 0.0;
    let context$2 = context$1;
    context$2._currentCol = 0.0;
    let context$3 = context$2;
    context$3._currentRow = 1;
    return context$3;
  };
  let rollbackX = (context, x) => {
    debug3("rollbackX: %o - %o => %o", context._currentX, x, context._currentX - x);
    context._currentX = context._currentX - x;
    return context;
  };
  let moveCursorX = (context, x) => {
    debug3("[cursor] moveX: %o + %o => %o", context._currentX, x, context._currentX + x);
    context._currentX = context._currentX + x;
    return context;
  };
  let consumeXIn = (x, context) => {
    debug3("[cursor] moveX: %o + %o => %o", context._currentX, x, context._currentX + x);
    context._currentX = context._currentX + x;
    return x;
  };
  let growCurrentY = (context, y) => {
    context._currentY = context._currentY + y;
    return context;
  };
  let rollbackCol = (context, col) => {
    context._currentCol = context._currentCol - col;
    return context;
  };
  let consumeCol = (context, col) => {
    context._currentCol = context._currentCol + col;
    return context;
  };
  let consumeColU = (context, col) => {
    context._currentCol = context._currentCol + col;
  };
  let growCurrentRow = context => {
    context._currentY = context._currentY + (context._currentRowHeight + env$2._yInterval);
    let context$1 = context;
    context$1._currentRow = context._currentRow + 1 | 0;
    return context$1;
  };
  let setLastCharWidth = (context, charWidth) => {
    context._lastCharWidth = charWidth;
    return context;
  };
  let nextIsWord = context => {
    let kind = context._cursor < context._source.length ? out(context._source[context._cursor + 1 | 0], getElementType) : out(context._END, getElementType);
    switch (kind) {
      case 3:
      case 12:
        return true;
      default:
        return false;
    }
  };
  let lastIsNotPunctuation = context => {
    context._lastIsPunctuation = false;
    context._lastPunctuations = 0.0;
    return context;
  };
  let consumePunctuation = context => {
    context._lastIsPunctuation = true;
    context._lastPunctuations = context._lastPunctuations + 1.0;
    return context;
  };
  let needForceWrap = context => {
    context._isNeedForceWrap = true;
    return context;
  };
  let resetNeedForceWrap = context => {
    context._isNeedForceWrap = false;
    return context;
  };
  let recordLastElement = (context, char) => {
    context.rows[context._currentRow] = char;
  };
  let getRowSize = context => context.rows.length;
  let prepare = (context, source, options) => {
    let firstChar = source[0];
    let hasLeftQuote = out(firstChar, getElementType) === 10;
    let paddingLeft = hasLeftQuote ? out(firstChar, getWidth) + env$2._xInterval : 0.0;
    let env$3 = initlize$2(env$2, options, firstChar, hasLeftQuote, paddingLeft);
    context._currentX = 0.0;
    let context$1 = context;
    context$1._currentY = 0.0;
    let context$2 = context$1;
    context$2._currentCol = 0.0;
    let context$3 = context$2;
    context$3._currentRow = 1;
    let context$4 = context$3;
    context$4._lastIsPunctuation = false;
    context$4._lastPunctuations = 0.0;
    let context$5 = context$4;
    context$5._currentRowHeight = env$3._gridSize;
    let context$6 = context$5;
    context$6._lastCharWidth = 0.0;
    let context$7 = context$6;
    context$7._isNeedForceWrap = false;
    let context$8 = context$7;
    action$1((context$8._contentWidth = 0.0, context$8), context => {
      context._cursor = 0;
      context._lastIsWesternChar = CompressOptions$2.westernCharacterFirst;
      context._source = source;
      context.rows = [firstChar, firstChar];
    });
    debug2("prepare", context, env$3);
  };
  let lineWrap = context => {
    log("lineWrap");
    /* lineWrap */
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let context$1 = context;
    context$1._currentCol = env$2._hasLeftQuote ? 1.0 : 0.0;
    let context$2 = context$1;
    context$2._currentX = env$2._paddingLeft;
    let context$3 = context$2;
    context$3._currentY = context$3._currentY + (context$3._currentRowHeight + env$2._yInterval);
    let context$4 = context$3;
    context$4._currentRow = context$3._currentRow + 1 | 0;
    let context$5 = context$4;
    context$5._lineHeights.push(context$5._currentRowHeight);
    let context$6 = context$5;
    context$6._currentRowHeight = env$2._gridSize;
    let context$7 = context$6;
    context$7._lastIsPunctuation = false;
    context$7._lastPunctuations = 0.0;
    let context$8 = context$7;
    context$8._lastCharWidth = 0.0;
    let context$9 = context$8;
    context$9._isNeedForceWrap = false;
  };
  let incrementFlush = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let lastLineIndex = context._currentRow;
    let overflowY = lastLineIndex > (context.rows.length - 1 | 0);
    let contentHeight = overflowY ? context._currentY : context._currentY + (context._lineHeights.push(context._currentRowHeight), context)._currentRowHeight;
    _buf.seqs = context.rows;
    _buf.areaWidth = context._contentWidth;
    _buf.areaHeight = contentHeight;
    _buf.width = env$2._maxWidth;
    _buf.height = contentHeight;
    _buf.lastLineIndex = overflowY ? lastLineIndex - 1 | 0 : lastLineIndex;
    _buf.lineHeights = context._lineHeights;
    _buf.status = "flushing";
    return _buf;
  };
  let flush = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let lastLineIndex = context._currentRow;
    let overflowY = lastLineIndex > (context.rows.length - 1 | 0);
    let contentHeight = overflowY ? context._currentY : context._currentY + (context._lineHeights.push(context._currentRowHeight), context)._currentRowHeight;
    _buf.seqs = context.rows;
    _buf.areaWidth = context._contentWidth;
    _buf.areaHeight = contentHeight;
    _buf.width = env$2._maxWidth;
    _buf.height = contentHeight;
    _buf.lastLineIndex = overflowY ? lastLineIndex - 1 | 0 : lastLineIndex;
    _buf.lineHeights = context._lineHeights;
    _buf.status = "closed";
    context._lineHeights = [];
    debug1("contentHeight: %o", contentHeight);
    return _buf;
  };
  let output$1 = param => ({
    seqs: _buf.seqs,
    areaWidth: _buf.areaWidth,
    areaHeight: _buf.areaHeight,
    width: _buf.width,
    height: _buf.height,
    lastLineIndex: _buf.lastLineIndex,
    lineHeights: _buf.lineHeights,
    status: "closed"
  });
  let Context = {
    LoggerOption: LoggerOption,
    Logger: Logger,
    IEnv: IEnv,
    ECS: Runtime$1,
    CompressOptions: CompressOptions$2,
    Pipe: Pipe$1,
    env: env$2,
    context: context$1,
    _buf: _buf,
    setContentWidth: setContentWidth,
    getContentWidth: getContentWidth,
    setCurrentX: setCurrentX,
    getCurrentX: getCurrentX,
    setCurrentY: setCurrentY,
    getCurrentY: getCurrentY,
    setRowHeight: setRowHeight,
    getRowHeight: getRowHeight,
    storeLineHeights: storeLineHeights,
    setCurrentCol: setCurrentCol,
    setCurrentColU: setCurrentColU,
    getCurrentCol: getCurrentCol,
    setCurrentRow: setCurrentRow,
    getCurrentRow: getCurrentRow,
    finish: finish,
    syncContentWidth: syncContentWidth,
    growRowHeight: growRowHeight,
    resetCurrent: resetCurrent,
    rollbackX: rollbackX,
    moveCursorX: moveCursorX,
    consumeXIn: consumeXIn,
    growCurrentY: growCurrentY,
    rollbackCol: rollbackCol,
    consumeCol: consumeCol,
    consumeColU: consumeColU,
    growCurrentRow: growCurrentRow,
    setLastCharWidth: setLastCharWidth,
    nextIsWord: nextIsWord,
    lastIsNotPunctuation: lastIsNotPunctuation,
    consumePunctuation: consumePunctuation,
    needForceWrap: needForceWrap,
    resetNeedForceWrap: resetNeedForceWrap,
    recordLastElement: recordLastElement,
    getRowSize: getRowSize,
    prepare: prepare,
    lineWrap: lineWrap,
    incrementFlush: incrementFlush,
    flush: flush,
    output: output$1
  };
  let Runtime$2 = {
    env: env$1,
    create: create,
    getCharacter: getCharacter,
    setCharacter: setCharacter,
    getFont: getFont,
    setFont: setFont,
    getCharCode: getCharCode,
    getElementType: getElementType,
    setElementType: setElementType,
    getWidth: getWidth,
    getHeight: getHeight,
    getX: getX,
    getY: getY,
    getFontSize: getFontSize,
    setW: setW,
    setH: setH,
    setX: setX,
    setY: setY,
    setFontSize: setFontSize,
    addX: addX,
    setPosition: setPosition,
    setSize: setSize,
    unwrap: unwrap$1,
    map: map,
    out: out,
    input: input,
    cursor: cursor,
    output: output,
    grow: grow,
    initlize: initlize$1,
    CompressOptions: CompressOptions,
    Store: Store
  };
  let logger$2 = console;
  let $$debugger$1 = CompressOptions.$$debugger;
  let groupCollapsed$2 = CompressOptions.groupCollapsed;
  let enable$2 = process.env.NODE_ENV !== "production";
  let label$1 = "compact";
  let LoggerOption$1 = {
    logger: logger$2,
    $$debugger: $$debugger$1,
    groupCollapsed: groupCollapsed$2,
    enable: enable$2,
    label: label$1
  };
  let toArgs$1 = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return ["%c%s%c " + match, "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args.slice(1));
    }
    return ["%c%s%c %s", "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args);
  };
  let logger$3 = logger$2;
  let enable$3 = enable$2;
  let debuggerGroupCollapsed$1 = groupCollapsed$2;
  let debug$3 = a => {
    if (enable$3) {
      logger$3.debug(a);
      return;
    }
  };
  let debug1$1 = (a, a2) => {
    if (enable$3) {
      logger$3.debug(a, a2);
      return;
    }
  };
  let debug2$1 = (a, a2, a3) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3);
      return;
    }
  };
  let debug3$1 = (a, a2, a3, a4) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3, a4);
      return;
    }
  };
  let debug4$1 = (a, a2, a3, a4, a5) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3, a4, a5);
      return;
    }
  };
  let debug5$1 = (a, a2, a3, a4, a5, a6) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let debug6$1 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
  };
  let banner$1 = a => {
    if (enable$3) {
      logger$3.banner(a);
      return;
    }
  };
  let banner1$1 = (a, a2) => {
    if (enable$3) {
      logger$3.banner(a, a2);
      return;
    }
  };
  let banner2$1 = (a, a2, a3) => {
    if (enable$3) {
      logger$3.banner(a, a2, a3);
      return;
    }
  };
  let banner3$1 = (a, a2, a3, a4) => {
    if (enable$3) {
      logger$3.banner(a, a2, a3, a4);
      return;
    }
  };
  let banner4$1 = (a, a2, a3, a4, a5) => {
    if (enable$3) {
      logger$3.banner(a, a2, a3, a4, a5);
      return;
    }
  };
  let banner5$1 = (a, a2, a3, a4, a5, a6) => {
    if (enable$3) {
      logger$3.banner(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let log$1 = a => {
    if (enable$3) {
      logger$3.log(a);
      return;
    }
  };
  let log1$1 = (a, a2) => {
    if (enable$3) {
      logger$3.log(a, a2);
      return;
    }
  };
  let log2$1 = (a, a2, a3) => {
    if (!enable$3) {
      return;
    }
    let args = toArgs$1(label$1, [a, a2, a3]);
    logger$3.log.apply(null, args);
  };
  let log3$1 = (a, a2, a3, a4) => {
    if (enable$3) {
      logger$3.log(a, a2, a3, a4);
      return;
    }
  };
  let log4$1 = (a, a2, a3, a4, a5) => {
    if (enable$3) {
      logger$3.log(a, a2, a3, a4, a5);
      return;
    }
  };
  let log5$1 = (a, a2, a3, a4, a5, a6) => {
    if (enable$3) {
      logger$3.log(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let groupCollapsed$3 = a => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a);
      } else {
        logger$3.group(a);
      }
      return;
    }
  };
  let groupCollapsed1$1 = (a, a1) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1);
      } else {
        logger$3.group(a, a1);
      }
      return;
    }
  };
  let groupCollapsed2$1 = (a, a1, a2) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2);
      } else {
        logger$3.group(a, a1, a2);
      }
      return;
    }
  };
  let groupCollapsed3$1 = (a, a1, a2, a3) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3);
      } else {
        logger$3.group(a, a1, a2, a3);
      }
      return;
    }
  };
  let groupCollapsed4$1 = (a, a1, a2, a3, a4) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger$3.group(a, a1, a2, a3, a4);
      }
      return;
    }
  };
  let groupCollapsed5$1 = (a, a1, a2, a3, a4, a5) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger$3.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
  };
  let groupCollapsed6$1 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger$3.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
  };
  let groupCollapsed7$1 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger$3.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
  };
  let groupEnd$1 = () => {
    if (enable$3) {
      logger$3.groupEnd();
      return;
    }
  };
  let groupEnd1$1 = a => {
    if (enable$3) {
      logger$3.groupEnd(a);
      return;
    }
  };
  let groupEnd2$1 = (a, a1) => {
    if (enable$3) {
      logger$3.groupEnd(a, a1);
      return;
    }
  };
  let groupEnd3$1 = (a, a1, a2) => {
    if (enable$3) {
      logger$3.groupEnd(a, a1, a2);
      return;
    }
  };
  let Logger$1 = {
    toArgs: toArgs$1,
    logger: logger$3,
    enable: enable$3,
    debuggerGroupCollapsed: debuggerGroupCollapsed$1,
    debug: debug$3,
    debug1: debug1$1,
    debug2: debug2$1,
    debug3: debug3$1,
    debug4: debug4$1,
    debug5: debug5$1,
    debug6: debug6$1,
    banner: banner$1,
    banner1: banner1$1,
    banner2: banner2$1,
    banner3: banner3$1,
    banner4: banner4$1,
    banner5: banner5$1,
    log: log$1,
    log1: log1$1,
    log2: log2$1,
    log3: log3$1,
    log4: log4$1,
    log5: log5$1,
    groupCollapsed: groupCollapsed$3,
    groupCollapsed1: groupCollapsed1$1,
    groupCollapsed2: groupCollapsed2$1,
    groupCollapsed3: groupCollapsed3$1,
    groupCollapsed4: groupCollapsed4$1,
    groupCollapsed5: groupCollapsed5$1,
    groupCollapsed6: groupCollapsed6$1,
    groupCollapsed7: groupCollapsed7$1,
    groupEnd: groupEnd$1,
    groupEnd1: groupEnd1$1,
    groupEnd2: groupEnd2$1,
    groupEnd3: groupEnd3$1
  };
  let Runtime_env$1 = env$1;
  let Runtime_CompressOptions$1 = CompressOptions;
  let CompressOptions$3 = Runtime_CompressOptions$1;
  let compressOptions_inlineCompression$2 = CompressOptions$3.inlineCompression;
  let compressOptions_forceGridAlignment$2 = CompressOptions$3.forceGridAlignment;
  let compressOptions_westernCharacterFirst$2 = CompressOptions$3.westernCharacterFirst;
  let compressOptions_forceSpaceBetweenCJKAndWestern$2 = CompressOptions$3.forceSpaceBetweenCJKAndWestern;
  let compressOptions_fixCJKLeftQuote$2 = CompressOptions$3.fixCJKLeftQuote;
  let compressOptions$2 = {
    inlineCompression: compressOptions_inlineCompression$2,
    forceGridAlignment: compressOptions_forceGridAlignment$2,
    westernCharacterFirst: compressOptions_westernCharacterFirst$2,
    forceSpaceBetweenCJKAndWestern: compressOptions_forceSpaceBetweenCJKAndWestern$2,
    fixCJKLeftQuote: compressOptions_fixCJKLeftQuote$2
  };
  let initlize$3 = (env, options, firstChar, hasLeftQuote, paddingLeft) => {
    env._firstChar = firstChar;
    env._hasLeftQuote = hasLeftQuote;
    env._paddingLeft = paddingLeft;
    env._gridSize = options.gridSize;
    env._forceSpace = CompressOptions$3.forceSpaceBetweenCJKAndWestern ? env._gridSize / 4.0 : 0.0;
    env._spaceWidth = env._gridSize * 0.35;
    env._xInterval = options.xInterval;
    env._yInterval = options.yInterval;
    env._maxCol = options.column;
    env._lastCol = env._maxCol - 1.0;
    env._maxRow = options.row;
    let value = options.wrapWidth;
    env._maxWidth = value !== undefined ? value : env._maxCol * env._gridSize;
    env._letterSpacing = options.letterSpacing;
    env._gridWidth = env._gridSize + env._xInterval;
    return env;
  };
  let IEnv_env$1 = Runtime_env$1;
  let IEnv$1 = {
    CompressOptions: CompressOptions$3,
    env: IEnv_env$1,
    compressOptions: compressOptions$2,
    initlize: initlize$3
  };
  let CompressOptions$4 = CompressOptions;
  let logger$4 = console;
  let $$debugger$2 = CompressOptions.$$debugger;
  let groupCollapsed$4 = CompressOptions.groupCollapsed;
  let enable$4 = process.env.NODE_ENV !== "production";
  let label$2 = "main";
  let LoggerOption$2 = {
    logger: logger$4,
    $$debugger: $$debugger$2,
    groupCollapsed: groupCollapsed$4,
    enable: enable$4,
    label: label$2
  };
  let toArgs$2 = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return ["%c%s%c " + match, "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args.slice(1));
    }
    return ["%c%s%c %s", "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args);
  };
  let logger$5 = logger$4;
  let enable$5 = enable$4;
  let debuggerGroupCollapsed$2 = groupCollapsed$4;
  let debug$4 = a => {
    if (enable$5) {
      logger$5.debug(a);
      return;
    }
  };
  let debug1$2 = (a, a2) => {
    if (enable$5) {
      logger$5.debug(a, a2);
      return;
    }
  };
  let debug2$2 = (a, a2, a3) => {
    if (enable$5) {
      logger$5.debug(a, a2, a3);
      return;
    }
  };
  let debug3$2 = (a, a2, a3, a4) => {
    if (enable$5) {
      logger$5.debug(a, a2, a3, a4);
      return;
    }
  };
  let debug4$2 = (a, a2, a3, a4, a5) => {
    if (enable$5) {
      logger$5.debug(a, a2, a3, a4, a5);
      return;
    }
  };
  let debug5$2 = (a, a2, a3, a4, a5, a6) => {
    if (enable$5) {
      logger$5.debug(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let debug6$2 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable$5) {
      logger$5.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
  };
  let banner$2 = a => {
    if (enable$5) {
      logger$5.banner(a);
      return;
    }
  };
  let banner1$2 = (a, a2) => {
    if (enable$5) {
      logger$5.banner(a, a2);
      return;
    }
  };
  let banner2$2 = (a, a2, a3) => {
    if (enable$5) {
      logger$5.banner(a, a2, a3);
      return;
    }
  };
  let banner3$2 = (a, a2, a3, a4) => {
    if (enable$5) {
      logger$5.banner(a, a2, a3, a4);
      return;
    }
  };
  let banner4$2 = (a, a2, a3, a4, a5) => {
    if (enable$5) {
      logger$5.banner(a, a2, a3, a4, a5);
      return;
    }
  };
  let banner5$2 = (a, a2, a3, a4, a5, a6) => {
    if (enable$5) {
      logger$5.banner(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let log$2 = a => {
    if (enable$5) {
      logger$5.log(a);
      return;
    }
  };
  let log1$2 = (a, a2) => {
    if (enable$5) {
      logger$5.log(a, a2);
      return;
    }
  };
  let log2$2 = (a, a2, a3) => {
    if (!enable$5) {
      return;
    }
    let args = toArgs$2(label$2, [a, a2, a3]);
    logger$5.log.apply(null, args);
  };
  let log3$2 = (a, a2, a3, a4) => {
    if (enable$5) {
      logger$5.log(a, a2, a3, a4);
      return;
    }
  };
  let log4$2 = (a, a2, a3, a4, a5) => {
    if (enable$5) {
      logger$5.log(a, a2, a3, a4, a5);
      return;
    }
  };
  let log5$2 = (a, a2, a3, a4, a5, a6) => {
    if (enable$5) {
      logger$5.log(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let groupCollapsed$5 = a => {
    if (enable$5) {
      if (debuggerGroupCollapsed$2) {
        logger$5.groupCollapsed(a);
      } else {
        logger$5.group(a);
      }
      return;
    }
  };
  let groupCollapsed1$2 = (a, a1) => {
    if (enable$5) {
      if (debuggerGroupCollapsed$2) {
        logger$5.groupCollapsed(a, a1);
      } else {
        logger$5.group(a, a1);
      }
      return;
    }
  };
  let groupCollapsed2$2 = (a, a1, a2) => {
    if (enable$5) {
      if (debuggerGroupCollapsed$2) {
        logger$5.groupCollapsed(a, a1, a2);
      } else {
        logger$5.group(a, a1, a2);
      }
      return;
    }
  };
  let groupCollapsed3$2 = (a, a1, a2, a3) => {
    if (enable$5) {
      if (debuggerGroupCollapsed$2) {
        logger$5.groupCollapsed(a, a1, a2, a3);
      } else {
        logger$5.group(a, a1, a2, a3);
      }
      return;
    }
  };
  let groupCollapsed4$2 = (a, a1, a2, a3, a4) => {
    if (enable$5) {
      if (debuggerGroupCollapsed$2) {
        logger$5.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger$5.group(a, a1, a2, a3, a4);
      }
      return;
    }
  };
  let groupCollapsed5$2 = (a, a1, a2, a3, a4, a5) => {
    if (enable$5) {
      if (debuggerGroupCollapsed$2) {
        logger$5.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger$5.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
  };
  let groupCollapsed6$2 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable$5) {
      if (debuggerGroupCollapsed$2) {
        logger$5.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger$5.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
  };
  let groupCollapsed7$2 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable$5) {
      if (debuggerGroupCollapsed$2) {
        logger$5.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger$5.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
  };
  let groupEnd$2 = () => {
    if (enable$5) {
      logger$5.groupEnd();
      return;
    }
  };
  let groupEnd1$2 = a => {
    if (enable$5) {
      logger$5.groupEnd(a);
      return;
    }
  };
  let groupEnd2$2 = (a, a1) => {
    if (enable$5) {
      logger$5.groupEnd(a, a1);
      return;
    }
  };
  let groupEnd3$2 = (a, a1, a2) => {
    if (enable$5) {
      logger$5.groupEnd(a, a1, a2);
      return;
    }
  };
  let Logger$2 = {
    toArgs: toArgs$2,
    logger: logger$5,
    enable: enable$5,
    debuggerGroupCollapsed: debuggerGroupCollapsed$2,
    debug: debug$4,
    debug1: debug1$2,
    debug2: debug2$2,
    debug3: debug3$2,
    debug4: debug4$2,
    debug5: debug5$2,
    debug6: debug6$2,
    banner: banner$2,
    banner1: banner1$2,
    banner2: banner2$2,
    banner3: banner3$2,
    banner4: banner4$2,
    banner5: banner5$2,
    log: log$2,
    log1: log1$2,
    log2: log2$2,
    log3: log3$2,
    log4: log4$2,
    log5: log5$2,
    groupCollapsed: groupCollapsed$5,
    groupCollapsed1: groupCollapsed1$2,
    groupCollapsed2: groupCollapsed2$2,
    groupCollapsed3: groupCollapsed3$2,
    groupCollapsed4: groupCollapsed4$2,
    groupCollapsed5: groupCollapsed5$2,
    groupCollapsed6: groupCollapsed6$2,
    groupCollapsed7: groupCollapsed7$2,
    groupEnd: groupEnd$2,
    groupEnd1: groupEnd1$2,
    groupEnd2: groupEnd2$2,
    groupEnd3: groupEnd3$2
  };
  let Runtime_env$2 = env$1;
  let Runtime_CompressOptions$2 = CompressOptions;
  let CompressOptions$5 = Runtime_CompressOptions$2;
  let compressOptions_inlineCompression$3 = CompressOptions$5.inlineCompression;
  let compressOptions_forceGridAlignment$3 = CompressOptions$5.forceGridAlignment;
  let compressOptions_westernCharacterFirst$3 = CompressOptions$5.westernCharacterFirst;
  let compressOptions_forceSpaceBetweenCJKAndWestern$3 = CompressOptions$5.forceSpaceBetweenCJKAndWestern;
  let compressOptions_fixCJKLeftQuote$3 = CompressOptions$5.fixCJKLeftQuote;
  let compressOptions$3 = {
    inlineCompression: compressOptions_inlineCompression$3,
    forceGridAlignment: compressOptions_forceGridAlignment$3,
    westernCharacterFirst: compressOptions_westernCharacterFirst$3,
    forceSpaceBetweenCJKAndWestern: compressOptions_forceSpaceBetweenCJKAndWestern$3,
    fixCJKLeftQuote: compressOptions_fixCJKLeftQuote$3
  };
  let initlize$4 = (env, options, firstChar, hasLeftQuote, paddingLeft) => {
    env._firstChar = firstChar;
    env._hasLeftQuote = hasLeftQuote;
    env._paddingLeft = paddingLeft;
    env._gridSize = options.gridSize;
    env._forceSpace = CompressOptions$5.forceSpaceBetweenCJKAndWestern ? env._gridSize / 4.0 : 0.0;
    env._spaceWidth = env._gridSize * 0.35;
    env._xInterval = options.xInterval;
    env._yInterval = options.yInterval;
    env._maxCol = options.column;
    env._lastCol = env._maxCol - 1.0;
    env._maxRow = options.row;
    let value = options.wrapWidth;
    env._maxWidth = value !== undefined ? value : env._maxCol * env._gridSize;
    env._letterSpacing = options.letterSpacing;
    env._gridWidth = env._gridSize + env._xInterval;
    return env;
  };
  let IEnv_env$2 = Runtime_env$2;
  let IEnv$2 = {
    CompressOptions: CompressOptions$5,
    env: IEnv_env$2,
    compressOptions: compressOptions$3,
    initlize: initlize$4
  };
  let CompressOptions$6 = CompressOptions;
  let Config$2 = {
    $$debugger: CompressOptions$6.$$debugger
  };
  let $$return$2 = (param, t) => t;
  let switchTo$2 = (param, t) => t;
  let tapIf$2 = (context, exp, fn) => {
    if (exp(context)) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let tapIfConst$2 = (context, exp, fn) => {
    if (exp) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let boolean$2 = (context, exp) => [exp(context) ? "True" : "False", context];
  let onTrue$2 = (context, fn) => {
    if (context[0] === "False") {
      return context;
    } else {
      return ["True", fn(context[1])];
    }
  };
  let onFalse$2 = (context, fn) => {
    if (context[0] === "False") {
      return ["False", fn(context[1])];
    } else {
      return context;
    }
  };
  let unwrap$3 = context => context[1];
  let computeIfConst$2 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIf$2 = (context, exp, fn) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIfElse$2 = (context, exp, fn, else$p) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return else$p(context);
    }
  };
  let pipeIfConst$2 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let switchIf$2 = (context, exp, else$p, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let check$2 = (context, if$p, else$p, fn) => {
    if (if$p(context)) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let pipe$2 = (context, fn) => fn(context);
  let computeIfElse$2 = (context, exp, fn, fn2) => {
    if (exp) {
      return fn(context);
    } else {
      return fn2(context);
    }
  };
  let action$2 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionU$2 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionWith$2 = (context, withCtx, fn) => {
    fn(withCtx);
    return context;
  };
  let tap$2 = (context, fn) => {
    fn();
    return context;
  };
  let debugTap$2 = (context, fn) => {
    if (Config$2.$$debugger) {
      fn();
      return context;
    } else {
      return context;
    }
  };
  let debug$5 = (context, fn) => {
    if (Config$2.$$debugger) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let ignore$2 = (context, withCtx) => context;
  let call$2 = (context, arg) => context(arg);
  let rev$2 = (b, fn, a) => fn(a, b);
  let Pipe$2 = {
    $$return: $$return$2,
    switchTo: switchTo$2,
    tapIf: tapIf$2,
    tapIfConst: tapIfConst$2,
    boolean: boolean$2,
    onTrue: onTrue$2,
    onFalse: onFalse$2,
    unwrap: unwrap$3,
    computeIfConst: computeIfConst$2,
    pipeIf: pipeIf$2,
    pipeIfElse: pipeIfElse$2,
    pipeIfConst: pipeIfConst$2,
    switchIf: switchIf$2,
    check: check$2,
    pipe: pipe$2,
    computeIfElse: computeIfElse$2,
    action: action$2,
    actionU: actionU$2,
    actionWith: actionWith$2,
    tap: tap$2,
    debugTap: debugTap$2,
    debug: debug$5,
    ignore: ignore$2,
    call: call$2,
    rev: rev$2
  };
  let env$3 = env$1;
  let context$2 = Store.context;
  let _buf$1 = {
    seqs: [],
    areaWidth: 0.0,
    areaHeight: 0.0,
    width: 0.0,
    height: 0.0,
    lastLineIndex: 0,
    lineHeights: context$2._lineHeights,
    status: context$2._status
  };
  let setContentWidth$1 = (context, width) => {
    context._contentWidth = width;
    return context;
  };
  let getContentWidth$1 = context => context._contentWidth;
  let setCurrentX$1 = (context, x) => {
    context._currentX = x;
    return context;
  };
  let getCurrentX$1 = context => context._currentX;
  let setCurrentY$1 = (context, y) => {
    context._currentY = y;
    return context;
  };
  let getCurrentY$1 = context => context._currentY;
  let setRowHeight$1 = (context, rowHeight) => {
    context._currentRowHeight = rowHeight;
    return context;
  };
  let getRowHeight$1 = context => context._currentRowHeight;
  let storeLineHeights$1 = context => {
    context._lineHeights.push(context._currentRowHeight);
    return context;
  };
  let setCurrentCol$1 = (context, col) => {
    context._currentCol = col;
    return context;
  };
  let setCurrentColU$1 = (context, col) => {
    context._currentCol = col;
  };
  let getCurrentCol$1 = context => context._currentCol;
  let setCurrentRow$1 = (context, row) => {
    context._currentRow = row;
    return context;
  };
  let getCurrentRow$1 = context => context._currentRow;
  let finish$1 = param => {};
  let syncContentWidth$1 = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    return context;
  };
  let growRowHeight$1 = (context, elementHeight) => {
    if (elementHeight > context._currentRowHeight) {
      context._currentRowHeight = elementHeight;
    }
    return context;
  };
  let resetCurrent$1 = context => {
    context._currentX = 0.0;
    let context$1 = context;
    context$1._currentY = 0.0;
    let context$2 = context$1;
    context$2._currentCol = 0.0;
    let context$3 = context$2;
    context$3._currentRow = 1;
    return context$3;
  };
  let rollbackX$1 = (context, x) => {
    debug3$2("rollbackX: %o - %o => %o", context._currentX, x, context._currentX - x);
    context._currentX = context._currentX - x;
    return context;
  };
  let moveCursorX$1 = (context, x) => {
    debug3$2("[cursor] moveX: %o + %o => %o", context._currentX, x, context._currentX + x);
    context._currentX = context._currentX + x;
    return context;
  };
  let consumeXIn$1 = (x, context) => {
    debug3$2("[cursor] moveX: %o + %o => %o", context._currentX, x, context._currentX + x);
    context._currentX = context._currentX + x;
    return x;
  };
  let growCurrentY$1 = (context, y) => {
    context._currentY = context._currentY + y;
    return context;
  };
  let rollbackCol$1 = (context, col) => {
    context._currentCol = context._currentCol - col;
    return context;
  };
  let consumeCol$1 = (context, col) => {
    context._currentCol = context._currentCol + col;
    return context;
  };
  let consumeColU$1 = (context, col) => {
    context._currentCol = context._currentCol + col;
  };
  let growCurrentRow$1 = context => {
    context._currentY = context._currentY + (context._currentRowHeight + env$3._yInterval);
    let context$1 = context;
    context$1._currentRow = context._currentRow + 1 | 0;
    return context$1;
  };
  let setLastCharWidth$1 = (context, charWidth) => {
    context._lastCharWidth = charWidth;
    return context;
  };
  let nextIsWord$1 = context => {
    let kind = context._cursor < context._source.length ? out(context._source[context._cursor + 1 | 0], getElementType) : out(context._END, getElementType);
    switch (kind) {
      case 3:
      case 12:
        return true;
      default:
        return false;
    }
  };
  let lastIsNotPunctuation$1 = context => {
    context._lastIsPunctuation = false;
    context._lastPunctuations = 0.0;
    return context;
  };
  let consumePunctuation$1 = context => {
    context._lastIsPunctuation = true;
    context._lastPunctuations = context._lastPunctuations + 1.0;
    return context;
  };
  let needForceWrap$1 = context => {
    context._isNeedForceWrap = true;
    return context;
  };
  let resetNeedForceWrap$1 = context => {
    context._isNeedForceWrap = false;
    return context;
  };
  let recordLastElement$1 = (context, char) => {
    context.rows[context._currentRow] = char;
  };
  let getRowSize$1 = context => context.rows.length;
  let prepare$1 = (context, source, options) => {
    let firstChar = source[0];
    let hasLeftQuote = out(firstChar, getElementType) === 10;
    let paddingLeft = hasLeftQuote ? out(firstChar, getWidth) + env$3._xInterval : 0.0;
    let env$4 = initlize$4(env$3, options, firstChar, hasLeftQuote, paddingLeft);
    context._currentX = 0.0;
    let context$1 = context;
    context$1._currentY = 0.0;
    let context$2 = context$1;
    context$2._currentCol = 0.0;
    let context$3 = context$2;
    context$3._currentRow = 1;
    let context$4 = context$3;
    context$4._lastIsPunctuation = false;
    context$4._lastPunctuations = 0.0;
    let context$5 = context$4;
    context$5._currentRowHeight = env$4._gridSize;
    let context$6 = context$5;
    context$6._lastCharWidth = 0.0;
    let context$7 = context$6;
    context$7._isNeedForceWrap = false;
    let context$8 = context$7;
    action$2((context$8._contentWidth = 0.0, context$8), context => {
      context._cursor = 0;
      context._lastIsWesternChar = CompressOptions$6.westernCharacterFirst;
      context._source = source;
      context.rows = [firstChar, firstChar];
    });
    debug2$2("prepare", context, env$4);
  };
  let lineWrap$1 = context => {
    log$2("lineWrap");
    /* lineWrap */
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let context$1 = context;
    context$1._currentCol = env$3._hasLeftQuote ? 1.0 : 0.0;
    let context$2 = context$1;
    context$2._currentX = env$3._paddingLeft;
    let context$3 = context$2;
    context$3._currentY = context$3._currentY + (context$3._currentRowHeight + env$3._yInterval);
    let context$4 = context$3;
    context$4._currentRow = context$3._currentRow + 1 | 0;
    let context$5 = context$4;
    context$5._lineHeights.push(context$5._currentRowHeight);
    let context$6 = context$5;
    context$6._currentRowHeight = env$3._gridSize;
    let context$7 = context$6;
    context$7._lastIsPunctuation = false;
    context$7._lastPunctuations = 0.0;
    let context$8 = context$7;
    context$8._lastCharWidth = 0.0;
    let context$9 = context$8;
    context$9._isNeedForceWrap = false;
  };
  let incrementFlush$1 = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let lastLineIndex = context._currentRow;
    let overflowY = lastLineIndex > (context.rows.length - 1 | 0);
    let contentHeight = overflowY ? context._currentY : context._currentY + (context._lineHeights.push(context._currentRowHeight), context)._currentRowHeight;
    _buf$1.seqs = context.rows;
    _buf$1.areaWidth = context._contentWidth;
    _buf$1.areaHeight = contentHeight;
    _buf$1.width = env$3._maxWidth;
    _buf$1.height = contentHeight;
    _buf$1.lastLineIndex = overflowY ? lastLineIndex - 1 | 0 : lastLineIndex;
    _buf$1.lineHeights = context._lineHeights;
    _buf$1.status = "flushing";
    return _buf$1;
  };
  let flush$1 = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let lastLineIndex = context._currentRow;
    let overflowY = lastLineIndex > (context.rows.length - 1 | 0);
    let contentHeight = overflowY ? context._currentY : context._currentY + (context._lineHeights.push(context._currentRowHeight), context)._currentRowHeight;
    _buf$1.seqs = context.rows;
    _buf$1.areaWidth = context._contentWidth;
    _buf$1.areaHeight = contentHeight;
    _buf$1.width = env$3._maxWidth;
    _buf$1.height = contentHeight;
    _buf$1.lastLineIndex = overflowY ? lastLineIndex - 1 | 0 : lastLineIndex;
    _buf$1.lineHeights = context._lineHeights;
    _buf$1.status = "closed";
    context._lineHeights = [];
    debug1$2("contentHeight: %o", contentHeight);
    return _buf$1;
  };
  let output$2 = param => ({
    seqs: _buf$1.seqs,
    areaWidth: _buf$1.areaWidth,
    areaHeight: _buf$1.areaHeight,
    width: _buf$1.width,
    height: _buf$1.height,
    lastLineIndex: _buf$1.lastLineIndex,
    lineHeights: _buf$1.lineHeights,
    status: "closed"
  });
  let Context$1 = {
    LoggerOption: LoggerOption$2,
    Logger: Logger$2,
    IEnv: IEnv$2,
    ECS: Runtime$2,
    CompressOptions: CompressOptions$6,
    Pipe: Pipe$2,
    env: env$3,
    context: context$2,
    _buf: _buf$1,
    setContentWidth: setContentWidth$1,
    getContentWidth: getContentWidth$1,
    setCurrentX: setCurrentX$1,
    getCurrentX: getCurrentX$1,
    setCurrentY: setCurrentY$1,
    getCurrentY: getCurrentY$1,
    setRowHeight: setRowHeight$1,
    getRowHeight: getRowHeight$1,
    storeLineHeights: storeLineHeights$1,
    setCurrentCol: setCurrentCol$1,
    setCurrentColU: setCurrentColU$1,
    getCurrentCol: getCurrentCol$1,
    setCurrentRow: setCurrentRow$1,
    getCurrentRow: getCurrentRow$1,
    finish: finish$1,
    syncContentWidth: syncContentWidth$1,
    growRowHeight: growRowHeight$1,
    resetCurrent: resetCurrent$1,
    rollbackX: rollbackX$1,
    moveCursorX: moveCursorX$1,
    consumeXIn: consumeXIn$1,
    growCurrentY: growCurrentY$1,
    rollbackCol: rollbackCol$1,
    consumeCol: consumeCol$1,
    consumeColU: consumeColU$1,
    growCurrentRow: growCurrentRow$1,
    setLastCharWidth: setLastCharWidth$1,
    nextIsWord: nextIsWord$1,
    lastIsNotPunctuation: lastIsNotPunctuation$1,
    consumePunctuation: consumePunctuation$1,
    needForceWrap: needForceWrap$1,
    resetNeedForceWrap: resetNeedForceWrap$1,
    recordLastElement: recordLastElement$1,
    getRowSize: getRowSize$1,
    prepare: prepare$1,
    lineWrap: lineWrap$1,
    incrementFlush: incrementFlush$1,
    flush: flush$1,
    output: output$2
  };
  let logger$6 = console;
  let $$debugger$3 = CompressOptions.$$debugger;
  let groupCollapsed$6 = CompressOptions.groupCollapsed;
  let enable$6 = process.env.NODE_ENV !== "production";
  let label$3 = "main";
  let LoggerOption$3 = {
    logger: logger$6,
    $$debugger: $$debugger$3,
    groupCollapsed: groupCollapsed$6,
    enable: enable$6,
    label: label$3
  };
  let toArgs$3 = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return ["%c%s%c " + match, "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args.slice(1));
    }
    return ["%c%s%c %s", "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args);
  };
  let logger$7 = logger$6;
  let enable$7 = enable$6;
  let debuggerGroupCollapsed$3 = groupCollapsed$6;
  let debug$6 = a => {
    if (enable$7) {
      logger$7.debug(a);
      return;
    }
  };
  let debug1$3 = (a, a2) => {
    if (enable$7) {
      logger$7.debug(a, a2);
      return;
    }
  };
  let debug2$3 = (a, a2, a3) => {
    if (enable$7) {
      logger$7.debug(a, a2, a3);
      return;
    }
  };
  let debug3$3 = (a, a2, a3, a4) => {
    if (enable$7) {
      logger$7.debug(a, a2, a3, a4);
      return;
    }
  };
  let debug4$3 = (a, a2, a3, a4, a5) => {
    if (enable$7) {
      logger$7.debug(a, a2, a3, a4, a5);
      return;
    }
  };
  let debug5$3 = (a, a2, a3, a4, a5, a6) => {
    if (enable$7) {
      logger$7.debug(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let debug6$3 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable$7) {
      logger$7.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
  };
  let banner$3 = a => {
    if (enable$7) {
      logger$7.banner(a);
      return;
    }
  };
  let banner1$3 = (a, a2) => {
    if (enable$7) {
      logger$7.banner(a, a2);
      return;
    }
  };
  let banner2$3 = (a, a2, a3) => {
    if (enable$7) {
      logger$7.banner(a, a2, a3);
      return;
    }
  };
  let banner3$3 = (a, a2, a3, a4) => {
    if (enable$7) {
      logger$7.banner(a, a2, a3, a4);
      return;
    }
  };
  let banner4$3 = (a, a2, a3, a4, a5) => {
    if (enable$7) {
      logger$7.banner(a, a2, a3, a4, a5);
      return;
    }
  };
  let banner5$3 = (a, a2, a3, a4, a5, a6) => {
    if (enable$7) {
      logger$7.banner(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let log$3 = a => {
    if (enable$7) {
      logger$7.log(a);
      return;
    }
  };
  let log1$3 = (a, a2) => {
    if (enable$7) {
      logger$7.log(a, a2);
      return;
    }
  };
  let log2$3 = (a, a2, a3) => {
    if (!enable$7) {
      return;
    }
    let args = toArgs$3(label$3, [a, a2, a3]);
    logger$7.log.apply(null, args);
  };
  let log3$3 = (a, a2, a3, a4) => {
    if (enable$7) {
      logger$7.log(a, a2, a3, a4);
      return;
    }
  };
  let log4$3 = (a, a2, a3, a4, a5) => {
    if (enable$7) {
      logger$7.log(a, a2, a3, a4, a5);
      return;
    }
  };
  let log5$3 = (a, a2, a3, a4, a5, a6) => {
    if (enable$7) {
      logger$7.log(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let groupCollapsed$7 = a => {
    if (enable$7) {
      if (debuggerGroupCollapsed$3) {
        logger$7.groupCollapsed(a);
      } else {
        logger$7.group(a);
      }
      return;
    }
  };
  let groupCollapsed1$3 = (a, a1) => {
    if (enable$7) {
      if (debuggerGroupCollapsed$3) {
        logger$7.groupCollapsed(a, a1);
      } else {
        logger$7.group(a, a1);
      }
      return;
    }
  };
  let groupCollapsed2$3 = (a, a1, a2) => {
    if (enable$7) {
      if (debuggerGroupCollapsed$3) {
        logger$7.groupCollapsed(a, a1, a2);
      } else {
        logger$7.group(a, a1, a2);
      }
      return;
    }
  };
  let groupCollapsed3$3 = (a, a1, a2, a3) => {
    if (enable$7) {
      if (debuggerGroupCollapsed$3) {
        logger$7.groupCollapsed(a, a1, a2, a3);
      } else {
        logger$7.group(a, a1, a2, a3);
      }
      return;
    }
  };
  let groupCollapsed4$3 = (a, a1, a2, a3, a4) => {
    if (enable$7) {
      if (debuggerGroupCollapsed$3) {
        logger$7.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger$7.group(a, a1, a2, a3, a4);
      }
      return;
    }
  };
  let groupCollapsed5$3 = (a, a1, a2, a3, a4, a5) => {
    if (enable$7) {
      if (debuggerGroupCollapsed$3) {
        logger$7.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger$7.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
  };
  let groupCollapsed6$3 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable$7) {
      if (debuggerGroupCollapsed$3) {
        logger$7.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger$7.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
  };
  let groupCollapsed7$3 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable$7) {
      if (debuggerGroupCollapsed$3) {
        logger$7.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger$7.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
  };
  let groupEnd$3 = () => {
    if (enable$7) {
      logger$7.groupEnd();
      return;
    }
  };
  let groupEnd1$3 = a => {
    if (enable$7) {
      logger$7.groupEnd(a);
      return;
    }
  };
  let groupEnd2$3 = (a, a1) => {
    if (enable$7) {
      logger$7.groupEnd(a, a1);
      return;
    }
  };
  let groupEnd3$3 = (a, a1, a2) => {
    if (enable$7) {
      logger$7.groupEnd(a, a1, a2);
      return;
    }
  };
  let Logger$3 = {
    toArgs: toArgs$3,
    logger: logger$7,
    enable: enable$7,
    debuggerGroupCollapsed: debuggerGroupCollapsed$3,
    debug: debug$6,
    debug1: debug1$3,
    debug2: debug2$3,
    debug3: debug3$3,
    debug4: debug4$3,
    debug5: debug5$3,
    debug6: debug6$3,
    banner: banner$3,
    banner1: banner1$3,
    banner2: banner2$3,
    banner3: banner3$3,
    banner4: banner4$3,
    banner5: banner5$3,
    log: log$3,
    log1: log1$3,
    log2: log2$3,
    log3: log3$3,
    log4: log4$3,
    log5: log5$3,
    groupCollapsed: groupCollapsed$7,
    groupCollapsed1: groupCollapsed1$3,
    groupCollapsed2: groupCollapsed2$3,
    groupCollapsed3: groupCollapsed3$3,
    groupCollapsed4: groupCollapsed4$3,
    groupCollapsed5: groupCollapsed5$3,
    groupCollapsed6: groupCollapsed6$3,
    groupCollapsed7: groupCollapsed7$3,
    groupEnd: groupEnd$3,
    groupEnd1: groupEnd1$3,
    groupEnd2: groupEnd2$3,
    groupEnd3: groupEnd3$3
  };
  let Runtime_env$3 = env$1;
  let Runtime_CompressOptions$3 = CompressOptions;
  let CompressOptions$7 = Runtime_CompressOptions$3;
  let compressOptions_inlineCompression$4 = CompressOptions$7.inlineCompression;
  let compressOptions_forceGridAlignment$4 = CompressOptions$7.forceGridAlignment;
  let compressOptions_westernCharacterFirst$4 = CompressOptions$7.westernCharacterFirst;
  let compressOptions_forceSpaceBetweenCJKAndWestern$4 = CompressOptions$7.forceSpaceBetweenCJKAndWestern;
  let compressOptions_fixCJKLeftQuote$4 = CompressOptions$7.fixCJKLeftQuote;
  let compressOptions$4 = {
    inlineCompression: compressOptions_inlineCompression$4,
    forceGridAlignment: compressOptions_forceGridAlignment$4,
    westernCharacterFirst: compressOptions_westernCharacterFirst$4,
    forceSpaceBetweenCJKAndWestern: compressOptions_forceSpaceBetweenCJKAndWestern$4,
    fixCJKLeftQuote: compressOptions_fixCJKLeftQuote$4
  };
  let initlize$5 = (env, options, firstChar, hasLeftQuote, paddingLeft) => {
    env._firstChar = firstChar;
    env._hasLeftQuote = hasLeftQuote;
    env._paddingLeft = paddingLeft;
    env._gridSize = options.gridSize;
    env._forceSpace = CompressOptions$7.forceSpaceBetweenCJKAndWestern ? env._gridSize / 4.0 : 0.0;
    env._spaceWidth = env._gridSize * 0.35;
    env._xInterval = options.xInterval;
    env._yInterval = options.yInterval;
    env._maxCol = options.column;
    env._lastCol = env._maxCol - 1.0;
    env._maxRow = options.row;
    let value = options.wrapWidth;
    env._maxWidth = value !== undefined ? value : env._maxCol * env._gridSize;
    env._letterSpacing = options.letterSpacing;
    env._gridWidth = env._gridSize + env._xInterval;
    return env;
  };
  let IEnv_env$3 = Runtime_env$3;
  let IEnv$3 = {
    CompressOptions: CompressOptions$7,
    env: IEnv_env$3,
    compressOptions: compressOptions$4,
    initlize: initlize$5
  };
  let CompressOptions$8 = CompressOptions;
  let Config$3 = {
    $$debugger: CompressOptions$8.$$debugger
  };
  let $$return$3 = (param, t) => t;
  let switchTo$3 = (param, t) => t;
  let tapIf$3 = (context, exp, fn) => {
    if (exp(context)) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let tapIfConst$3 = (context, exp, fn) => {
    if (exp) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let boolean$3 = (context, exp) => [exp(context) ? "True" : "False", context];
  let onTrue$3 = (context, fn) => {
    if (context[0] === "False") {
      return context;
    } else {
      return ["True", fn(context[1])];
    }
  };
  let onFalse$3 = (context, fn) => {
    if (context[0] === "False") {
      return ["False", fn(context[1])];
    } else {
      return context;
    }
  };
  let unwrap$4 = context => context[1];
  let computeIfConst$3 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIf$3 = (context, exp, fn) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIfElse$3 = (context, exp, fn, else$p) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return else$p(context);
    }
  };
  let pipeIfConst$3 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let switchIf$3 = (context, exp, else$p, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let check$3 = (context, if$p, else$p, fn) => {
    if (if$p(context)) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let pipe$3 = (context, fn) => fn(context);
  let computeIfElse$3 = (context, exp, fn, fn2) => {
    if (exp) {
      return fn(context);
    } else {
      return fn2(context);
    }
  };
  let action$3 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionU$3 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionWith$3 = (context, withCtx, fn) => {
    fn(withCtx);
    return context;
  };
  let tap$3 = (context, fn) => {
    fn();
    return context;
  };
  let debugTap$3 = (context, fn) => {
    if (Config$3.$$debugger) {
      fn();
      return context;
    } else {
      return context;
    }
  };
  let debug$7 = (context, fn) => {
    if (Config$3.$$debugger) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let ignore$3 = (context, withCtx) => context;
  let call$3 = (context, arg) => context(arg);
  let rev$3 = (b, fn, a) => fn(a, b);
  let Pipe$3 = {
    $$return: $$return$3,
    switchTo: switchTo$3,
    tapIf: tapIf$3,
    tapIfConst: tapIfConst$3,
    boolean: boolean$3,
    onTrue: onTrue$3,
    onFalse: onFalse$3,
    unwrap: unwrap$4,
    computeIfConst: computeIfConst$3,
    pipeIf: pipeIf$3,
    pipeIfElse: pipeIfElse$3,
    pipeIfConst: pipeIfConst$3,
    switchIf: switchIf$3,
    check: check$3,
    pipe: pipe$3,
    computeIfElse: computeIfElse$3,
    action: action$3,
    actionU: actionU$3,
    actionWith: actionWith$3,
    tap: tap$3,
    debugTap: debugTap$3,
    debug: debug$7,
    ignore: ignore$3,
    call: call$3,
    rev: rev$3
  };
  let env$4 = env$1;
  let context$3 = Store.context;
  let _buf$2 = {
    seqs: [],
    areaWidth: 0.0,
    areaHeight: 0.0,
    width: 0.0,
    height: 0.0,
    lastLineIndex: 0,
    lineHeights: context$3._lineHeights,
    status: context$3._status
  };
  let setContentWidth$2 = (context, width) => {
    context._contentWidth = width;
    return context;
  };
  let getContentWidth$2 = context => context._contentWidth;
  let setCurrentX$2 = (context, x) => {
    context._currentX = x;
    return context;
  };
  let getCurrentX$2 = context => context._currentX;
  let setCurrentY$2 = (context, y) => {
    context._currentY = y;
    return context;
  };
  let getCurrentY$2 = context => context._currentY;
  let setRowHeight$2 = (context, rowHeight) => {
    context._currentRowHeight = rowHeight;
    return context;
  };
  let getRowHeight$2 = context => context._currentRowHeight;
  let storeLineHeights$2 = context => {
    context._lineHeights.push(context._currentRowHeight);
    return context;
  };
  let setCurrentCol$2 = (context, col) => {
    context._currentCol = col;
    return context;
  };
  let setCurrentColU$2 = (context, col) => {
    context._currentCol = col;
  };
  let getCurrentCol$2 = context => context._currentCol;
  let setCurrentRow$2 = (context, row) => {
    context._currentRow = row;
    return context;
  };
  let getCurrentRow$2 = context => context._currentRow;
  let finish$2 = param => {};
  let syncContentWidth$2 = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    return context;
  };
  let growRowHeight$2 = (context, elementHeight) => {
    if (elementHeight > context._currentRowHeight) {
      context._currentRowHeight = elementHeight;
    }
    return context;
  };
  let resetCurrent$2 = context => {
    context._currentX = 0.0;
    let context$1 = context;
    context$1._currentY = 0.0;
    let context$2 = context$1;
    context$2._currentCol = 0.0;
    let context$3 = context$2;
    context$3._currentRow = 1;
    return context$3;
  };
  let rollbackX$2 = (context, x) => {
    debug3$3("rollbackX: %o - %o => %o", context._currentX, x, context._currentX - x);
    context._currentX = context._currentX - x;
    return context;
  };
  let moveCursorX$2 = (context, x) => {
    debug3$3("[cursor] moveX: %o + %o => %o", context._currentX, x, context._currentX + x);
    context._currentX = context._currentX + x;
    return context;
  };
  let consumeXIn$2 = (x, context) => {
    debug3$3("[cursor] moveX: %o + %o => %o", context._currentX, x, context._currentX + x);
    context._currentX = context._currentX + x;
    return x;
  };
  let growCurrentY$2 = (context, y) => {
    context._currentY = context._currentY + y;
    return context;
  };
  let rollbackCol$2 = (context, col) => {
    context._currentCol = context._currentCol - col;
    return context;
  };
  let consumeCol$2 = (context, col) => {
    context._currentCol = context._currentCol + col;
    return context;
  };
  let consumeColU$2 = (context, col) => {
    context._currentCol = context._currentCol + col;
  };
  let growCurrentRow$2 = context => {
    context._currentY = context._currentY + (context._currentRowHeight + env$4._yInterval);
    let context$1 = context;
    context$1._currentRow = context._currentRow + 1 | 0;
    return context$1;
  };
  let setLastCharWidth$2 = (context, charWidth) => {
    context._lastCharWidth = charWidth;
    return context;
  };
  let nextIsWord$2 = context => {
    let kind = context._cursor < context._source.length ? out(context._source[context._cursor + 1 | 0], getElementType) : out(context._END, getElementType);
    switch (kind) {
      case 3:
      case 12:
        return true;
      default:
        return false;
    }
  };
  let lastIsNotPunctuation$2 = context => {
    context._lastIsPunctuation = false;
    context._lastPunctuations = 0.0;
    return context;
  };
  let consumePunctuation$2 = context => {
    context._lastIsPunctuation = true;
    context._lastPunctuations = context._lastPunctuations + 1.0;
    return context;
  };
  let needForceWrap$2 = context => {
    context._isNeedForceWrap = true;
    return context;
  };
  let resetNeedForceWrap$2 = context => {
    context._isNeedForceWrap = false;
    return context;
  };
  let recordLastElement$2 = (context, char) => {
    context.rows[context._currentRow] = char;
  };
  let getRowSize$2 = context => context.rows.length;
  let prepare$2 = (context, source, options) => {
    let firstChar = source[0];
    let hasLeftQuote = out(firstChar, getElementType) === 10;
    let paddingLeft = hasLeftQuote ? out(firstChar, getWidth) + env$4._xInterval : 0.0;
    let env$5 = initlize$5(env$4, options, firstChar, hasLeftQuote, paddingLeft);
    context._currentX = 0.0;
    let context$1 = context;
    context$1._currentY = 0.0;
    let context$2 = context$1;
    context$2._currentCol = 0.0;
    let context$3 = context$2;
    context$3._currentRow = 1;
    let context$4 = context$3;
    context$4._lastIsPunctuation = false;
    context$4._lastPunctuations = 0.0;
    let context$5 = context$4;
    context$5._currentRowHeight = env$5._gridSize;
    let context$6 = context$5;
    context$6._lastCharWidth = 0.0;
    let context$7 = context$6;
    context$7._isNeedForceWrap = false;
    let context$8 = context$7;
    action$3((context$8._contentWidth = 0.0, context$8), context => {
      context._cursor = 0;
      context._lastIsWesternChar = CompressOptions$8.westernCharacterFirst;
      context._source = source;
      context.rows = [firstChar, firstChar];
    });
    debug2$3("prepare", context, env$5);
  };
  let lineWrap$2 = context => {
    log$3("lineWrap");
    /* lineWrap */
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let context$1 = context;
    context$1._currentCol = env$4._hasLeftQuote ? 1.0 : 0.0;
    let context$2 = context$1;
    context$2._currentX = env$4._paddingLeft;
    let context$3 = context$2;
    context$3._currentY = context$3._currentY + (context$3._currentRowHeight + env$4._yInterval);
    let context$4 = context$3;
    context$4._currentRow = context$3._currentRow + 1 | 0;
    let context$5 = context$4;
    context$5._lineHeights.push(context$5._currentRowHeight);
    let context$6 = context$5;
    context$6._currentRowHeight = env$4._gridSize;
    let context$7 = context$6;
    context$7._lastIsPunctuation = false;
    context$7._lastPunctuations = 0.0;
    let context$8 = context$7;
    context$8._lastCharWidth = 0.0;
    let context$9 = context$8;
    context$9._isNeedForceWrap = false;
  };
  let incrementFlush$2 = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let lastLineIndex = context._currentRow;
    let overflowY = lastLineIndex > (context.rows.length - 1 | 0);
    let contentHeight = overflowY ? context._currentY : context._currentY + (context._lineHeights.push(context._currentRowHeight), context)._currentRowHeight;
    _buf$2.seqs = context.rows;
    _buf$2.areaWidth = context._contentWidth;
    _buf$2.areaHeight = contentHeight;
    _buf$2.width = env$4._maxWidth;
    _buf$2.height = contentHeight;
    _buf$2.lastLineIndex = overflowY ? lastLineIndex - 1 | 0 : lastLineIndex;
    _buf$2.lineHeights = context._lineHeights;
    _buf$2.status = "flushing";
    return _buf$2;
  };
  let flush$2 = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let lastLineIndex = context._currentRow;
    let overflowY = lastLineIndex > (context.rows.length - 1 | 0);
    let contentHeight = overflowY ? context._currentY : context._currentY + (context._lineHeights.push(context._currentRowHeight), context)._currentRowHeight;
    _buf$2.seqs = context.rows;
    _buf$2.areaWidth = context._contentWidth;
    _buf$2.areaHeight = contentHeight;
    _buf$2.width = env$4._maxWidth;
    _buf$2.height = contentHeight;
    _buf$2.lastLineIndex = overflowY ? lastLineIndex - 1 | 0 : lastLineIndex;
    _buf$2.lineHeights = context._lineHeights;
    _buf$2.status = "closed";
    context._lineHeights = [];
    debug1$3("contentHeight: %o", contentHeight);
    return _buf$2;
  };
  let output$3 = param => ({
    seqs: _buf$2.seqs,
    areaWidth: _buf$2.areaWidth,
    areaHeight: _buf$2.areaHeight,
    width: _buf$2.width,
    height: _buf$2.height,
    lastLineIndex: _buf$2.lastLineIndex,
    lineHeights: _buf$2.lineHeights,
    status: "closed"
  });
  let Context$2 = {
    LoggerOption: LoggerOption$3,
    Logger: Logger$3,
    IEnv: IEnv$3,
    ECS: Runtime$2,
    CompressOptions: CompressOptions$8,
    Pipe: Pipe$3,
    env: env$4,
    context: context$3,
    _buf: _buf$2,
    setContentWidth: setContentWidth$2,
    getContentWidth: getContentWidth$2,
    setCurrentX: setCurrentX$2,
    getCurrentX: getCurrentX$2,
    setCurrentY: setCurrentY$2,
    getCurrentY: getCurrentY$2,
    setRowHeight: setRowHeight$2,
    getRowHeight: getRowHeight$2,
    storeLineHeights: storeLineHeights$2,
    setCurrentCol: setCurrentCol$2,
    setCurrentColU: setCurrentColU$2,
    getCurrentCol: getCurrentCol$2,
    setCurrentRow: setCurrentRow$2,
    getCurrentRow: getCurrentRow$2,
    finish: finish$2,
    syncContentWidth: syncContentWidth$2,
    growRowHeight: growRowHeight$2,
    resetCurrent: resetCurrent$2,
    rollbackX: rollbackX$2,
    moveCursorX: moveCursorX$2,
    consumeXIn: consumeXIn$2,
    growCurrentY: growCurrentY$2,
    rollbackCol: rollbackCol$2,
    consumeCol: consumeCol$2,
    consumeColU: consumeColU$2,
    growCurrentRow: growCurrentRow$2,
    setLastCharWidth: setLastCharWidth$2,
    nextIsWord: nextIsWord$2,
    lastIsNotPunctuation: lastIsNotPunctuation$2,
    consumePunctuation: consumePunctuation$2,
    needForceWrap: needForceWrap$2,
    resetNeedForceWrap: resetNeedForceWrap$2,
    recordLastElement: recordLastElement$2,
    getRowSize: getRowSize$2,
    prepare: prepare$2,
    lineWrap: lineWrap$2,
    incrementFlush: incrementFlush$2,
    flush: flush$2,
    output: output$3
  };
  let logger$8 = console;
  let $$debugger$4 = CompressOptions.$$debugger;
  let groupCollapsed$8 = CompressOptions.groupCollapsed;
  let enable$8 = process.env.NODE_ENV !== "production";
  let label$4 = "compact";
  let LoggerOption$4 = {
    logger: logger$8,
    $$debugger: $$debugger$4,
    groupCollapsed: groupCollapsed$8,
    enable: enable$8,
    label: label$4
  };
  let toArgs$4 = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return ["%c%s%c " + match, "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args.slice(1));
    }
    return ["%c%s%c %s", "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args);
  };
  let logger$9 = logger$8;
  let enable$9 = enable$8;
  let debuggerGroupCollapsed$4 = groupCollapsed$8;
  let debug$8 = a => {
    if (enable$9) {
      logger$9.debug(a);
      return;
    }
  };
  let debug1$4 = (a, a2) => {
    if (enable$9) {
      logger$9.debug(a, a2);
      return;
    }
  };
  let debug2$4 = (a, a2, a3) => {
    if (enable$9) {
      logger$9.debug(a, a2, a3);
      return;
    }
  };
  let debug3$4 = (a, a2, a3, a4) => {
    if (enable$9) {
      logger$9.debug(a, a2, a3, a4);
      return;
    }
  };
  let debug4$4 = (a, a2, a3, a4, a5) => {
    if (enable$9) {
      logger$9.debug(a, a2, a3, a4, a5);
      return;
    }
  };
  let debug5$4 = (a, a2, a3, a4, a5, a6) => {
    if (enable$9) {
      logger$9.debug(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let debug6$4 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable$9) {
      logger$9.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
  };
  let banner$4 = a => {
    if (enable$9) {
      logger$9.banner(a);
      return;
    }
  };
  let banner1$4 = (a, a2) => {
    if (enable$9) {
      logger$9.banner(a, a2);
      return;
    }
  };
  let banner2$4 = (a, a2, a3) => {
    if (enable$9) {
      logger$9.banner(a, a2, a3);
      return;
    }
  };
  let banner3$4 = (a, a2, a3, a4) => {
    if (enable$9) {
      logger$9.banner(a, a2, a3, a4);
      return;
    }
  };
  let banner4$4 = (a, a2, a3, a4, a5) => {
    if (enable$9) {
      logger$9.banner(a, a2, a3, a4, a5);
      return;
    }
  };
  let banner5$4 = (a, a2, a3, a4, a5, a6) => {
    if (enable$9) {
      logger$9.banner(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let log$4 = a => {
    if (enable$9) {
      logger$9.log(a);
      return;
    }
  };
  let log1$4 = (a, a2) => {
    if (enable$9) {
      logger$9.log(a, a2);
      return;
    }
  };
  let log2$4 = (a, a2, a3) => {
    if (!enable$9) {
      return;
    }
    let args = toArgs$4(label$4, [a, a2, a3]);
    logger$9.log.apply(null, args);
  };
  let log3$4 = (a, a2, a3, a4) => {
    if (enable$9) {
      logger$9.log(a, a2, a3, a4);
      return;
    }
  };
  let log4$4 = (a, a2, a3, a4, a5) => {
    if (enable$9) {
      logger$9.log(a, a2, a3, a4, a5);
      return;
    }
  };
  let log5$4 = (a, a2, a3, a4, a5, a6) => {
    if (enable$9) {
      logger$9.log(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let groupCollapsed$9 = a => {
    if (enable$9) {
      if (debuggerGroupCollapsed$4) {
        logger$9.groupCollapsed(a);
      } else {
        logger$9.group(a);
      }
      return;
    }
  };
  let groupCollapsed1$4 = (a, a1) => {
    if (enable$9) {
      if (debuggerGroupCollapsed$4) {
        logger$9.groupCollapsed(a, a1);
      } else {
        logger$9.group(a, a1);
      }
      return;
    }
  };
  let groupCollapsed2$4 = (a, a1, a2) => {
    if (enable$9) {
      if (debuggerGroupCollapsed$4) {
        logger$9.groupCollapsed(a, a1, a2);
      } else {
        logger$9.group(a, a1, a2);
      }
      return;
    }
  };
  let groupCollapsed3$4 = (a, a1, a2, a3) => {
    if (enable$9) {
      if (debuggerGroupCollapsed$4) {
        logger$9.groupCollapsed(a, a1, a2, a3);
      } else {
        logger$9.group(a, a1, a2, a3);
      }
      return;
    }
  };
  let groupCollapsed4$4 = (a, a1, a2, a3, a4) => {
    if (enable$9) {
      if (debuggerGroupCollapsed$4) {
        logger$9.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger$9.group(a, a1, a2, a3, a4);
      }
      return;
    }
  };
  let groupCollapsed5$4 = (a, a1, a2, a3, a4, a5) => {
    if (enable$9) {
      if (debuggerGroupCollapsed$4) {
        logger$9.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger$9.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
  };
  let groupCollapsed6$4 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable$9) {
      if (debuggerGroupCollapsed$4) {
        logger$9.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger$9.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
  };
  let groupCollapsed7$4 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable$9) {
      if (debuggerGroupCollapsed$4) {
        logger$9.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger$9.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
  };
  let groupEnd$4 = () => {
    if (enable$9) {
      logger$9.groupEnd();
      return;
    }
  };
  let groupEnd1$4 = a => {
    if (enable$9) {
      logger$9.groupEnd(a);
      return;
    }
  };
  let groupEnd2$4 = (a, a1) => {
    if (enable$9) {
      logger$9.groupEnd(a, a1);
      return;
    }
  };
  let groupEnd3$4 = (a, a1, a2) => {
    if (enable$9) {
      logger$9.groupEnd(a, a1, a2);
      return;
    }
  };
  let Logger$4 = {
    toArgs: toArgs$4,
    logger: logger$9,
    enable: enable$9,
    debuggerGroupCollapsed: debuggerGroupCollapsed$4,
    debug: debug$8,
    debug1: debug1$4,
    debug2: debug2$4,
    debug3: debug3$4,
    debug4: debug4$4,
    debug5: debug5$4,
    debug6: debug6$4,
    banner: banner$4,
    banner1: banner1$4,
    banner2: banner2$4,
    banner3: banner3$4,
    banner4: banner4$4,
    banner5: banner5$4,
    log: log$4,
    log1: log1$4,
    log2: log2$4,
    log3: log3$4,
    log4: log4$4,
    log5: log5$4,
    groupCollapsed: groupCollapsed$9,
    groupCollapsed1: groupCollapsed1$4,
    groupCollapsed2: groupCollapsed2$4,
    groupCollapsed3: groupCollapsed3$4,
    groupCollapsed4: groupCollapsed4$4,
    groupCollapsed5: groupCollapsed5$4,
    groupCollapsed6: groupCollapsed6$4,
    groupCollapsed7: groupCollapsed7$4,
    groupEnd: groupEnd$4,
    groupEnd1: groupEnd1$4,
    groupEnd2: groupEnd2$4,
    groupEnd3: groupEnd3$4
  };
  let Config$4 = {
    $$debugger: CompressOptions.$$debugger
  };
  let $$return$4 = (param, t) => t;
  let switchTo$4 = (param, t) => t;
  let tapIf$4 = (context, exp, fn) => {
    if (exp(context)) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let tapIfConst$4 = (context, exp, fn) => {
    if (exp) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let boolean$4 = (context, exp) => [exp(context) ? "True" : "False", context];
  let onTrue$4 = (context, fn) => {
    if (context[0] === "False") {
      return context;
    } else {
      return ["True", fn(context[1])];
    }
  };
  let onFalse$4 = (context, fn) => {
    if (context[0] === "False") {
      return ["False", fn(context[1])];
    } else {
      return context;
    }
  };
  let unwrap$5 = context => context[1];
  let computeIfConst$4 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIf$4 = (context, exp, fn) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIfElse$4 = (context, exp, fn, else$p) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return else$p(context);
    }
  };
  let pipeIfConst$4 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let switchIf$4 = (context, exp, else$p, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let check$4 = (context, if$p, else$p, fn) => {
    if (if$p(context)) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let pipe$4 = (context, fn) => fn(context);
  let computeIfElse$4 = (context, exp, fn, fn2) => {
    if (exp) {
      return fn(context);
    } else {
      return fn2(context);
    }
  };
  let action$4 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionU$4 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionWith$4 = (context, withCtx, fn) => {
    fn(withCtx);
    return context;
  };
  let tap$4 = (context, fn) => {
    fn();
    return context;
  };
  let debugTap$4 = (context, fn) => {
    if (Config$4.$$debugger) {
      fn();
      return context;
    } else {
      return context;
    }
  };
  let debug$9 = (context, fn) => {
    if (Config$4.$$debugger) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let ignore$4 = (context, withCtx) => context;
  let call$4 = (context, arg) => context(arg);
  let rev$4 = (b, fn, a) => fn(a, b);
  let Pipe$4 = {
    $$return: $$return$4,
    switchTo: switchTo$4,
    tapIf: tapIf$4,
    tapIfConst: tapIfConst$4,
    boolean: boolean$4,
    onTrue: onTrue$4,
    onFalse: onFalse$4,
    unwrap: unwrap$5,
    computeIfConst: computeIfConst$4,
    pipeIf: pipeIf$4,
    pipeIfElse: pipeIfElse$4,
    pipeIfConst: pipeIfConst$4,
    switchIf: switchIf$4,
    check: check$4,
    pipe: pipe$4,
    computeIfElse: computeIfElse$4,
    action: action$4,
    actionU: actionU$4,
    actionWith: actionWith$4,
    tap: tap$4,
    debugTap: debugTap$4,
    debug: debug$9,
    ignore: ignore$4,
    call: call$4,
    rev: rev$4
  };
  let env$5 = env$1;
  let processOffset = (context, start, end, offset, isMultLine) => {
    let source = context._source;
    if (!(!isMultLine && offset > 0.0)) {
      return;
    }
    debug1$4("currentX_offset", offset);
    for (let i = start; i <= end; ++i) {
      addX(unwrap$1(source[i]), offset);
    }
  };
  let pipeOffset = (context, isMultLine, start, end, currentX, currentX_before, forceSpace, allow) => {
    let offset = allow ? (currentX - currentX_before + forceSpace) / 2.0 - forceSpace : (currentX - currentX_before) / 2;
    let source = context._source;
    if (!(!isMultLine && offset > 0.0)) {
      return;
    }
    debug1$4("currentX_offset", offset);
    for (let i = start; i <= end; ++i) {
      addX(unwrap$1(source[i]), offset);
    }
  };
  let getOffsetX = (context, currentX_before, isMultLine, startElementIndex, endElementIndex, allowPangu) => {
    let forceSpace = env$5._forceSpace;
    let gridWidth = env$5._gridWidth;
    let currentX = tap$4((action$4(Math.ceil(currentX_before / gridWidth), x => setCurrentX$2(context, x)), getCurrentCol$2(context) * gridWidth), () => debug$8("forceGridAlignment"));
    if (allowPangu) {
      if (currentX >= currentX_before + forceSpace) {
        return currentX;
      }
      let t = currentX + gridWidth;
      return action$4((consumeCol$2(context, 1.0), t), __x => {
        let offset = (__x - currentX_before + forceSpace) / 2.0 - forceSpace;
        let source = context._source;
        if (!(!isMultLine && offset > 0.0)) {
          return;
        }
        debug1$4("currentX_offset", offset);
        for (let i = startElementIndex; i <= endElementIndex; ++i) {
          addX(unwrap$1(source[i]), offset);
        }
      });
    }
    if (currentX >= currentX_before) {
      return currentX;
    }
    let t$1 = currentX + gridWidth;
    return action$4((consumeCol$2(context, 1.0), t$1), __x => {
      let offset = (__x - currentX_before) / 2;
      let source = context._source;
      if (!(!isMultLine && offset > 0.0)) {
        return;
      }
      debug1$4("currentX_offset", offset);
      for (let i = startElementIndex; i <= endElementIndex; ++i) {
        addX(unwrap$1(source[i]), offset);
      }
    });
  };
  let consumeOffsetX = context => {
    let offsetX = context._buf_offsetX;
    if (offsetX !== 0.0) {
      consumeXIn$2(pipeIfConst$4(action$4(offsetX, __x => debug1$4("consumeOffsetX", __x)), offsetX > env$5._gridSize, offsetX => {
        rollbackCol$2(context, 1.0);
        return offsetX - env$5._gridSize;
      }), context);
      return;
    }
  };
  let buffOffsetX = (context, fontSize, overflow) => {
    let gridSize = env$5._gridSize;
    let gridWidth = env$5._gridWidth;
    if (fontSize === gridSize) {
      return;
    }
    debug$8("gridAlignment");
    let offset_x = (fontSize - gridSize) / 2.0;
    context._buf_offsetX = pipe$4(consumeCol$2(context, offset_x > 0.0 ? Math.ceil(offset_x * 2.0 / gridWidth) : 0.0), context => {
      let offset_x$1 = ((Math.ceil(offset_x * 2.0 / gridWidth) + 1.0) * gridWidth - fontSize) / 2.0;
      let t = overflow ? (lineWrap$2(context), offset_x$1) * 2.0 : offset_x$1;
      moveCursorX$2(context, offset_x$1);
      return t;
    });
  };
  let ForceGridAlignment_getOffsetX = getOffsetX;
  let ForceGridAlignment_consumeOffsetX = consumeOffsetX;
  let ForceGridAlignment_buffOffsetX = buffOffsetX;
  let ForceGridAlignment = {
    getOffsetX: ForceGridAlignment_getOffsetX,
    consumeOffsetX: ForceGridAlignment_consumeOffsetX,
    buffOffsetX: ForceGridAlignment_buffOffsetX
  };
  let Config$5 = {
    $$debugger: CompressOptions$4.$$debugger
  };
  let $$return$5 = (param, t) => t;
  let switchTo$5 = (param, t) => t;
  let tapIf$5 = (context, exp, fn) => {
    if (exp(context)) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let tapIfConst$5 = (context, exp, fn) => {
    if (exp) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let boolean$5 = (context, exp) => [exp(context) ? "True" : "False", context];
  let onTrue$5 = (context, fn) => {
    if (context[0] === "False") {
      return context;
    } else {
      return ["True", fn(context[1])];
    }
  };
  let onFalse$5 = (context, fn) => {
    if (context[0] === "False") {
      return ["False", fn(context[1])];
    } else {
      return context;
    }
  };
  let unwrap$6 = context => context[1];
  let computeIfConst$5 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIf$5 = (context, exp, fn) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIfElse$5 = (context, exp, fn, else$p) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return else$p(context);
    }
  };
  let pipeIfConst$5 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let switchIf$5 = (context, exp, else$p, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let check$5 = (context, if$p, else$p, fn) => {
    if (if$p(context)) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let pipe$5 = (context, fn) => fn(context);
  let computeIfElse$5 = (context, exp, fn, fn2) => {
    if (exp) {
      return fn(context);
    } else {
      return fn2(context);
    }
  };
  let action$5 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionU$5 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionWith$5 = (context, withCtx, fn) => {
    fn(withCtx);
    return context;
  };
  let tap$5 = (context, fn) => {
    fn();
    return context;
  };
  let debugTap$5 = (context, fn) => {
    if (Config$5.$$debugger) {
      fn();
      return context;
    } else {
      return context;
    }
  };
  let debug$10 = (context, fn) => {
    if (Config$5.$$debugger) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let ignore$5 = (context, withCtx) => context;
  let call$5 = (context, arg) => context(arg);
  let rev$5 = (b, fn, a) => fn(a, b);
  let Pipe$5 = {
    $$return: $$return$5,
    switchTo: switchTo$5,
    tapIf: tapIf$5,
    tapIfConst: tapIfConst$5,
    boolean: boolean$5,
    onTrue: onTrue$5,
    onFalse: onFalse$5,
    unwrap: unwrap$6,
    computeIfConst: computeIfConst$5,
    pipeIf: pipeIf$5,
    pipeIfElse: pipeIfElse$5,
    pipeIfConst: pipeIfConst$5,
    switchIf: switchIf$5,
    check: check$5,
    pipe: pipe$5,
    computeIfElse: computeIfElse$5,
    action: action$5,
    actionU: actionU$5,
    actionWith: actionWith$5,
    tap: tap$5,
    debugTap: debugTap$5,
    debug: debug$10,
    ignore: ignore$5,
    call: call$5,
    rev: rev$5
  };
  let env$6 = env$1;
  let ctx = {
    _buffer_width: 0.0,
    _wordStartElementIndex: 0,
    _processingElement: 0,
    _startElement: 0,
    _buffer_row: 0,
    _prevIsSpace: false,
    _processing: false
  };
  let isProcessing = () => ctx._processing;
  let debug_get_characters = (context, start, end) => {
    if (CompressOptions$4.$$debugger) {
      return context._source.slice(start, end).map(__x => out(__x, getCharacter));
    } else {
      return [];
    }
  };
  let buffWidth = (compactCtx, add) => {
    /* buffWidth */compactCtx._buffer_width = compactCtx._buffer_width + add;
    debug2$1("buffWidth += %o = %o", add, compactCtx._buffer_width);
    return compactCtx;
  };
  let clearBuffWidth = compactCtx => {
    if (compactCtx._buffer_width > 0.0) {
      log$1("clear buffWidth");
      compactCtx._buffer_width = 0.0;
    }
    return compactCtx;
  };
  let growRow = compactCtx => {
    compactCtx._buffer_row = compactCtx._buffer_row + 1 | 0;
  };
  let isMultipleRow = compactCtx => compactCtx._buffer_row !== 0;
  let endProcess = compactCtx => {
    compactCtx._processing = false;
  };
  let next = compactCtx => {
    compactCtx._processingElement = compactCtx._processingElement + 1 | 0;
  };
  let allowPanguClose = context => {
    let kind = out(context._prevChar, getElementType);
    let tmp = false;
    if (kind !== 2 && kind !== 14 && kind !== 3 && kind !== 17 && kind !== 4 && kind !== 5 && kind !== 1 && kind !== 0) {
      let tmp$1;
      switch (kind) {
        case 6:
        case 8:
        case 9:
        case 11:
        case 16:
          tmp$1 = true;
          break;
        default:
          tmp$1 = false;
      }
      tmp = tmp$1;
    }
    return !tmp;
  };
  let processPanguBegin = context => {
    if (getCurrentX$1(context) > 0.0) {
      return finish$1(moveCursorX$1(context, env$6._forceSpace));
    }
  };
  let lineWrap$3 = (compactCtx, context, buffer_width) => {
    debug4$1("lineWrap (buffer_width)%f + (currentX)%f = (overflowWidth)%f > (maxWidth)%f", buffer_width, getCurrentX$1(context), buffer_width + getCurrentX$1(context), env$6._maxWidth);
    growCurrentRow$1(setCurrentX$1(syncContentWidth$1(rollbackX$1(context, env$6._spaceWidth)), env$6._paddingLeft));
    compactCtx._buffer_row = compactCtx._buffer_row + 1 | 0;
    if (getCurrentRow$1(context) > env$6._maxRow) {
      context.rows.pop();
      return "Break";
    } else {
      return "Continue";
    }
  };
  let loopPos = (context, wordEnd, i) => {
    for (let i$1 = i; i$1 <= wordEnd; ++i$1) {
      out(context._source[i$1], target => tapIfConst$5(setPosition(target, getCurrentX$1(context), getCurrentY$1(context) - (getFontSize(target) - env$6._gridSize) / 2.0), getElementType(target) !== 4, target => {
        log4$1("[%d] move %o %o(width) + %o(letterSpacing)", context._source[i$1], getCharacter(target), getWidth(target), env$6._letterSpacing);
        return moveCursorX$1(context, getWidth(target) + env$6._letterSpacing);
      }));
    }
    return context._source[wordEnd];
  };
  let openWindow = start_i => {
    groupCollapsed1$1("initWindow start=%o", start_i);
    ctx._processing = true;
    if (ctx._buffer_width > 0.0) {
      log$1("clear buffWidth");
      ctx._buffer_width = 0.0;
    }
    ctx._startElement = start_i;
    ctx._processingElement = start_i;
    ctx._wordStartElementIndex = start_i;
    ctx._buffer_row = 0;
    groupEnd$1();
  };
  let _flush = (compactCtx, context, isEnd, end, start) => {
    let buffer_width = compactCtx._buffer_width;
    if (buffer_width + getCurrentX$1(context) > env$6._maxWidth) {
      debug4$1("lineWrap (buffer_width)%f + (currentX)%f = (overflowWidth)%f > (maxWidth)%f", buffer_width, getCurrentX$1(context), buffer_width + getCurrentX$1(context), env$6._maxWidth);
      growCurrentRow$1(setCurrentX$1(syncContentWidth$1(rollbackX$1(context, env$6._spaceWidth)), env$6._paddingLeft));
      compactCtx._buffer_row = compactCtx._buffer_row + 1 | 0;
      let $$continue = getCurrentRow$1(context) > env$6._maxRow ? (context.rows.pop(), "Break") : "Continue";
      if (isEnd) {
        groupCollapsed3$1("consume flush end char=%d...%d characters=%o...%o", start, end - 1 | 0, CompressOptions$4.$$debugger ? context._source.slice(start, end).map(__x => out(__x, getCharacter)) : []);
      } else {
        groupCollapsed4$1("consume [%d] flush char=%d...%d => %o", end, start, end - 1 | 0, CompressOptions$4.$$debugger ? context._source.slice(start, end).map(__x => out(__x, getCharacter)) : []);
      }
      if ($$continue === "Continue") {
        compactCtx._wordStartElementIndex = end + 1 | 0;
        let wordEnd = end - 1 | 0;
        for (let i = start; i <= wordEnd; ++i) {
          out(context._source[i], target => tapIfConst$5(setPosition(target, getCurrentX$1(context), getCurrentY$1(context) - (getFontSize(target) - env$6._gridSize) / 2.0), getElementType(target) !== 4, target => {
            log4$1("[%d] move %o %o(width) + %o(letterSpacing)", context._source[i], getCharacter(target), getWidth(target), env$6._letterSpacing);
            return moveCursorX$1(context, getWidth(target) + env$6._letterSpacing);
          }));
        }
        tapIfConst$5(recordLastElement$1(context, context._source[wordEnd]), !isEnd, () => {
          log1$1("add Space", env$6._spaceWidth);
          compactCtx._prevIsSpace = true;
          finish$1(moveCursorX$1(context, env$6._spaceWidth));
        });
        let compactCtx$1 = compactCtx;
        if (compactCtx$1._buffer_width > 0.0) {
          log$1("clear buffWidth");
          compactCtx$1._buffer_width = 0.0;
        }
        let compactCtx$2 = compactCtx$1;
        compactCtx$2._processingElement = compactCtx$2._processingElement + 1 | 0;
      }
      groupEnd$1();
      return $$continue;
    }
    if (isEnd) {
      groupCollapsed3$1("consume flush end char=%d...%d characters=%o...%o", start, end - 1 | 0, CompressOptions$4.$$debugger ? context._source.slice(start, end).map(__x => out(__x, getCharacter)) : []);
    } else {
      groupCollapsed4$1("consume [%d] flush char=%d...%d => %o", end, start, end - 1 | 0, CompressOptions$4.$$debugger ? context._source.slice(start, end).map(__x => out(__x, getCharacter)) : []);
    }
    compactCtx._wordStartElementIndex = end + 1 | 0;
    let wordEnd$1 = end - 1 | 0;
    for (let i$1 = start; i$1 <= wordEnd$1; ++i$1) {
      out(context._source[i$1], target => tapIfConst$5(setPosition(target, getCurrentX$1(context), getCurrentY$1(context) - (getFontSize(target) - env$6._gridSize) / 2.0), getElementType(target) !== 4, target => {
        log4$1("[%d] move %o %o(width) + %o(letterSpacing)", context._source[i$1], getCharacter(target), getWidth(target), env$6._letterSpacing);
        return moveCursorX$1(context, getWidth(target) + env$6._letterSpacing);
      }));
    }
    tapIfConst$5(recordLastElement$1(context, context._source[wordEnd$1]), !isEnd, () => {
      log1$1("add Space", env$6._spaceWidth);
      compactCtx._prevIsSpace = true;
      finish$1(moveCursorX$1(context, env$6._spaceWidth));
    });
    let compactCtx$3 = compactCtx;
    if (compactCtx$3._buffer_width > 0.0) {
      log$1("clear buffWidth");
      compactCtx$3._buffer_width = 0.0;
    }
    let compactCtx$4 = compactCtx$3;
    compactCtx$4._processingElement = compactCtx$4._processingElement + 1 | 0;
    groupEnd$1();
    return "Continue";
  };
  let flush$3 = (compactCtx, context, end, start) => {
    let buffer_width = compactCtx._buffer_width;
    let _next = $$continue => {
      groupCollapsed4$1("consume [%d] flush char=%d...%d => %o", end, start, end - 1 | 0, CompressOptions$4.$$debugger ? context._source.slice(start, end).map(__x => out(__x, getCharacter)) : []);
      if ($$continue === "Continue") {
        compactCtx._wordStartElementIndex = end + 1 | 0;
        let wordEnd = end - 1 | 0;
        for (let i = start; i <= wordEnd; ++i) {
          out(context._source[i], target => tapIfConst$5(setPosition(target, getCurrentX$1(context), getCurrentY$1(context) - (getFontSize(target) - env$6._gridSize) / 2.0), getElementType(target) !== 4, target => {
            log4$1("[%d] move %o %o(width) + %o(letterSpacing)", context._source[i], getCharacter(target), getWidth(target), env$6._letterSpacing);
            return moveCursorX$1(context, getWidth(target) + env$6._letterSpacing);
          }));
        }
        tapIfConst$5(recordLastElement$1(context, context._source[wordEnd]), true, () => {
          log1$1("add Space", env$6._spaceWidth);
          compactCtx._prevIsSpace = true;
          finish$1(moveCursorX$1(context, env$6._spaceWidth));
        });
        let compactCtx$1 = compactCtx;
        if (compactCtx$1._buffer_width > 0.0) {
          log$1("clear buffWidth");
          compactCtx$1._buffer_width = 0.0;
        }
        let compactCtx$2 = compactCtx$1;
        compactCtx$2._processingElement = compactCtx$2._processingElement + 1 | 0;
      }
      groupEnd$1();
      return $$continue;
    };
    if (buffer_width + getCurrentX$1(context) > env$6._maxWidth) {
      return Primitive_curry._1(_next, (debug4$1("lineWrap (buffer_width)%f + (currentX)%f = (overflowWidth)%f > (maxWidth)%f", buffer_width, getCurrentX$1(context), buffer_width + getCurrentX$1(context), env$6._maxWidth), growCurrentRow$1(setCurrentX$1(syncContentWidth$1(rollbackX$1(context, env$6._spaceWidth)), env$6._paddingLeft)), compactCtx._buffer_row = compactCtx._buffer_row + 1 | 0, getCurrentRow$1(context) > env$6._maxRow ? (context.rows.pop(), "Break") : "Continue"));
    } else {
      return _next("Continue");
    }
  };
  let end_flush = (compactCtx, context, end, start) => {
    let buffer_width = compactCtx._buffer_width;
    let _next = $$continue => {
      groupCollapsed3$1("consume flush end char=%d...%d characters=%o...%o", start, end - 1 | 0, CompressOptions$4.$$debugger ? context._source.slice(start, end).map(__x => out(__x, getCharacter)) : []);
      if ($$continue === "Continue") {
        compactCtx._wordStartElementIndex = end + 1 | 0;
        let wordEnd = end - 1 | 0;
        for (let i = start; i <= wordEnd; ++i) {
          out(context._source[i], target => tapIfConst$5(setPosition(target, getCurrentX$1(context), getCurrentY$1(context) - (getFontSize(target) - env$6._gridSize) / 2.0), getElementType(target) !== 4, target => {
            log4$1("[%d] move %o %o(width) + %o(letterSpacing)", context._source[i], getCharacter(target), getWidth(target), env$6._letterSpacing);
            return moveCursorX$1(context, getWidth(target) + env$6._letterSpacing);
          }));
        }
        tapIfConst$5(recordLastElement$1(context, context._source[wordEnd]), false, () => {
          log1$1("add Space", env$6._spaceWidth);
          compactCtx._prevIsSpace = true;
          finish$1(moveCursorX$1(context, env$6._spaceWidth));
        });
        let compactCtx$1 = compactCtx;
        if (compactCtx$1._buffer_width > 0.0) {
          log$1("clear buffWidth");
          compactCtx$1._buffer_width = 0.0;
        }
        let compactCtx$2 = compactCtx$1;
        compactCtx$2._processingElement = compactCtx$2._processingElement + 1 | 0;
      }
      groupEnd$1();
      return $$continue;
    };
    if (buffer_width + getCurrentX$1(context) > env$6._maxWidth) {
      return Primitive_curry._1(_next, (debug4$1("lineWrap (buffer_width)%f + (currentX)%f = (overflowWidth)%f > (maxWidth)%f", buffer_width, getCurrentX$1(context), buffer_width + getCurrentX$1(context), env$6._maxWidth), growCurrentRow$1(setCurrentX$1(syncContentWidth$1(rollbackX$1(context, env$6._spaceWidth)), env$6._paddingLeft)), compactCtx._buffer_row = compactCtx._buffer_row + 1 | 0, getCurrentRow$1(context) > env$6._maxRow ? (context.rows.pop(), "Break") : "Continue"));
    } else {
      return _next("Continue");
    }
  };
  let _processWindow = (context, char, elementHeight, isEnd) => {
    let start = ctx._wordStartElementIndex;
    debug1$1("processWindow start=%o", start);
    let tmp;
    if (isEnd) {
      let end = ctx._processingElement;
      let buffer_width = ctx._buffer_width;
      let _next = $$continue => {
        groupCollapsed3$1("consume flush end char=%d...%d characters=%o...%o", start, end - 1 | 0, CompressOptions$4.$$debugger ? context._source.slice(start, end).map(__x => out(__x, getCharacter)) : []);
        if ($$continue === "Continue") {
          ctx._wordStartElementIndex = end + 1 | 0;
          let wordEnd = end - 1 | 0;
          for (let i = start; i <= wordEnd; ++i) {
            out(context._source[i], target => tapIfConst$5(setPosition(target, getCurrentX$1(context), getCurrentY$1(context) - (getFontSize(target) - env$6._gridSize) / 2.0), getElementType(target) !== 4, target => {
              log4$1("[%d] move %o %o(width) + %o(letterSpacing)", context._source[i], getCharacter(target), getWidth(target), env$6._letterSpacing);
              return moveCursorX$1(context, getWidth(target) + env$6._letterSpacing);
            }));
          }
          tapIfConst$5(recordLastElement$1(context, context._source[wordEnd]), false, () => {
            log1$1("add Space", env$6._spaceWidth);
            ctx._prevIsSpace = true;
            finish$1(moveCursorX$1(context, env$6._spaceWidth));
          });
          let compactCtx = ctx;
          if (compactCtx._buffer_width > 0.0) {
            log$1("clear buffWidth");
            compactCtx._buffer_width = 0.0;
          }
          let compactCtx$1 = compactCtx;
          compactCtx$1._processingElement = compactCtx$1._processingElement + 1 | 0;
        }
        groupEnd$1();
        return $$continue;
      };
      tmp = buffer_width + getCurrentX$1(context) > env$6._maxWidth ? Primitive_curry._1(_next, (debug4$1("lineWrap (buffer_width)%f + (currentX)%f = (overflowWidth)%f > (maxWidth)%f", buffer_width, getCurrentX$1(context), buffer_width + getCurrentX$1(context), env$6._maxWidth), growCurrentRow$1(setCurrentX$1(syncContentWidth$1(rollbackX$1(context, env$6._spaceWidth)), env$6._paddingLeft)), ctx._buffer_row = ctx._buffer_row + 1 | 0, getCurrentRow$1(context) > env$6._maxRow ? (context.rows.pop(), "Break") : "Continue")) : _next("Continue");
    } else if (out(char, getElementType) === 4) {
      let end$1 = ctx._processingElement;
      let buffer_width$1 = ctx._buffer_width;
      let _next$1 = $$continue => {
        groupCollapsed4$1("consume [%d] flush char=%d...%d => %o", end$1, start, end$1 - 1 | 0, CompressOptions$4.$$debugger ? context._source.slice(start, end$1).map(__x => out(__x, getCharacter)) : []);
        if ($$continue === "Continue") {
          ctx._wordStartElementIndex = end$1 + 1 | 0;
          let wordEnd = end$1 - 1 | 0;
          for (let i = start; i <= wordEnd; ++i) {
            out(context._source[i], target => tapIfConst$5(setPosition(target, getCurrentX$1(context), getCurrentY$1(context) - (getFontSize(target) - env$6._gridSize) / 2.0), getElementType(target) !== 4, target => {
              log4$1("[%d] move %o %o(width) + %o(letterSpacing)", context._source[i], getCharacter(target), getWidth(target), env$6._letterSpacing);
              return moveCursorX$1(context, getWidth(target) + env$6._letterSpacing);
            }));
          }
          tapIfConst$5(recordLastElement$1(context, context._source[wordEnd]), true, () => {
            log1$1("add Space", env$6._spaceWidth);
            ctx._prevIsSpace = true;
            finish$1(moveCursorX$1(context, env$6._spaceWidth));
          });
          let compactCtx = ctx;
          if (compactCtx._buffer_width > 0.0) {
            log$1("clear buffWidth");
            compactCtx._buffer_width = 0.0;
          }
          let compactCtx$1 = compactCtx;
          compactCtx$1._processingElement = compactCtx$1._processingElement + 1 | 0;
        }
        groupEnd$1();
        return $$continue;
      };
      tmp = buffer_width$1 + getCurrentX$1(context) > env$6._maxWidth ? Primitive_curry._1(_next$1, (debug4$1("lineWrap (buffer_width)%f + (currentX)%f = (overflowWidth)%f > (maxWidth)%f", buffer_width$1, getCurrentX$1(context), buffer_width$1 + getCurrentX$1(context), env$6._maxWidth), growCurrentRow$1(setCurrentX$1(syncContentWidth$1(rollbackX$1(context, env$6._spaceWidth)), env$6._paddingLeft)), ctx._buffer_row = ctx._buffer_row + 1 | 0, getCurrentRow$1(context) > env$6._maxRow ? (context.rows.pop(), "Break") : "Continue")) : _next$1("Continue");
    } else {
      if (ctx._prevIsSpace) {
        ctx._prevIsSpace = false;
      }
      groupCollapsed2$1("consume [%d]:%o", ctx._processingElement, out(char, getCharacter));
      growRowHeight$1(context, elementHeight);
      out(char, char => {
        setH(char, elementHeight);
        let add = getWidth(char);
        /* buffWidth */
        ctx._buffer_width = ctx._buffer_width + add;
        debug2$1("buffWidth += %o = %o", add, ctx._buffer_width);
        let compactCtx = ctx;
        compactCtx._processingElement = compactCtx._processingElement + 1 | 0;
      });
      groupEnd$1();
      tmp = "Continue";
    }
    return debugTap$5(tmp, () => banner1$1("next currentX=%o", getCurrentX$1(context)));
  };
  let processWindow = (context, char, elementHeight) => {
    let start = ctx._wordStartElementIndex;
    debug1$1("processWindow start=%o", start);
    let tmp;
    if (out(char, getElementType) === 4) {
      let end = ctx._processingElement;
      let buffer_width = ctx._buffer_width;
      let _next = $$continue => {
        groupCollapsed4$1("consume [%d] flush char=%d...%d => %o", end, start, end - 1 | 0, CompressOptions$4.$$debugger ? context._source.slice(start, end).map(__x => out(__x, getCharacter)) : []);
        if ($$continue === "Continue") {
          ctx._wordStartElementIndex = end + 1 | 0;
          let wordEnd = end - 1 | 0;
          for (let i = start; i <= wordEnd; ++i) {
            out(context._source[i], target => tapIfConst$5(setPosition(target, getCurrentX$1(context), getCurrentY$1(context) - (getFontSize(target) - env$6._gridSize) / 2.0), getElementType(target) !== 4, target => {
              log4$1("[%d] move %o %o(width) + %o(letterSpacing)", context._source[i], getCharacter(target), getWidth(target), env$6._letterSpacing);
              return moveCursorX$1(context, getWidth(target) + env$6._letterSpacing);
            }));
          }
          tapIfConst$5(recordLastElement$1(context, context._source[wordEnd]), true, () => {
            log1$1("add Space", env$6._spaceWidth);
            ctx._prevIsSpace = true;
            finish$1(moveCursorX$1(context, env$6._spaceWidth));
          });
          let compactCtx = ctx;
          if (compactCtx._buffer_width > 0.0) {
            log$1("clear buffWidth");
            compactCtx._buffer_width = 0.0;
          }
          let compactCtx$1 = compactCtx;
          compactCtx$1._processingElement = compactCtx$1._processingElement + 1 | 0;
        }
        groupEnd$1();
        return $$continue;
      };
      tmp = buffer_width + getCurrentX$1(context) > env$6._maxWidth ? Primitive_curry._1(_next, (debug4$1("lineWrap (buffer_width)%f + (currentX)%f = (overflowWidth)%f > (maxWidth)%f", buffer_width, getCurrentX$1(context), buffer_width + getCurrentX$1(context), env$6._maxWidth), growCurrentRow$1(setCurrentX$1(syncContentWidth$1(rollbackX$1(context, env$6._spaceWidth)), env$6._paddingLeft)), ctx._buffer_row = ctx._buffer_row + 1 | 0, getCurrentRow$1(context) > env$6._maxRow ? (context.rows.pop(), "Break") : "Continue")) : _next("Continue");
    } else {
      if (ctx._prevIsSpace) {
        ctx._prevIsSpace = false;
      }
      groupCollapsed2$1("consume [%d]:%o", ctx._processingElement, out(char, getCharacter));
      growRowHeight$1(context, elementHeight);
      out(char, char => {
        setH(char, elementHeight);
        let add = getWidth(char);
        /* buffWidth */
        ctx._buffer_width = ctx._buffer_width + add;
        debug2$1("buffWidth += %o = %o", add, ctx._buffer_width);
        let compactCtx = ctx;
        compactCtx._processingElement = compactCtx._processingElement + 1 | 0;
      });
      groupEnd$1();
      tmp = "Continue";
    }
    return debugTap$5(tmp, () => banner1$1("next currentX=%o", getCurrentX$1(context)));
  };
  let _closeWindow = context => {
    let start = ctx._wordStartElementIndex;
    debug1$1("processWindow start=%o", start);
    let end = ctx._processingElement;
    let buffer_width = ctx._buffer_width;
    let _next = $$continue => {
      groupCollapsed3$1("consume flush end char=%d...%d characters=%o...%o", start, end - 1 | 0, CompressOptions$4.$$debugger ? context._source.slice(start, end).map(__x => out(__x, getCharacter)) : []);
      if ($$continue === "Continue") {
        ctx._wordStartElementIndex = end + 1 | 0;
        let wordEnd = end - 1 | 0;
        for (let i = start; i <= wordEnd; ++i) {
          out(context._source[i], target => tapIfConst$5(setPosition(target, getCurrentX$1(context), getCurrentY$1(context) - (getFontSize(target) - env$6._gridSize) / 2.0), getElementType(target) !== 4, target => {
            log4$1("[%d] move %o %o(width) + %o(letterSpacing)", context._source[i], getCharacter(target), getWidth(target), env$6._letterSpacing);
            return moveCursorX$1(context, getWidth(target) + env$6._letterSpacing);
          }));
        }
        tapIfConst$5(recordLastElement$1(context, context._source[wordEnd]), false, () => {
          log1$1("add Space", env$6._spaceWidth);
          ctx._prevIsSpace = true;
          finish$1(moveCursorX$1(context, env$6._spaceWidth));
        });
        let compactCtx = ctx;
        if (compactCtx._buffer_width > 0.0) {
          log$1("clear buffWidth");
          compactCtx._buffer_width = 0.0;
        }
        let compactCtx$1 = compactCtx;
        compactCtx$1._processingElement = compactCtx$1._processingElement + 1 | 0;
      }
      groupEnd$1();
      return $$continue;
    };
    return debugTap$5(buffer_width + getCurrentX$1(context) > env$6._maxWidth ? Primitive_curry._1(_next, (debug4$1("lineWrap (buffer_width)%f + (currentX)%f = (overflowWidth)%f > (maxWidth)%f", buffer_width, getCurrentX$1(context), buffer_width + getCurrentX$1(context), env$6._maxWidth), growCurrentRow$1(setCurrentX$1(syncContentWidth$1(rollbackX$1(context, env$6._spaceWidth)), env$6._paddingLeft)), ctx._buffer_row = ctx._buffer_row + 1 | 0, getCurrentRow$1(context) > env$6._maxRow ? (context.rows.pop(), "Break") : "Continue")) : _next("Continue"), () => banner1$1("next currentX=%o", getCurrentX$1(context)));
  };
  let closeWindow = (context, allowPangu) => {
    let endElementIndex = context._cursor;
    let startElementIndex = ctx._startElement;
    debug1$1("closeWindow: %o", {
      startElementIndex: startElementIndex,
      endElementIndex: endElementIndex,
      allowPangu: allowPangu,
      forceGridAlignment: CompressOptions$4.forceGridAlignment
    });
    let start = ctx._wordStartElementIndex;
    debug1$1("processWindow start=%o", start);
    let end = ctx._processingElement;
    let buffer_width = ctx._buffer_width;
    let _next = $$continue => {
      groupCollapsed3$1("consume flush end char=%d...%d characters=%o...%o", start, end - 1 | 0, CompressOptions$4.$$debugger ? context._source.slice(start, end).map(__x => out(__x, getCharacter)) : []);
      if ($$continue === "Continue") {
        ctx._wordStartElementIndex = end + 1 | 0;
        let wordEnd = end - 1 | 0;
        for (let i = start; i <= wordEnd; ++i) {
          out(context._source[i], target => tapIfConst$5(setPosition(target, getCurrentX$1(context), getCurrentY$1(context) - (getFontSize(target) - env$6._gridSize) / 2.0), getElementType(target) !== 4, target => {
            log4$1("[%d] move %o %o(width) + %o(letterSpacing)", context._source[i], getCharacter(target), getWidth(target), env$6._letterSpacing);
            return moveCursorX$1(context, getWidth(target) + env$6._letterSpacing);
          }));
        }
        tapIfConst$5(recordLastElement$1(context, context._source[wordEnd]), false, () => {
          log1$1("add Space", env$6._spaceWidth);
          ctx._prevIsSpace = true;
          finish$1(moveCursorX$1(context, env$6._spaceWidth));
        });
        let compactCtx = ctx;
        if (compactCtx._buffer_width > 0.0) {
          log$1("clear buffWidth");
          compactCtx._buffer_width = 0.0;
        }
        let compactCtx$1 = compactCtx;
        compactCtx$1._processingElement = compactCtx$1._processingElement + 1 | 0;
      }
      groupEnd$1();
      return $$continue;
    };
    debugTap$5(buffer_width + getCurrentX$1(context) > env$6._maxWidth ? Primitive_curry._1(_next, (debug4$1("lineWrap (buffer_width)%f + (currentX)%f = (overflowWidth)%f > (maxWidth)%f", buffer_width, getCurrentX$1(context), buffer_width + getCurrentX$1(context), env$6._maxWidth), growCurrentRow$1(setCurrentX$1(syncContentWidth$1(rollbackX$1(context, env$6._spaceWidth)), env$6._paddingLeft)), ctx._buffer_row = ctx._buffer_row + 1 | 0, getCurrentRow$1(context) > env$6._maxRow ? (context.rows.pop(), "Break") : "Continue")) : _next("Continue"), () => banner1$1("next currentX=%o", getCurrentX$1(context)));
    ctx._processing = false;
    let currentX_before = getCurrentX$1(context);
    debug1$1("buffer_row: %o", ctx._buffer_row);
    let currentX = CompressOptions$4.forceGridAlignment ? getOffsetX(context, currentX_before, ctx._buffer_row !== 0, ctx._startElement, endElementIndex, allowPangu) : (setCurrentColU$1(context, currentX_before / env$6._gridWidth), allowPangu ? (consumeColU$1(context, 0.25), currentX_before + (env$6._forceSpace + env$6._xInterval)) : currentX_before);
    debug2$1("currentX %d => %d", currentX_before, currentX);
    setCurrentX$1(context, currentX);
  };
  let CompactContext = {
    LoggerOption: LoggerOption$1,
    Logger: Logger$1,
    IEnv: IEnv$1,
    ECS: Runtime$2,
    CompressOptions: CompressOptions$4,
    Context: Context$1,
    ForceGridAlignment: ForceGridAlignment,
    Pipe: Pipe$5,
    env: env$6,
    ctx: ctx,
    isProcessing: isProcessing,
    debug_get_characters: debug_get_characters,
    buffWidth: buffWidth,
    clearBuffWidth: clearBuffWidth,
    growRow: growRow,
    isMultipleRow: isMultipleRow,
    endProcess: endProcess,
    next: next,
    allowPanguClose: allowPanguClose,
    processPanguBegin: processPanguBegin,
    lineWrap: lineWrap$3,
    loopPos: loopPos,
    openWindow: openWindow,
    _flush: _flush,
    flush: flush$3,
    end_flush: end_flush,
    _processWindow: _processWindow,
    processWindow: processWindow,
    _closeWindow: _closeWindow,
    closeWindow: closeWindow
  };
  let Runtime$3 = {
    env: env$1,
    create: create,
    getCharacter: getCharacter,
    setCharacter: setCharacter,
    getFont: getFont,
    setFont: setFont,
    getCharCode: getCharCode,
    getElementType: getElementType,
    setElementType: setElementType,
    getWidth: getWidth,
    getHeight: getHeight,
    getX: getX,
    getY: getY,
    getFontSize: getFontSize,
    setW: setW,
    setH: setH,
    setX: setX,
    setY: setY,
    setFontSize: setFontSize,
    addX: addX,
    setPosition: setPosition,
    setSize: setSize,
    unwrap: unwrap$1,
    map: map,
    out: out,
    input: input,
    cursor: cursor,
    output: output,
    grow: grow,
    initlize: initlize$1,
    CompressOptions: CompressOptions,
    Store: Store
  };
  let logger$10 = console;
  let $$debugger$5 = CompressOptions.$$debugger;
  let groupCollapsed$10 = CompressOptions.groupCollapsed;
  let enable$10 = process.env.NODE_ENV !== "production";
  let label$5 = "main";
  let LoggerOption$5 = {
    logger: logger$10,
    $$debugger: $$debugger$5,
    groupCollapsed: groupCollapsed$10,
    enable: enable$10,
    label: label$5
  };
  let toArgs$5 = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return ["%c%s%c " + match, "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args.slice(1));
    }
    return ["%c%s%c %s", "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args);
  };
  let logger$11 = logger$10;
  let enable$11 = enable$10;
  let debuggerGroupCollapsed$5 = groupCollapsed$10;
  let debug$11 = a => {
    if (enable$11) {
      logger$11.debug(a);
      return;
    }
  };
  let debug1$5 = (a, a2) => {
    if (enable$11) {
      logger$11.debug(a, a2);
      return;
    }
  };
  let debug2$5 = (a, a2, a3) => {
    if (enable$11) {
      logger$11.debug(a, a2, a3);
      return;
    }
  };
  let debug3$5 = (a, a2, a3, a4) => {
    if (enable$11) {
      logger$11.debug(a, a2, a3, a4);
      return;
    }
  };
  let debug4$5 = (a, a2, a3, a4, a5) => {
    if (enable$11) {
      logger$11.debug(a, a2, a3, a4, a5);
      return;
    }
  };
  let debug5$5 = (a, a2, a3, a4, a5, a6) => {
    if (enable$11) {
      logger$11.debug(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let debug6$5 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable$11) {
      logger$11.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
  };
  let banner$5 = a => {
    if (enable$11) {
      logger$11.banner(a);
      return;
    }
  };
  let banner1$5 = (a, a2) => {
    if (enable$11) {
      logger$11.banner(a, a2);
      return;
    }
  };
  let banner2$5 = (a, a2, a3) => {
    if (enable$11) {
      logger$11.banner(a, a2, a3);
      return;
    }
  };
  let banner3$5 = (a, a2, a3, a4) => {
    if (enable$11) {
      logger$11.banner(a, a2, a3, a4);
      return;
    }
  };
  let banner4$5 = (a, a2, a3, a4, a5) => {
    if (enable$11) {
      logger$11.banner(a, a2, a3, a4, a5);
      return;
    }
  };
  let banner5$5 = (a, a2, a3, a4, a5, a6) => {
    if (enable$11) {
      logger$11.banner(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let log$5 = a => {
    if (enable$11) {
      logger$11.log(a);
      return;
    }
  };
  let log1$5 = (a, a2) => {
    if (enable$11) {
      logger$11.log(a, a2);
      return;
    }
  };
  let log2$5 = (a, a2, a3) => {
    if (!enable$11) {
      return;
    }
    let args = toArgs$5(label$5, [a, a2, a3]);
    logger$11.log.apply(null, args);
  };
  let log3$5 = (a, a2, a3, a4) => {
    if (enable$11) {
      logger$11.log(a, a2, a3, a4);
      return;
    }
  };
  let log4$5 = (a, a2, a3, a4, a5) => {
    if (enable$11) {
      logger$11.log(a, a2, a3, a4, a5);
      return;
    }
  };
  let log5$5 = (a, a2, a3, a4, a5, a6) => {
    if (enable$11) {
      logger$11.log(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let groupCollapsed$11 = a => {
    if (enable$11) {
      if (debuggerGroupCollapsed$5) {
        logger$11.groupCollapsed(a);
      } else {
        logger$11.group(a);
      }
      return;
    }
  };
  let groupCollapsed1$5 = (a, a1) => {
    if (enable$11) {
      if (debuggerGroupCollapsed$5) {
        logger$11.groupCollapsed(a, a1);
      } else {
        logger$11.group(a, a1);
      }
      return;
    }
  };
  let groupCollapsed2$5 = (a, a1, a2) => {
    if (enable$11) {
      if (debuggerGroupCollapsed$5) {
        logger$11.groupCollapsed(a, a1, a2);
      } else {
        logger$11.group(a, a1, a2);
      }
      return;
    }
  };
  let groupCollapsed3$5 = (a, a1, a2, a3) => {
    if (enable$11) {
      if (debuggerGroupCollapsed$5) {
        logger$11.groupCollapsed(a, a1, a2, a3);
      } else {
        logger$11.group(a, a1, a2, a3);
      }
      return;
    }
  };
  let groupCollapsed4$5 = (a, a1, a2, a3, a4) => {
    if (enable$11) {
      if (debuggerGroupCollapsed$5) {
        logger$11.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger$11.group(a, a1, a2, a3, a4);
      }
      return;
    }
  };
  let groupCollapsed5$5 = (a, a1, a2, a3, a4, a5) => {
    if (enable$11) {
      if (debuggerGroupCollapsed$5) {
        logger$11.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger$11.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
  };
  let groupCollapsed6$5 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable$11) {
      if (debuggerGroupCollapsed$5) {
        logger$11.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger$11.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
  };
  let groupCollapsed7$5 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable$11) {
      if (debuggerGroupCollapsed$5) {
        logger$11.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger$11.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
  };
  let groupEnd$5 = () => {
    if (enable$11) {
      logger$11.groupEnd();
      return;
    }
  };
  let groupEnd1$5 = a => {
    if (enable$11) {
      logger$11.groupEnd(a);
      return;
    }
  };
  let groupEnd2$5 = (a, a1) => {
    if (enable$11) {
      logger$11.groupEnd(a, a1);
      return;
    }
  };
  let groupEnd3$5 = (a, a1, a2) => {
    if (enable$11) {
      logger$11.groupEnd(a, a1, a2);
      return;
    }
  };
  let Logger$5 = {
    toArgs: toArgs$5,
    logger: logger$11,
    enable: enable$11,
    debuggerGroupCollapsed: debuggerGroupCollapsed$5,
    debug: debug$11,
    debug1: debug1$5,
    debug2: debug2$5,
    debug3: debug3$5,
    debug4: debug4$5,
    debug5: debug5$5,
    debug6: debug6$5,
    banner: banner$5,
    banner1: banner1$5,
    banner2: banner2$5,
    banner3: banner3$5,
    banner4: banner4$5,
    banner5: banner5$5,
    log: log$5,
    log1: log1$5,
    log2: log2$5,
    log3: log3$5,
    log4: log4$5,
    log5: log5$5,
    groupCollapsed: groupCollapsed$11,
    groupCollapsed1: groupCollapsed1$5,
    groupCollapsed2: groupCollapsed2$5,
    groupCollapsed3: groupCollapsed3$5,
    groupCollapsed4: groupCollapsed4$5,
    groupCollapsed5: groupCollapsed5$5,
    groupCollapsed6: groupCollapsed6$5,
    groupCollapsed7: groupCollapsed7$5,
    groupEnd: groupEnd$5,
    groupEnd1: groupEnd1$5,
    groupEnd2: groupEnd2$5,
    groupEnd3: groupEnd3$5
  };
  let Runtime_env$4 = env$1;
  let Runtime_CompressOptions$4 = CompressOptions;
  let CompressOptions$9 = Runtime_CompressOptions$4;
  let compressOptions_inlineCompression$5 = CompressOptions$9.inlineCompression;
  let compressOptions_forceGridAlignment$5 = CompressOptions$9.forceGridAlignment;
  let compressOptions_westernCharacterFirst$5 = CompressOptions$9.westernCharacterFirst;
  let compressOptions_forceSpaceBetweenCJKAndWestern$5 = CompressOptions$9.forceSpaceBetweenCJKAndWestern;
  let compressOptions_fixCJKLeftQuote$5 = CompressOptions$9.fixCJKLeftQuote;
  let compressOptions$5 = {
    inlineCompression: compressOptions_inlineCompression$5,
    forceGridAlignment: compressOptions_forceGridAlignment$5,
    westernCharacterFirst: compressOptions_westernCharacterFirst$5,
    forceSpaceBetweenCJKAndWestern: compressOptions_forceSpaceBetweenCJKAndWestern$5,
    fixCJKLeftQuote: compressOptions_fixCJKLeftQuote$5
  };
  let initlize$6 = (env, options, firstChar, hasLeftQuote, paddingLeft) => {
    env._firstChar = firstChar;
    env._hasLeftQuote = hasLeftQuote;
    env._paddingLeft = paddingLeft;
    env._gridSize = options.gridSize;
    env._forceSpace = CompressOptions$9.forceSpaceBetweenCJKAndWestern ? env._gridSize / 4.0 : 0.0;
    env._spaceWidth = env._gridSize * 0.35;
    env._xInterval = options.xInterval;
    env._yInterval = options.yInterval;
    env._maxCol = options.column;
    env._lastCol = env._maxCol - 1.0;
    env._maxRow = options.row;
    let value = options.wrapWidth;
    env._maxWidth = value !== undefined ? value : env._maxCol * env._gridSize;
    env._letterSpacing = options.letterSpacing;
    env._gridWidth = env._gridSize + env._xInterval;
    return env;
  };
  let IEnv_env$4 = Runtime_env$4;
  let IEnv$4 = {
    CompressOptions: CompressOptions$9,
    env: IEnv_env$4,
    compressOptions: compressOptions$5,
    initlize: initlize$6
  };
  let CompressOptions$10 = CompressOptions;
  let Config$6 = {
    $$debugger: CompressOptions$10.$$debugger
  };
  let $$return$6 = (param, t) => t;
  let switchTo$6 = (param, t) => t;
  let tapIf$6 = (context, exp, fn) => {
    if (exp(context)) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let tapIfConst$6 = (context, exp, fn) => {
    if (exp) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let boolean$6 = (context, exp) => [exp(context) ? "True" : "False", context];
  let onTrue$6 = (context, fn) => {
    if (context[0] === "False") {
      return context;
    } else {
      return ["True", fn(context[1])];
    }
  };
  let onFalse$6 = (context, fn) => {
    if (context[0] === "False") {
      return ["False", fn(context[1])];
    } else {
      return context;
    }
  };
  let unwrap$7 = context => context[1];
  let computeIfConst$6 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIf$6 = (context, exp, fn) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIfElse$6 = (context, exp, fn, else$p) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return else$p(context);
    }
  };
  let pipeIfConst$6 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let switchIf$6 = (context, exp, else$p, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let check$6 = (context, if$p, else$p, fn) => {
    if (if$p(context)) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let pipe$6 = (context, fn) => fn(context);
  let computeIfElse$6 = (context, exp, fn, fn2) => {
    if (exp) {
      return fn(context);
    } else {
      return fn2(context);
    }
  };
  let action$6 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionU$6 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionWith$6 = (context, withCtx, fn) => {
    fn(withCtx);
    return context;
  };
  let tap$6 = (context, fn) => {
    fn();
    return context;
  };
  let debugTap$6 = (context, fn) => {
    if (Config$6.$$debugger) {
      fn();
      return context;
    } else {
      return context;
    }
  };
  let debug$12 = (context, fn) => {
    if (Config$6.$$debugger) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let ignore$6 = (context, withCtx) => context;
  let call$6 = (context, arg) => context(arg);
  let rev$6 = (b, fn, a) => fn(a, b);
  let Pipe$6 = {
    $$return: $$return$6,
    switchTo: switchTo$6,
    tapIf: tapIf$6,
    tapIfConst: tapIfConst$6,
    boolean: boolean$6,
    onTrue: onTrue$6,
    onFalse: onFalse$6,
    unwrap: unwrap$7,
    computeIfConst: computeIfConst$6,
    pipeIf: pipeIf$6,
    pipeIfElse: pipeIfElse$6,
    pipeIfConst: pipeIfConst$6,
    switchIf: switchIf$6,
    check: check$6,
    pipe: pipe$6,
    computeIfElse: computeIfElse$6,
    action: action$6,
    actionU: actionU$6,
    actionWith: actionWith$6,
    tap: tap$6,
    debugTap: debugTap$6,
    debug: debug$12,
    ignore: ignore$6,
    call: call$6,
    rev: rev$6
  };
  let env$7 = env$1;
  let context$4 = Store.context;
  let _buf$3 = {
    seqs: [],
    areaWidth: 0.0,
    areaHeight: 0.0,
    width: 0.0,
    height: 0.0,
    lastLineIndex: 0,
    lineHeights: context$4._lineHeights,
    status: context$4._status
  };
  let setContentWidth$3 = (context, width) => {
    context._contentWidth = width;
    return context;
  };
  let getContentWidth$3 = context => context._contentWidth;
  let setCurrentX$3 = (context, x) => {
    context._currentX = x;
    return context;
  };
  let getCurrentX$3 = context => context._currentX;
  let setCurrentY$3 = (context, y) => {
    context._currentY = y;
    return context;
  };
  let getCurrentY$3 = context => context._currentY;
  let setRowHeight$3 = (context, rowHeight) => {
    context._currentRowHeight = rowHeight;
    return context;
  };
  let getRowHeight$3 = context => context._currentRowHeight;
  let storeLineHeights$3 = context => {
    context._lineHeights.push(context._currentRowHeight);
    return context;
  };
  let setCurrentCol$3 = (context, col) => {
    context._currentCol = col;
    return context;
  };
  let setCurrentColU$3 = (context, col) => {
    context._currentCol = col;
  };
  let getCurrentCol$3 = context => context._currentCol;
  let setCurrentRow$3 = (context, row) => {
    context._currentRow = row;
    return context;
  };
  let getCurrentRow$3 = context => context._currentRow;
  let finish$3 = param => {};
  let syncContentWidth$3 = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    return context;
  };
  let growRowHeight$3 = (context, elementHeight) => {
    if (elementHeight > context._currentRowHeight) {
      context._currentRowHeight = elementHeight;
    }
    return context;
  };
  let resetCurrent$3 = context => {
    context._currentX = 0.0;
    let context$1 = context;
    context$1._currentY = 0.0;
    let context$2 = context$1;
    context$2._currentCol = 0.0;
    let context$3 = context$2;
    context$3._currentRow = 1;
    return context$3;
  };
  let rollbackX$3 = (context, x) => {
    debug3$5("rollbackX: %o - %o => %o", context._currentX, x, context._currentX - x);
    context._currentX = context._currentX - x;
    return context;
  };
  let moveCursorX$3 = (context, x) => {
    debug3$5("[cursor] moveX: %o + %o => %o", context._currentX, x, context._currentX + x);
    context._currentX = context._currentX + x;
    return context;
  };
  let consumeXIn$3 = (x, context) => {
    debug3$5("[cursor] moveX: %o + %o => %o", context._currentX, x, context._currentX + x);
    context._currentX = context._currentX + x;
    return x;
  };
  let growCurrentY$3 = (context, y) => {
    context._currentY = context._currentY + y;
    return context;
  };
  let rollbackCol$3 = (context, col) => {
    context._currentCol = context._currentCol - col;
    return context;
  };
  let consumeCol$3 = (context, col) => {
    context._currentCol = context._currentCol + col;
    return context;
  };
  let consumeColU$3 = (context, col) => {
    context._currentCol = context._currentCol + col;
  };
  let growCurrentRow$3 = context => {
    context._currentY = context._currentY + (context._currentRowHeight + env$7._yInterval);
    let context$1 = context;
    context$1._currentRow = context._currentRow + 1 | 0;
    return context$1;
  };
  let setLastCharWidth$3 = (context, charWidth) => {
    context._lastCharWidth = charWidth;
    return context;
  };
  let nextIsWord$3 = context => {
    let kind = context._cursor < context._source.length ? out(context._source[context._cursor + 1 | 0], getElementType) : out(context._END, getElementType);
    switch (kind) {
      case 3:
      case 12:
        return true;
      default:
        return false;
    }
  };
  let lastIsNotPunctuation$3 = context => {
    context._lastIsPunctuation = false;
    context._lastPunctuations = 0.0;
    return context;
  };
  let consumePunctuation$3 = context => {
    context._lastIsPunctuation = true;
    context._lastPunctuations = context._lastPunctuations + 1.0;
    return context;
  };
  let needForceWrap$3 = context => {
    context._isNeedForceWrap = true;
    return context;
  };
  let resetNeedForceWrap$3 = context => {
    context._isNeedForceWrap = false;
    return context;
  };
  let recordLastElement$3 = (context, char) => {
    context.rows[context._currentRow] = char;
  };
  let getRowSize$3 = context => context.rows.length;
  let prepare$3 = (context, source, options) => {
    let firstChar = source[0];
    let hasLeftQuote = out(firstChar, getElementType) === 10;
    let paddingLeft = hasLeftQuote ? out(firstChar, getWidth) + env$7._xInterval : 0.0;
    let env$8 = initlize$6(env$7, options, firstChar, hasLeftQuote, paddingLeft);
    context._currentX = 0.0;
    let context$1 = context;
    context$1._currentY = 0.0;
    let context$2 = context$1;
    context$2._currentCol = 0.0;
    let context$3 = context$2;
    context$3._currentRow = 1;
    let context$4 = context$3;
    context$4._lastIsPunctuation = false;
    context$4._lastPunctuations = 0.0;
    let context$5 = context$4;
    context$5._currentRowHeight = env$8._gridSize;
    let context$6 = context$5;
    context$6._lastCharWidth = 0.0;
    let context$7 = context$6;
    context$7._isNeedForceWrap = false;
    let context$8 = context$7;
    action$6((context$8._contentWidth = 0.0, context$8), context => {
      context._cursor = 0;
      context._lastIsWesternChar = CompressOptions$10.westernCharacterFirst;
      context._source = source;
      context.rows = [firstChar, firstChar];
    });
    debug2$5("prepare", context, env$8);
  };
  let lineWrap$4 = context => {
    log$5("lineWrap");
    /* lineWrap */
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let context$1 = context;
    context$1._currentCol = env$7._hasLeftQuote ? 1.0 : 0.0;
    let context$2 = context$1;
    context$2._currentX = env$7._paddingLeft;
    let context$3 = context$2;
    context$3._currentY = context$3._currentY + (context$3._currentRowHeight + env$7._yInterval);
    let context$4 = context$3;
    context$4._currentRow = context$3._currentRow + 1 | 0;
    let context$5 = context$4;
    context$5._lineHeights.push(context$5._currentRowHeight);
    let context$6 = context$5;
    context$6._currentRowHeight = env$7._gridSize;
    let context$7 = context$6;
    context$7._lastIsPunctuation = false;
    context$7._lastPunctuations = 0.0;
    let context$8 = context$7;
    context$8._lastCharWidth = 0.0;
    let context$9 = context$8;
    context$9._isNeedForceWrap = false;
  };
  let incrementFlush$3 = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let lastLineIndex = context._currentRow;
    let overflowY = lastLineIndex > (context.rows.length - 1 | 0);
    let contentHeight = overflowY ? context._currentY : context._currentY + (context._lineHeights.push(context._currentRowHeight), context)._currentRowHeight;
    _buf$3.seqs = context.rows;
    _buf$3.areaWidth = context._contentWidth;
    _buf$3.areaHeight = contentHeight;
    _buf$3.width = env$7._maxWidth;
    _buf$3.height = contentHeight;
    _buf$3.lastLineIndex = overflowY ? lastLineIndex - 1 | 0 : lastLineIndex;
    _buf$3.lineHeights = context._lineHeights;
    _buf$3.status = "flushing";
    return _buf$3;
  };
  let flush$4 = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let lastLineIndex = context._currentRow;
    let overflowY = lastLineIndex > (context.rows.length - 1 | 0);
    let contentHeight = overflowY ? context._currentY : context._currentY + (context._lineHeights.push(context._currentRowHeight), context)._currentRowHeight;
    _buf$3.seqs = context.rows;
    _buf$3.areaWidth = context._contentWidth;
    _buf$3.areaHeight = contentHeight;
    _buf$3.width = env$7._maxWidth;
    _buf$3.height = contentHeight;
    _buf$3.lastLineIndex = overflowY ? lastLineIndex - 1 | 0 : lastLineIndex;
    _buf$3.lineHeights = context._lineHeights;
    _buf$3.status = "closed";
    context._lineHeights = [];
    debug1$5("contentHeight: %o", contentHeight);
    return _buf$3;
  };
  let output$4 = param => ({
    seqs: _buf$3.seqs,
    areaWidth: _buf$3.areaWidth,
    areaHeight: _buf$3.areaHeight,
    width: _buf$3.width,
    height: _buf$3.height,
    lastLineIndex: _buf$3.lastLineIndex,
    lineHeights: _buf$3.lineHeights,
    status: "closed"
  });
  let Context$3 = {
    LoggerOption: LoggerOption$5,
    Logger: Logger$5,
    IEnv: IEnv$4,
    ECS: Runtime$3,
    CompressOptions: CompressOptions$10,
    Pipe: Pipe$6,
    env: env$7,
    context: context$4,
    _buf: _buf$3,
    setContentWidth: setContentWidth$3,
    getContentWidth: getContentWidth$3,
    setCurrentX: setCurrentX$3,
    getCurrentX: getCurrentX$3,
    setCurrentY: setCurrentY$3,
    getCurrentY: getCurrentY$3,
    setRowHeight: setRowHeight$3,
    getRowHeight: getRowHeight$3,
    storeLineHeights: storeLineHeights$3,
    setCurrentCol: setCurrentCol$3,
    setCurrentColU: setCurrentColU$3,
    getCurrentCol: getCurrentCol$3,
    setCurrentRow: setCurrentRow$3,
    getCurrentRow: getCurrentRow$3,
    finish: finish$3,
    syncContentWidth: syncContentWidth$3,
    growRowHeight: growRowHeight$3,
    resetCurrent: resetCurrent$3,
    rollbackX: rollbackX$3,
    moveCursorX: moveCursorX$3,
    consumeXIn: consumeXIn$3,
    growCurrentY: growCurrentY$3,
    rollbackCol: rollbackCol$3,
    consumeCol: consumeCol$3,
    consumeColU: consumeColU$3,
    growCurrentRow: growCurrentRow$3,
    setLastCharWidth: setLastCharWidth$3,
    nextIsWord: nextIsWord$3,
    lastIsNotPunctuation: lastIsNotPunctuation$3,
    consumePunctuation: consumePunctuation$3,
    needForceWrap: needForceWrap$3,
    resetNeedForceWrap: resetNeedForceWrap$3,
    recordLastElement: recordLastElement$3,
    getRowSize: getRowSize$3,
    prepare: prepare$3,
    lineWrap: lineWrap$4,
    incrementFlush: incrementFlush$3,
    flush: flush$4,
    output: output$4
  };
  let logger$12 = console;
  let $$debugger$6 = CompressOptions.$$debugger;
  let groupCollapsed$12 = CompressOptions.groupCollapsed;
  let enable$12 = process.env.NODE_ENV !== "production";
  let label$6 = "compact";
  let LoggerOption$6 = {
    logger: logger$12,
    $$debugger: $$debugger$6,
    groupCollapsed: groupCollapsed$12,
    enable: enable$12,
    label: label$6
  };
  let toArgs$6 = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return ["%c%s%c " + match, "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args.slice(1));
    }
    return ["%c%s%c %s", "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args);
  };
  let logger$13 = logger$12;
  let enable$13 = enable$12;
  let debuggerGroupCollapsed$6 = groupCollapsed$12;
  let debug$13 = a => {
    if (enable$13) {
      logger$13.debug(a);
      return;
    }
  };
  let debug1$6 = (a, a2) => {
    if (enable$13) {
      logger$13.debug(a, a2);
      return;
    }
  };
  let debug2$6 = (a, a2, a3) => {
    if (enable$13) {
      logger$13.debug(a, a2, a3);
      return;
    }
  };
  let debug3$6 = (a, a2, a3, a4) => {
    if (enable$13) {
      logger$13.debug(a, a2, a3, a4);
      return;
    }
  };
  let debug4$6 = (a, a2, a3, a4, a5) => {
    if (enable$13) {
      logger$13.debug(a, a2, a3, a4, a5);
      return;
    }
  };
  let debug5$6 = (a, a2, a3, a4, a5, a6) => {
    if (enable$13) {
      logger$13.debug(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let debug6$6 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable$13) {
      logger$13.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
  };
  let banner$6 = a => {
    if (enable$13) {
      logger$13.banner(a);
      return;
    }
  };
  let banner1$6 = (a, a2) => {
    if (enable$13) {
      logger$13.banner(a, a2);
      return;
    }
  };
  let banner2$6 = (a, a2, a3) => {
    if (enable$13) {
      logger$13.banner(a, a2, a3);
      return;
    }
  };
  let banner3$6 = (a, a2, a3, a4) => {
    if (enable$13) {
      logger$13.banner(a, a2, a3, a4);
      return;
    }
  };
  let banner4$6 = (a, a2, a3, a4, a5) => {
    if (enable$13) {
      logger$13.banner(a, a2, a3, a4, a5);
      return;
    }
  };
  let banner5$6 = (a, a2, a3, a4, a5, a6) => {
    if (enable$13) {
      logger$13.banner(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let log$6 = a => {
    if (enable$13) {
      logger$13.log(a);
      return;
    }
  };
  let log1$6 = (a, a2) => {
    if (enable$13) {
      logger$13.log(a, a2);
      return;
    }
  };
  let log2$6 = (a, a2, a3) => {
    if (!enable$13) {
      return;
    }
    let args = toArgs$6(label$6, [a, a2, a3]);
    logger$13.log.apply(null, args);
  };
  let log3$6 = (a, a2, a3, a4) => {
    if (enable$13) {
      logger$13.log(a, a2, a3, a4);
      return;
    }
  };
  let log4$6 = (a, a2, a3, a4, a5) => {
    if (enable$13) {
      logger$13.log(a, a2, a3, a4, a5);
      return;
    }
  };
  let log5$6 = (a, a2, a3, a4, a5, a6) => {
    if (enable$13) {
      logger$13.log(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let groupCollapsed$13 = a => {
    if (enable$13) {
      if (debuggerGroupCollapsed$6) {
        logger$13.groupCollapsed(a);
      } else {
        logger$13.group(a);
      }
      return;
    }
  };
  let groupCollapsed1$6 = (a, a1) => {
    if (enable$13) {
      if (debuggerGroupCollapsed$6) {
        logger$13.groupCollapsed(a, a1);
      } else {
        logger$13.group(a, a1);
      }
      return;
    }
  };
  let groupCollapsed2$6 = (a, a1, a2) => {
    if (enable$13) {
      if (debuggerGroupCollapsed$6) {
        logger$13.groupCollapsed(a, a1, a2);
      } else {
        logger$13.group(a, a1, a2);
      }
      return;
    }
  };
  let groupCollapsed3$6 = (a, a1, a2, a3) => {
    if (enable$13) {
      if (debuggerGroupCollapsed$6) {
        logger$13.groupCollapsed(a, a1, a2, a3);
      } else {
        logger$13.group(a, a1, a2, a3);
      }
      return;
    }
  };
  let groupCollapsed4$6 = (a, a1, a2, a3, a4) => {
    if (enable$13) {
      if (debuggerGroupCollapsed$6) {
        logger$13.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger$13.group(a, a1, a2, a3, a4);
      }
      return;
    }
  };
  let groupCollapsed5$6 = (a, a1, a2, a3, a4, a5) => {
    if (enable$13) {
      if (debuggerGroupCollapsed$6) {
        logger$13.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger$13.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
  };
  let groupCollapsed6$6 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable$13) {
      if (debuggerGroupCollapsed$6) {
        logger$13.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger$13.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
  };
  let groupCollapsed7$6 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable$13) {
      if (debuggerGroupCollapsed$6) {
        logger$13.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger$13.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
  };
  let groupEnd$6 = () => {
    if (enable$13) {
      logger$13.groupEnd();
      return;
    }
  };
  let groupEnd1$6 = a => {
    if (enable$13) {
      logger$13.groupEnd(a);
      return;
    }
  };
  let groupEnd2$6 = (a, a1) => {
    if (enable$13) {
      logger$13.groupEnd(a, a1);
      return;
    }
  };
  let groupEnd3$6 = (a, a1, a2) => {
    if (enable$13) {
      logger$13.groupEnd(a, a1, a2);
      return;
    }
  };
  let Logger$6 = {
    toArgs: toArgs$6,
    logger: logger$13,
    enable: enable$13,
    debuggerGroupCollapsed: debuggerGroupCollapsed$6,
    debug: debug$13,
    debug1: debug1$6,
    debug2: debug2$6,
    debug3: debug3$6,
    debug4: debug4$6,
    debug5: debug5$6,
    debug6: debug6$6,
    banner: banner$6,
    banner1: banner1$6,
    banner2: banner2$6,
    banner3: banner3$6,
    banner4: banner4$6,
    banner5: banner5$6,
    log: log$6,
    log1: log1$6,
    log2: log2$6,
    log3: log3$6,
    log4: log4$6,
    log5: log5$6,
    groupCollapsed: groupCollapsed$13,
    groupCollapsed1: groupCollapsed1$6,
    groupCollapsed2: groupCollapsed2$6,
    groupCollapsed3: groupCollapsed3$6,
    groupCollapsed4: groupCollapsed4$6,
    groupCollapsed5: groupCollapsed5$6,
    groupCollapsed6: groupCollapsed6$6,
    groupCollapsed7: groupCollapsed7$6,
    groupEnd: groupEnd$6,
    groupEnd1: groupEnd1$6,
    groupEnd2: groupEnd2$6,
    groupEnd3: groupEnd3$6
  };
  let Config$7 = {
    $$debugger: CompressOptions.$$debugger
  };
  let $$return$7 = (param, t) => t;
  let switchTo$7 = (param, t) => t;
  let tapIf$7 = (context, exp, fn) => {
    if (exp(context)) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let tapIfConst$7 = (context, exp, fn) => {
    if (exp) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let boolean$7 = (context, exp) => [exp(context) ? "True" : "False", context];
  let onTrue$7 = (context, fn) => {
    if (context[0] === "False") {
      return context;
    } else {
      return ["True", fn(context[1])];
    }
  };
  let onFalse$7 = (context, fn) => {
    if (context[0] === "False") {
      return ["False", fn(context[1])];
    } else {
      return context;
    }
  };
  let unwrap$8 = context => context[1];
  let computeIfConst$7 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIf$7 = (context, exp, fn) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIfElse$7 = (context, exp, fn, else$p) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return else$p(context);
    }
  };
  let pipeIfConst$7 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let switchIf$7 = (context, exp, else$p, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let check$7 = (context, if$p, else$p, fn) => {
    if (if$p(context)) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let pipe$7 = (context, fn) => fn(context);
  let computeIfElse$7 = (context, exp, fn, fn2) => {
    if (exp) {
      return fn(context);
    } else {
      return fn2(context);
    }
  };
  let action$7 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionU$7 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionWith$7 = (context, withCtx, fn) => {
    fn(withCtx);
    return context;
  };
  let tap$7 = (context, fn) => {
    fn();
    return context;
  };
  let debugTap$7 = (context, fn) => {
    if (Config$7.$$debugger) {
      fn();
      return context;
    } else {
      return context;
    }
  };
  let debug$14 = (context, fn) => {
    if (Config$7.$$debugger) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let ignore$7 = (context, withCtx) => context;
  let call$7 = (context, arg) => context(arg);
  let rev$7 = (b, fn, a) => fn(a, b);
  let Pipe$7 = {
    $$return: $$return$7,
    switchTo: switchTo$7,
    tapIf: tapIf$7,
    tapIfConst: tapIfConst$7,
    boolean: boolean$7,
    onTrue: onTrue$7,
    onFalse: onFalse$7,
    unwrap: unwrap$8,
    computeIfConst: computeIfConst$7,
    pipeIf: pipeIf$7,
    pipeIfElse: pipeIfElse$7,
    pipeIfConst: pipeIfConst$7,
    switchIf: switchIf$7,
    check: check$7,
    pipe: pipe$7,
    computeIfElse: computeIfElse$7,
    action: action$7,
    actionU: actionU$7,
    actionWith: actionWith$7,
    tap: tap$7,
    debugTap: debugTap$7,
    debug: debug$14,
    ignore: ignore$7,
    call: call$7,
    rev: rev$7
  };
  let env$8 = env$1;
  let processOffset$1 = (context, start, end, offset, isMultLine) => {
    let source = context._source;
    if (!(!isMultLine && offset > 0.0)) {
      return;
    }
    debug1$6("currentX_offset", offset);
    for (let i = start; i <= end; ++i) {
      addX(unwrap$1(source[i]), offset);
    }
  };
  let pipeOffset$1 = (context, isMultLine, start, end, currentX, currentX_before, forceSpace, allow) => {
    let offset = allow ? (currentX - currentX_before + forceSpace) / 2.0 - forceSpace : (currentX - currentX_before) / 2;
    let source = context._source;
    if (!(!isMultLine && offset > 0.0)) {
      return;
    }
    debug1$6("currentX_offset", offset);
    for (let i = start; i <= end; ++i) {
      addX(unwrap$1(source[i]), offset);
    }
  };
  let getOffsetX$1 = (context, currentX_before, isMultLine, startElementIndex, endElementIndex, allowPangu) => {
    let forceSpace = env$8._forceSpace;
    let gridWidth = env$8._gridWidth;
    let currentX = tap$7((action$7(Math.ceil(currentX_before / gridWidth), x => setCurrentX$3(context, x)), getCurrentCol$3(context) * gridWidth), () => debug$13("forceGridAlignment"));
    if (allowPangu) {
      if (currentX >= currentX_before + forceSpace) {
        return currentX;
      }
      let t = currentX + gridWidth;
      return action$7((consumeCol$3(context, 1.0), t), __x => {
        let offset = (__x - currentX_before + forceSpace) / 2.0 - forceSpace;
        let source = context._source;
        if (!(!isMultLine && offset > 0.0)) {
          return;
        }
        debug1$6("currentX_offset", offset);
        for (let i = startElementIndex; i <= endElementIndex; ++i) {
          addX(unwrap$1(source[i]), offset);
        }
      });
    }
    if (currentX >= currentX_before) {
      return currentX;
    }
    let t$1 = currentX + gridWidth;
    return action$7((consumeCol$3(context, 1.0), t$1), __x => {
      let offset = (__x - currentX_before) / 2;
      let source = context._source;
      if (!(!isMultLine && offset > 0.0)) {
        return;
      }
      debug1$6("currentX_offset", offset);
      for (let i = startElementIndex; i <= endElementIndex; ++i) {
        addX(unwrap$1(source[i]), offset);
      }
    });
  };
  let consumeOffsetX$1 = context => {
    let offsetX = context._buf_offsetX;
    if (offsetX !== 0.0) {
      consumeXIn$3(pipeIfConst$7(action$7(offsetX, __x => debug1$6("consumeOffsetX", __x)), offsetX > env$8._gridSize, offsetX => {
        rollbackCol$3(context, 1.0);
        return offsetX - env$8._gridSize;
      }), context);
      return;
    }
  };
  let buffOffsetX$1 = (context, fontSize, overflow) => {
    let gridSize = env$8._gridSize;
    let gridWidth = env$8._gridWidth;
    if (fontSize === gridSize) {
      return;
    }
    debug$13("gridAlignment");
    let offset_x = (fontSize - gridSize) / 2.0;
    context._buf_offsetX = pipe$7(consumeCol$3(context, offset_x > 0.0 ? Math.ceil(offset_x * 2.0 / gridWidth) : 0.0), context => {
      let offset_x$1 = ((Math.ceil(offset_x * 2.0 / gridWidth) + 1.0) * gridWidth - fontSize) / 2.0;
      let t = overflow ? (lineWrap$4(context), offset_x$1) * 2.0 : offset_x$1;
      moveCursorX$3(context, offset_x$1);
      return t;
    });
  };
  let ForceGridAlignment_getOffsetX$1 = getOffsetX$1;
  let ForceGridAlignment_consumeOffsetX$1 = consumeOffsetX$1;
  let ForceGridAlignment_buffOffsetX$1 = buffOffsetX$1;
  let ForceGridAlignment$1 = {
    getOffsetX: ForceGridAlignment_getOffsetX$1,
    consumeOffsetX: ForceGridAlignment_consumeOffsetX$1,
    buffOffsetX: ForceGridAlignment_buffOffsetX$1
  };
  let Runtime$4 = {
    env: env$1,
    create: create,
    getCharacter: getCharacter,
    setCharacter: setCharacter,
    getFont: getFont,
    setFont: setFont,
    getCharCode: getCharCode,
    getElementType: getElementType,
    setElementType: setElementType,
    getWidth: getWidth,
    getHeight: getHeight,
    getX: getX,
    getY: getY,
    getFontSize: getFontSize,
    setW: setW,
    setH: setH,
    setX: setX,
    setY: setY,
    setFontSize: setFontSize,
    addX: addX,
    setPosition: setPosition,
    setSize: setSize,
    unwrap: unwrap$1,
    map: map,
    out: out,
    input: input,
    cursor: cursor,
    output: output,
    grow: grow,
    initlize: initlize$1,
    CompressOptions: CompressOptions,
    Store: Store
  };
  let CompressOptions$11 = CompressOptions;
  let logger$14 = console;
  let $$debugger$7 = CompressOptions.$$debugger;
  let groupCollapsed$14 = CompressOptions.groupCollapsed;
  let enable$14 = process.env.NODE_ENV !== "production";
  let label$7 = "main";
  let LoggerOption$7 = {
    logger: logger$14,
    $$debugger: $$debugger$7,
    groupCollapsed: groupCollapsed$14,
    enable: enable$14,
    label: label$7
  };
  let toArgs$7 = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return ["%c%s%c " + match, "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args.slice(1));
    }
    return ["%c%s%c %s", "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args);
  };
  let logger$15 = logger$14;
  let enable$15 = enable$14;
  let debuggerGroupCollapsed$7 = groupCollapsed$14;
  let debug$15 = a => {
    if (enable$15) {
      logger$15.debug(a);
      return;
    }
  };
  let debug1$7 = (a, a2) => {
    if (enable$15) {
      logger$15.debug(a, a2);
      return;
    }
  };
  let debug2$7 = (a, a2, a3) => {
    if (enable$15) {
      logger$15.debug(a, a2, a3);
      return;
    }
  };
  let debug3$7 = (a, a2, a3, a4) => {
    if (enable$15) {
      logger$15.debug(a, a2, a3, a4);
      return;
    }
  };
  let debug4$7 = (a, a2, a3, a4, a5) => {
    if (enable$15) {
      logger$15.debug(a, a2, a3, a4, a5);
      return;
    }
  };
  let debug5$7 = (a, a2, a3, a4, a5, a6) => {
    if (enable$15) {
      logger$15.debug(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let debug6$7 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable$15) {
      logger$15.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
  };
  let banner$7 = a => {
    if (enable$15) {
      logger$15.banner(a);
      return;
    }
  };
  let banner1$7 = (a, a2) => {
    if (enable$15) {
      logger$15.banner(a, a2);
      return;
    }
  };
  let banner2$7 = (a, a2, a3) => {
    if (enable$15) {
      logger$15.banner(a, a2, a3);
      return;
    }
  };
  let banner3$7 = (a, a2, a3, a4) => {
    if (enable$15) {
      logger$15.banner(a, a2, a3, a4);
      return;
    }
  };
  let banner4$7 = (a, a2, a3, a4, a5) => {
    if (enable$15) {
      logger$15.banner(a, a2, a3, a4, a5);
      return;
    }
  };
  let banner5$7 = (a, a2, a3, a4, a5, a6) => {
    if (enable$15) {
      logger$15.banner(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let log$7 = a => {
    if (enable$15) {
      logger$15.log(a);
      return;
    }
  };
  let log1$7 = (a, a2) => {
    if (enable$15) {
      logger$15.log(a, a2);
      return;
    }
  };
  let log2$7 = (a, a2, a3) => {
    if (!enable$15) {
      return;
    }
    let args = toArgs$7(label$7, [a, a2, a3]);
    logger$15.log.apply(null, args);
  };
  let log3$7 = (a, a2, a3, a4) => {
    if (enable$15) {
      logger$15.log(a, a2, a3, a4);
      return;
    }
  };
  let log4$7 = (a, a2, a3, a4, a5) => {
    if (enable$15) {
      logger$15.log(a, a2, a3, a4, a5);
      return;
    }
  };
  let log5$7 = (a, a2, a3, a4, a5, a6) => {
    if (enable$15) {
      logger$15.log(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let groupCollapsed$15 = a => {
    if (enable$15) {
      if (debuggerGroupCollapsed$7) {
        logger$15.groupCollapsed(a);
      } else {
        logger$15.group(a);
      }
      return;
    }
  };
  let groupCollapsed1$7 = (a, a1) => {
    if (enable$15) {
      if (debuggerGroupCollapsed$7) {
        logger$15.groupCollapsed(a, a1);
      } else {
        logger$15.group(a, a1);
      }
      return;
    }
  };
  let groupCollapsed2$7 = (a, a1, a2) => {
    if (enable$15) {
      if (debuggerGroupCollapsed$7) {
        logger$15.groupCollapsed(a, a1, a2);
      } else {
        logger$15.group(a, a1, a2);
      }
      return;
    }
  };
  let groupCollapsed3$7 = (a, a1, a2, a3) => {
    if (enable$15) {
      if (debuggerGroupCollapsed$7) {
        logger$15.groupCollapsed(a, a1, a2, a3);
      } else {
        logger$15.group(a, a1, a2, a3);
      }
      return;
    }
  };
  let groupCollapsed4$7 = (a, a1, a2, a3, a4) => {
    if (enable$15) {
      if (debuggerGroupCollapsed$7) {
        logger$15.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger$15.group(a, a1, a2, a3, a4);
      }
      return;
    }
  };
  let groupCollapsed5$7 = (a, a1, a2, a3, a4, a5) => {
    if (enable$15) {
      if (debuggerGroupCollapsed$7) {
        logger$15.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger$15.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
  };
  let groupCollapsed6$7 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable$15) {
      if (debuggerGroupCollapsed$7) {
        logger$15.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger$15.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
  };
  let groupCollapsed7$7 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable$15) {
      if (debuggerGroupCollapsed$7) {
        logger$15.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger$15.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
  };
  let groupEnd$7 = () => {
    if (enable$15) {
      logger$15.groupEnd();
      return;
    }
  };
  let groupEnd1$7 = a => {
    if (enable$15) {
      logger$15.groupEnd(a);
      return;
    }
  };
  let groupEnd2$7 = (a, a1) => {
    if (enable$15) {
      logger$15.groupEnd(a, a1);
      return;
    }
  };
  let groupEnd3$7 = (a, a1, a2) => {
    if (enable$15) {
      logger$15.groupEnd(a, a1, a2);
      return;
    }
  };
  let Logger$7 = {
    toArgs: toArgs$7,
    logger: logger$15,
    enable: enable$15,
    debuggerGroupCollapsed: debuggerGroupCollapsed$7,
    debug: debug$15,
    debug1: debug1$7,
    debug2: debug2$7,
    debug3: debug3$7,
    debug4: debug4$7,
    debug5: debug5$7,
    debug6: debug6$7,
    banner: banner$7,
    banner1: banner1$7,
    banner2: banner2$7,
    banner3: banner3$7,
    banner4: banner4$7,
    banner5: banner5$7,
    log: log$7,
    log1: log1$7,
    log2: log2$7,
    log3: log3$7,
    log4: log4$7,
    log5: log5$7,
    groupCollapsed: groupCollapsed$15,
    groupCollapsed1: groupCollapsed1$7,
    groupCollapsed2: groupCollapsed2$7,
    groupCollapsed3: groupCollapsed3$7,
    groupCollapsed4: groupCollapsed4$7,
    groupCollapsed5: groupCollapsed5$7,
    groupCollapsed6: groupCollapsed6$7,
    groupCollapsed7: groupCollapsed7$7,
    groupEnd: groupEnd$7,
    groupEnd1: groupEnd1$7,
    groupEnd2: groupEnd2$7,
    groupEnd3: groupEnd3$7
  };
  let Runtime_env$5 = env$1;
  let Runtime_CompressOptions$5 = CompressOptions;
  let CompressOptions$12 = Runtime_CompressOptions$5;
  let compressOptions_inlineCompression$6 = CompressOptions$12.inlineCompression;
  let compressOptions_forceGridAlignment$6 = CompressOptions$12.forceGridAlignment;
  let compressOptions_westernCharacterFirst$6 = CompressOptions$12.westernCharacterFirst;
  let compressOptions_forceSpaceBetweenCJKAndWestern$6 = CompressOptions$12.forceSpaceBetweenCJKAndWestern;
  let compressOptions_fixCJKLeftQuote$6 = CompressOptions$12.fixCJKLeftQuote;
  let compressOptions$6 = {
    inlineCompression: compressOptions_inlineCompression$6,
    forceGridAlignment: compressOptions_forceGridAlignment$6,
    westernCharacterFirst: compressOptions_westernCharacterFirst$6,
    forceSpaceBetweenCJKAndWestern: compressOptions_forceSpaceBetweenCJKAndWestern$6,
    fixCJKLeftQuote: compressOptions_fixCJKLeftQuote$6
  };
  let initlize$7 = (env, options, firstChar, hasLeftQuote, paddingLeft) => {
    env._firstChar = firstChar;
    env._hasLeftQuote = hasLeftQuote;
    env._paddingLeft = paddingLeft;
    env._gridSize = options.gridSize;
    env._forceSpace = CompressOptions$12.forceSpaceBetweenCJKAndWestern ? env._gridSize / 4.0 : 0.0;
    env._spaceWidth = env._gridSize * 0.35;
    env._xInterval = options.xInterval;
    env._yInterval = options.yInterval;
    env._maxCol = options.column;
    env._lastCol = env._maxCol - 1.0;
    env._maxRow = options.row;
    let value = options.wrapWidth;
    env._maxWidth = value !== undefined ? value : env._maxCol * env._gridSize;
    env._letterSpacing = options.letterSpacing;
    env._gridWidth = env._gridSize + env._xInterval;
    return env;
  };
  let IEnv_env$5 = Runtime_env$5;
  let IEnv$5 = {
    CompressOptions: CompressOptions$12,
    env: IEnv_env$5,
    compressOptions: compressOptions$6,
    initlize: initlize$7
  };
  let CompressOptions$13 = CompressOptions;
  let Config$8 = {
    $$debugger: CompressOptions$13.$$debugger
  };
  let $$return$8 = (param, t) => t;
  let switchTo$8 = (param, t) => t;
  let tapIf$8 = (context, exp, fn) => {
    if (exp(context)) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let tapIfConst$8 = (context, exp, fn) => {
    if (exp) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let boolean$8 = (context, exp) => [exp(context) ? "True" : "False", context];
  let onTrue$8 = (context, fn) => {
    if (context[0] === "False") {
      return context;
    } else {
      return ["True", fn(context[1])];
    }
  };
  let onFalse$8 = (context, fn) => {
    if (context[0] === "False") {
      return ["False", fn(context[1])];
    } else {
      return context;
    }
  };
  let unwrap$9 = context => context[1];
  let computeIfConst$8 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIf$8 = (context, exp, fn) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIfElse$8 = (context, exp, fn, else$p) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return else$p(context);
    }
  };
  let pipeIfConst$8 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let switchIf$8 = (context, exp, else$p, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let check$8 = (context, if$p, else$p, fn) => {
    if (if$p(context)) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let pipe$8 = (context, fn) => fn(context);
  let computeIfElse$8 = (context, exp, fn, fn2) => {
    if (exp) {
      return fn(context);
    } else {
      return fn2(context);
    }
  };
  let action$8 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionU$8 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionWith$8 = (context, withCtx, fn) => {
    fn(withCtx);
    return context;
  };
  let tap$8 = (context, fn) => {
    fn();
    return context;
  };
  let debugTap$8 = (context, fn) => {
    if (Config$8.$$debugger) {
      fn();
      return context;
    } else {
      return context;
    }
  };
  let debug$16 = (context, fn) => {
    if (Config$8.$$debugger) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let ignore$8 = (context, withCtx) => context;
  let call$8 = (context, arg) => context(arg);
  let rev$8 = (b, fn, a) => fn(a, b);
  let Pipe$8 = {
    $$return: $$return$8,
    switchTo: switchTo$8,
    tapIf: tapIf$8,
    tapIfConst: tapIfConst$8,
    boolean: boolean$8,
    onTrue: onTrue$8,
    onFalse: onFalse$8,
    unwrap: unwrap$9,
    computeIfConst: computeIfConst$8,
    pipeIf: pipeIf$8,
    pipeIfElse: pipeIfElse$8,
    pipeIfConst: pipeIfConst$8,
    switchIf: switchIf$8,
    check: check$8,
    pipe: pipe$8,
    computeIfElse: computeIfElse$8,
    action: action$8,
    actionU: actionU$8,
    actionWith: actionWith$8,
    tap: tap$8,
    debugTap: debugTap$8,
    debug: debug$16,
    ignore: ignore$8,
    call: call$8,
    rev: rev$8
  };
  let env$9 = env$1;
  let context$5 = Store.context;
  let _buf$4 = {
    seqs: [],
    areaWidth: 0.0,
    areaHeight: 0.0,
    width: 0.0,
    height: 0.0,
    lastLineIndex: 0,
    lineHeights: context$5._lineHeights,
    status: context$5._status
  };
  let setContentWidth$4 = (context, width) => {
    context._contentWidth = width;
    return context;
  };
  let getContentWidth$4 = context => context._contentWidth;
  let setCurrentX$4 = (context, x) => {
    context._currentX = x;
    return context;
  };
  let getCurrentX$4 = context => context._currentX;
  let setCurrentY$4 = (context, y) => {
    context._currentY = y;
    return context;
  };
  let getCurrentY$4 = context => context._currentY;
  let setRowHeight$4 = (context, rowHeight) => {
    context._currentRowHeight = rowHeight;
    return context;
  };
  let getRowHeight$4 = context => context._currentRowHeight;
  let storeLineHeights$4 = context => {
    context._lineHeights.push(context._currentRowHeight);
    return context;
  };
  let setCurrentCol$4 = (context, col) => {
    context._currentCol = col;
    return context;
  };
  let setCurrentColU$4 = (context, col) => {
    context._currentCol = col;
  };
  let getCurrentCol$4 = context => context._currentCol;
  let setCurrentRow$4 = (context, row) => {
    context._currentRow = row;
    return context;
  };
  let getCurrentRow$4 = context => context._currentRow;
  let finish$4 = param => {};
  let syncContentWidth$4 = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    return context;
  };
  let growRowHeight$4 = (context, elementHeight) => {
    if (elementHeight > context._currentRowHeight) {
      context._currentRowHeight = elementHeight;
    }
    return context;
  };
  let resetCurrent$4 = context => {
    context._currentX = 0.0;
    let context$1 = context;
    context$1._currentY = 0.0;
    let context$2 = context$1;
    context$2._currentCol = 0.0;
    let context$3 = context$2;
    context$3._currentRow = 1;
    return context$3;
  };
  let rollbackX$4 = (context, x) => {
    debug3$7("rollbackX: %o - %o => %o", context._currentX, x, context._currentX - x);
    context._currentX = context._currentX - x;
    return context;
  };
  let moveCursorX$4 = (context, x) => {
    debug3$7("[cursor] moveX: %o + %o => %o", context._currentX, x, context._currentX + x);
    context._currentX = context._currentX + x;
    return context;
  };
  let consumeXIn$4 = (x, context) => {
    debug3$7("[cursor] moveX: %o + %o => %o", context._currentX, x, context._currentX + x);
    context._currentX = context._currentX + x;
    return x;
  };
  let growCurrentY$4 = (context, y) => {
    context._currentY = context._currentY + y;
    return context;
  };
  let rollbackCol$4 = (context, col) => {
    context._currentCol = context._currentCol - col;
    return context;
  };
  let consumeCol$4 = (context, col) => {
    context._currentCol = context._currentCol + col;
    return context;
  };
  let consumeColU$4 = (context, col) => {
    context._currentCol = context._currentCol + col;
  };
  let growCurrentRow$4 = context => {
    context._currentY = context._currentY + (context._currentRowHeight + env$9._yInterval);
    let context$1 = context;
    context$1._currentRow = context._currentRow + 1 | 0;
    return context$1;
  };
  let setLastCharWidth$4 = (context, charWidth) => {
    context._lastCharWidth = charWidth;
    return context;
  };
  let nextIsWord$4 = context => {
    let kind = context._cursor < context._source.length ? out(context._source[context._cursor + 1 | 0], getElementType) : out(context._END, getElementType);
    switch (kind) {
      case 3:
      case 12:
        return true;
      default:
        return false;
    }
  };
  let lastIsNotPunctuation$4 = context => {
    context._lastIsPunctuation = false;
    context._lastPunctuations = 0.0;
    return context;
  };
  let consumePunctuation$4 = context => {
    context._lastIsPunctuation = true;
    context._lastPunctuations = context._lastPunctuations + 1.0;
    return context;
  };
  let needForceWrap$4 = context => {
    context._isNeedForceWrap = true;
    return context;
  };
  let resetNeedForceWrap$4 = context => {
    context._isNeedForceWrap = false;
    return context;
  };
  let recordLastElement$4 = (context, char) => {
    context.rows[context._currentRow] = char;
  };
  let getRowSize$4 = context => context.rows.length;
  let prepare$4 = (context, source, options) => {
    let firstChar = source[0];
    let hasLeftQuote = out(firstChar, getElementType) === 10;
    let paddingLeft = hasLeftQuote ? out(firstChar, getWidth) + env$9._xInterval : 0.0;
    let env$10 = initlize$7(env$9, options, firstChar, hasLeftQuote, paddingLeft);
    context._currentX = 0.0;
    let context$1 = context;
    context$1._currentY = 0.0;
    let context$2 = context$1;
    context$2._currentCol = 0.0;
    let context$3 = context$2;
    context$3._currentRow = 1;
    let context$4 = context$3;
    context$4._lastIsPunctuation = false;
    context$4._lastPunctuations = 0.0;
    let context$5 = context$4;
    context$5._currentRowHeight = env$10._gridSize;
    let context$6 = context$5;
    context$6._lastCharWidth = 0.0;
    let context$7 = context$6;
    context$7._isNeedForceWrap = false;
    let context$8 = context$7;
    action$8((context$8._contentWidth = 0.0, context$8), context => {
      context._cursor = 0;
      context._lastIsWesternChar = CompressOptions$13.westernCharacterFirst;
      context._source = source;
      context.rows = [firstChar, firstChar];
    });
    debug2$7("prepare", context, env$10);
  };
  let lineWrap$5 = context => {
    log$7("lineWrap");
    /* lineWrap */
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let context$1 = context;
    context$1._currentCol = env$9._hasLeftQuote ? 1.0 : 0.0;
    let context$2 = context$1;
    context$2._currentX = env$9._paddingLeft;
    let context$3 = context$2;
    context$3._currentY = context$3._currentY + (context$3._currentRowHeight + env$9._yInterval);
    let context$4 = context$3;
    context$4._currentRow = context$3._currentRow + 1 | 0;
    let context$5 = context$4;
    context$5._lineHeights.push(context$5._currentRowHeight);
    let context$6 = context$5;
    context$6._currentRowHeight = env$9._gridSize;
    let context$7 = context$6;
    context$7._lastIsPunctuation = false;
    context$7._lastPunctuations = 0.0;
    let context$8 = context$7;
    context$8._lastCharWidth = 0.0;
    let context$9 = context$8;
    context$9._isNeedForceWrap = false;
  };
  let incrementFlush$4 = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let lastLineIndex = context._currentRow;
    let overflowY = lastLineIndex > (context.rows.length - 1 | 0);
    let contentHeight = overflowY ? context._currentY : context._currentY + (context._lineHeights.push(context._currentRowHeight), context)._currentRowHeight;
    _buf$4.seqs = context.rows;
    _buf$4.areaWidth = context._contentWidth;
    _buf$4.areaHeight = contentHeight;
    _buf$4.width = env$9._maxWidth;
    _buf$4.height = contentHeight;
    _buf$4.lastLineIndex = overflowY ? lastLineIndex - 1 | 0 : lastLineIndex;
    _buf$4.lineHeights = context._lineHeights;
    _buf$4.status = "flushing";
    return _buf$4;
  };
  let flush$5 = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let lastLineIndex = context._currentRow;
    let overflowY = lastLineIndex > (context.rows.length - 1 | 0);
    let contentHeight = overflowY ? context._currentY : context._currentY + (context._lineHeights.push(context._currentRowHeight), context)._currentRowHeight;
    _buf$4.seqs = context.rows;
    _buf$4.areaWidth = context._contentWidth;
    _buf$4.areaHeight = contentHeight;
    _buf$4.width = env$9._maxWidth;
    _buf$4.height = contentHeight;
    _buf$4.lastLineIndex = overflowY ? lastLineIndex - 1 | 0 : lastLineIndex;
    _buf$4.lineHeights = context._lineHeights;
    _buf$4.status = "closed";
    context._lineHeights = [];
    debug1$7("contentHeight: %o", contentHeight);
    return _buf$4;
  };
  let output$5 = param => ({
    seqs: _buf$4.seqs,
    areaWidth: _buf$4.areaWidth,
    areaHeight: _buf$4.areaHeight,
    width: _buf$4.width,
    height: _buf$4.height,
    lastLineIndex: _buf$4.lastLineIndex,
    lineHeights: _buf$4.lineHeights,
    status: "closed"
  });
  let logger$16 = console;
  let groupCollapsed$16 = CompressOptions.groupCollapsed;
  let enable$16 = process.env.NODE_ENV !== "production";
  let ILogger_label = "compact";
  let toArgs$8 = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return ["%c%s%c " + match, "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args.slice(1));
    }
    return ["%c%s%c %s", "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args);
  };
  let logger$17 = logger$16;
  let enable$17 = enable$16;
  let debuggerGroupCollapsed$8 = groupCollapsed$16;
  let debug$17 = a => {
    if (enable$17) {
      logger$17.debug(a);
      return;
    }
  };
  let debug1$8 = (a, a2) => {
    if (enable$17) {
      logger$17.debug(a, a2);
      return;
    }
  };
  let debug2$8 = (a, a2, a3) => {
    if (enable$17) {
      logger$17.debug(a, a2, a3);
      return;
    }
  };
  let debug3$8 = (a, a2, a3, a4) => {
    if (enable$17) {
      logger$17.debug(a, a2, a3, a4);
      return;
    }
  };
  let debug4$8 = (a, a2, a3, a4, a5) => {
    if (enable$17) {
      logger$17.debug(a, a2, a3, a4, a5);
      return;
    }
  };
  let debug5$8 = (a, a2, a3, a4, a5, a6) => {
    if (enable$17) {
      logger$17.debug(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let debug6$8 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable$17) {
      logger$17.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
  };
  let banner$8 = a => {
    if (enable$17) {
      logger$17.banner(a);
      return;
    }
  };
  let banner1$8 = (a, a2) => {
    if (enable$17) {
      logger$17.banner(a, a2);
      return;
    }
  };
  let banner2$8 = (a, a2, a3) => {
    if (enable$17) {
      logger$17.banner(a, a2, a3);
      return;
    }
  };
  let banner3$8 = (a, a2, a3, a4) => {
    if (enable$17) {
      logger$17.banner(a, a2, a3, a4);
      return;
    }
  };
  let banner4$8 = (a, a2, a3, a4, a5) => {
    if (enable$17) {
      logger$17.banner(a, a2, a3, a4, a5);
      return;
    }
  };
  let banner5$8 = (a, a2, a3, a4, a5, a6) => {
    if (enable$17) {
      logger$17.banner(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let log$8 = a => {
    if (enable$17) {
      logger$17.log(a);
      return;
    }
  };
  let log1$8 = (a, a2) => {
    if (enable$17) {
      logger$17.log(a, a2);
      return;
    }
  };
  let log2$8 = (a, a2, a3) => {
    if (!enable$17) {
      return;
    }
    let args = toArgs$8(ILogger_label, [a, a2, a3]);
    logger$17.log.apply(null, args);
  };
  let log3$8 = (a, a2, a3, a4) => {
    if (enable$17) {
      logger$17.log(a, a2, a3, a4);
      return;
    }
  };
  let log4$8 = (a, a2, a3, a4, a5) => {
    if (enable$17) {
      logger$17.log(a, a2, a3, a4, a5);
      return;
    }
  };
  let log5$8 = (a, a2, a3, a4, a5, a6) => {
    if (enable$17) {
      logger$17.log(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let groupCollapsed$17 = a => {
    if (enable$17) {
      if (debuggerGroupCollapsed$8) {
        logger$17.groupCollapsed(a);
      } else {
        logger$17.group(a);
      }
      return;
    }
  };
  let groupCollapsed1$8 = (a, a1) => {
    if (enable$17) {
      if (debuggerGroupCollapsed$8) {
        logger$17.groupCollapsed(a, a1);
      } else {
        logger$17.group(a, a1);
      }
      return;
    }
  };
  let groupCollapsed2$8 = (a, a1, a2) => {
    if (enable$17) {
      if (debuggerGroupCollapsed$8) {
        logger$17.groupCollapsed(a, a1, a2);
      } else {
        logger$17.group(a, a1, a2);
      }
      return;
    }
  };
  let groupCollapsed3$8 = (a, a1, a2, a3) => {
    if (enable$17) {
      if (debuggerGroupCollapsed$8) {
        logger$17.groupCollapsed(a, a1, a2, a3);
      } else {
        logger$17.group(a, a1, a2, a3);
      }
      return;
    }
  };
  let groupCollapsed4$8 = (a, a1, a2, a3, a4) => {
    if (enable$17) {
      if (debuggerGroupCollapsed$8) {
        logger$17.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger$17.group(a, a1, a2, a3, a4);
      }
      return;
    }
  };
  let groupCollapsed5$8 = (a, a1, a2, a3, a4, a5) => {
    if (enable$17) {
      if (debuggerGroupCollapsed$8) {
        logger$17.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger$17.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
  };
  let groupCollapsed6$8 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable$17) {
      if (debuggerGroupCollapsed$8) {
        logger$17.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger$17.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
  };
  let groupCollapsed7$8 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable$17) {
      if (debuggerGroupCollapsed$8) {
        logger$17.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger$17.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
  };
  let groupEnd$8 = () => {
    if (enable$17) {
      logger$17.groupEnd();
      return;
    }
  };
  let groupEnd1$8 = a => {
    if (enable$17) {
      logger$17.groupEnd(a);
      return;
    }
  };
  let groupEnd2$8 = (a, a1) => {
    if (enable$17) {
      logger$17.groupEnd(a, a1);
      return;
    }
  };
  let groupEnd3$8 = (a, a1, a2) => {
    if (enable$17) {
      logger$17.groupEnd(a, a1, a2);
      return;
    }
  };
  let Config$9 = {
    $$debugger: CompressOptions.$$debugger
  };
  let $$return$9 = (param, t) => t;
  let switchTo$9 = (param, t) => t;
  let tapIf$9 = (context, exp, fn) => {
    if (exp(context)) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let tapIfConst$9 = (context, exp, fn) => {
    if (exp) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let boolean$9 = (context, exp) => [exp(context) ? "True" : "False", context];
  let onTrue$9 = (context, fn) => {
    if (context[0] === "False") {
      return context;
    } else {
      return ["True", fn(context[1])];
    }
  };
  let onFalse$9 = (context, fn) => {
    if (context[0] === "False") {
      return ["False", fn(context[1])];
    } else {
      return context;
    }
  };
  let unwrap$10 = context => context[1];
  let computeIfConst$9 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIf$9 = (context, exp, fn) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIfElse$9 = (context, exp, fn, else$p) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return else$p(context);
    }
  };
  let pipeIfConst$9 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let switchIf$9 = (context, exp, else$p, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let check$9 = (context, if$p, else$p, fn) => {
    if (if$p(context)) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let pipe$9 = (context, fn) => fn(context);
  let computeIfElse$9 = (context, exp, fn, fn2) => {
    if (exp) {
      return fn(context);
    } else {
      return fn2(context);
    }
  };
  let action$9 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionU$9 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionWith$9 = (context, withCtx, fn) => {
    fn(withCtx);
    return context;
  };
  let tap$9 = (context, fn) => {
    fn();
    return context;
  };
  let debugTap$9 = (context, fn) => {
    if (Config$9.$$debugger) {
      fn();
      return context;
    } else {
      return context;
    }
  };
  let debug$18 = (context, fn) => {
    if (Config$9.$$debugger) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let ignore$9 = (context, withCtx) => context;
  let call$9 = (context, arg) => context(arg);
  let rev$9 = (b, fn, a) => fn(a, b);
  let env$10 = env$1;
  let isCompressableChar = char => {
    if (char >= 8252) {
      if (char >= 11834) {
        if (char !== 65374) {
          return char >= 11835;
        } else {
          return false;
        }
      } else if (char !== 8263) {
        return char >= 8253;
      } else {
        return false;
      }
    } else if (char > 8212 || char < 95) {
      return char !== 8230;
    } else {
      return !(char > 8211 || char < 96);
    }
  };
  let process = (context, char, charFontSize, charWidth, currentIsPunctuation, isEOL, isValidEndChar) => {
    let match = isCompressableChar(out(char, getCharCode));
    let match$1 = match ? isEOL && isValidEndChar ? "EOLCompression" : CompressOptions$11.inlineCompression && currentIsPunctuation ? "InlineCompression" : "NoCompression" : "NoCompression";
    switch (match$1) {
      case "EOLCompression":
        groupCollapsed$17("EOLCompression");
        return tap$9(tapIf$9(consumeCol$4(moveCursorX$4(context, charFontSize / 2.0), 0.5), context => getCurrentCol$4(context) % 1.0 !== 0.5, needForceWrap$4), groupEnd$8);
      case "InlineCompression":
        let match$2 = out(char, getElementType);
        let tmp;
        let exit = 0;
        switch (match$2) {
          case 6:
          case 7:
          case 9:
          case 10:
          case 11:
            tmp = true;
            break;
          case 15:
          case 16:
            exit = 1;
            break;
          default:
            tmp = false;
        }
        if (exit === 1) {
          let match$3 = out(char, getCharCode);
          tmp = match$3 !== 34 ? match$3 !== 39 : false;
        }
        if (tmp) {
          consumePunctuation$4(context);
          groupCollapsed$17("InlineCompression");
          return tap$9(consumeCol$4(moveCursorX$4(context, charWidth - charFontSize / 2.0 + env$10._xInterval * (context._lastIsPunctuation ? 1.0 : 0.0)), charWidth / charFontSize - 0.5), groupEnd$8);
        } else {
          return consumeCol$4(moveCursorX$4(tapIfConst$9(context, context._lastIsPunctuation, lastIsNotPunctuation$4), charWidth + env$10._xInterval), charWidth / charFontSize);
        }
      case "NoCompression":
        return consumeCol$4(moveCursorX$4(tapIfConst$9(context, context._lastIsPunctuation, lastIsNotPunctuation$4), charWidth + env$10._xInterval), charWidth / charFontSize);
    }
  };
  let processUncompress = (context, elementType, currentIsPunctuation) => {
    if (CompressOptions$11.inlineCompression && context._lastIsPunctuation && !currentIsPunctuation) {
      groupCollapsed2$8("processUncompress", context._lastPunctuations, ElementType$Std.print(elementType));
      return finish$4(tap$9(lastIsNotPunctuation$4(consumeCol$4(moveCursorX$4(context, context._lastCharWidth / 2.0 + env$10._xInterval), 0.5)), groupEnd$8));
    }
  };
  let fixCjkLeftQuote = (currentX, context, charFontSize, charCode, charWidth) => {
    let flag_STDWIDTH = env$10._flag_STDWIDTH;
    let offset = !context._lastIsPunctuation && /* '“' */8220 === charCode ? charFontSize / 2.0 : 0.0;
    return currentX + (CompressOptions$11.fixCJKLeftQuote && /* '“' */8220 === charCode && (!flag_STDWIDTH || charWidth === charFontSize) ? offset - charFontSize / 2.0 : offset);
  };
  let Compress = {
    process: process,
    processUncompress: processUncompress,
    fixCjkLeftQuote: fixCjkLeftQuote
  };
  let env$11 = env;
  let logger$18 = console;
  let groupCollapsed$18 = CompressOptions.groupCollapsed;
  let label$8 = "Process";
  let ProcessLogger_debugger = CompressOptions.$$debugger;
  let ProcessLogger = {
    logger: logger$18,
    $$debugger: ProcessLogger_debugger,
    groupCollapsed: groupCollapsed$18,
    enable: true,
    label: label$8
  };
  let ILogger = {
    enable: true,
    groupCollapsed: groupCollapsed$18,
    label: label$8,
    logger: logger$18
  };
  let toArgs$9 = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return ["%c%s%c " + match, "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args.slice(1));
    }
    return ["%c%s%c %s", "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;", label, ""].concat(args);
  };
  let logger$19 = logger$18;
  let enable$18 = ILogger.enable;
  let debuggerGroupCollapsed$9 = groupCollapsed$18;
  let debug$19 = a => {
    if (enable$18) {
      logger$19.debug(a);
      return;
    }
  };
  let debug1$9 = (a, a2) => {
    if (enable$18) {
      logger$19.debug(a, a2);
      return;
    }
  };
  let debug2$9 = (a, a2, a3) => {
    if (enable$18) {
      logger$19.debug(a, a2, a3);
      return;
    }
  };
  let debug3$9 = (a, a2, a3, a4) => {
    if (enable$18) {
      logger$19.debug(a, a2, a3, a4);
      return;
    }
  };
  let debug4$9 = (a, a2, a3, a4, a5) => {
    if (enable$18) {
      logger$19.debug(a, a2, a3, a4, a5);
      return;
    }
  };
  let debug5$9 = (a, a2, a3, a4, a5, a6) => {
    if (enable$18) {
      logger$19.debug(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let debug6$9 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable$18) {
      logger$19.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
  };
  let banner$9 = a => {
    if (enable$18) {
      logger$19.banner(a);
      return;
    }
  };
  let banner1$9 = (a, a2) => {
    if (enable$18) {
      logger$19.banner(a, a2);
      return;
    }
  };
  let banner2$9 = (a, a2, a3) => {
    if (enable$18) {
      logger$19.banner(a, a2, a3);
      return;
    }
  };
  let banner3$9 = (a, a2, a3, a4) => {
    if (enable$18) {
      logger$19.banner(a, a2, a3, a4);
      return;
    }
  };
  let banner4$9 = (a, a2, a3, a4, a5) => {
    if (enable$18) {
      logger$19.banner(a, a2, a3, a4, a5);
      return;
    }
  };
  let banner5$9 = (a, a2, a3, a4, a5, a6) => {
    if (enable$18) {
      logger$19.banner(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let log$9 = a => {
    if (enable$18) {
      logger$19.log(a);
      return;
    }
  };
  let log1$9 = (a, a2) => {
    if (enable$18) {
      logger$19.log(a, a2);
      return;
    }
  };
  let log2$9 = (a, a2, a3) => {
    if (!enable$18) {
      return;
    }
    let args = toArgs$9(label$8, [a, a2, a3]);
    logger$19.log.apply(null, args);
  };
  let log3$9 = (a, a2, a3, a4) => {
    if (enable$18) {
      logger$19.log(a, a2, a3, a4);
      return;
    }
  };
  let log4$9 = (a, a2, a3, a4, a5) => {
    if (enable$18) {
      logger$19.log(a, a2, a3, a4, a5);
      return;
    }
  };
  let log5$9 = (a, a2, a3, a4, a5, a6) => {
    if (enable$18) {
      logger$19.log(a, a2, a3, a4, a5, a6);
      return;
    }
  };
  let groupCollapsed$19 = a => {
    if (enable$18) {
      if (debuggerGroupCollapsed$9) {
        logger$19.groupCollapsed(a);
      } else {
        logger$19.group(a);
      }
      return;
    }
  };
  let groupCollapsed1$9 = (a, a1) => {
    if (enable$18) {
      if (debuggerGroupCollapsed$9) {
        logger$19.groupCollapsed(a, a1);
      } else {
        logger$19.group(a, a1);
      }
      return;
    }
  };
  let groupCollapsed2$9 = (a, a1, a2) => {
    if (enable$18) {
      if (debuggerGroupCollapsed$9) {
        logger$19.groupCollapsed(a, a1, a2);
      } else {
        logger$19.group(a, a1, a2);
      }
      return;
    }
  };
  let groupCollapsed3$9 = (a, a1, a2, a3) => {
    if (enable$18) {
      if (debuggerGroupCollapsed$9) {
        logger$19.groupCollapsed(a, a1, a2, a3);
      } else {
        logger$19.group(a, a1, a2, a3);
      }
      return;
    }
  };
  let groupCollapsed4$9 = (a, a1, a2, a3, a4) => {
    if (enable$18) {
      if (debuggerGroupCollapsed$9) {
        logger$19.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger$19.group(a, a1, a2, a3, a4);
      }
      return;
    }
  };
  let groupCollapsed5$9 = (a, a1, a2, a3, a4, a5) => {
    if (enable$18) {
      if (debuggerGroupCollapsed$9) {
        logger$19.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger$19.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
  };
  let groupCollapsed6$9 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable$18) {
      if (debuggerGroupCollapsed$9) {
        logger$19.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger$19.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
  };
  let groupCollapsed7$9 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable$18) {
      if (debuggerGroupCollapsed$9) {
        logger$19.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger$19.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
  };
  let groupEnd$9 = () => {
    if (enable$18) {
      logger$19.groupEnd();
      return;
    }
  };
  let groupEnd1$9 = a => {
    if (enable$18) {
      logger$19.groupEnd(a);
      return;
    }
  };
  let groupEnd2$9 = (a, a1) => {
    if (enable$18) {
      logger$19.groupEnd(a, a1);
      return;
    }
  };
  let groupEnd3$9 = (a, a1, a2) => {
    if (enable$18) {
      logger$19.groupEnd(a, a1, a2);
      return;
    }
  };
  let Logger$8 = {
    toArgs: toArgs$9,
    logger: logger$19,
    enable: enable$18,
    debuggerGroupCollapsed: debuggerGroupCollapsed$9,
    debug: debug$19,
    debug1: debug1$9,
    debug2: debug2$9,
    debug3: debug3$9,
    debug4: debug4$9,
    debug5: debug5$9,
    debug6: debug6$9,
    banner: banner$9,
    banner1: banner1$9,
    banner2: banner2$9,
    banner3: banner3$9,
    banner4: banner4$9,
    banner5: banner5$9,
    log: log$9,
    log1: log1$9,
    log2: log2$9,
    log3: log3$9,
    log4: log4$9,
    log5: log5$9,
    groupCollapsed: groupCollapsed$19,
    groupCollapsed1: groupCollapsed1$9,
    groupCollapsed2: groupCollapsed2$9,
    groupCollapsed3: groupCollapsed3$9,
    groupCollapsed4: groupCollapsed4$9,
    groupCollapsed5: groupCollapsed5$9,
    groupCollapsed6: groupCollapsed6$9,
    groupCollapsed7: groupCollapsed7$9,
    groupEnd: groupEnd$9,
    groupEnd1: groupEnd1$9,
    groupEnd2: groupEnd2$9,
    groupEnd3: groupEnd3$9
  };
  let closeCompactWindow = (context, currentIsPunctuation, end) => {
    /*closeCompactWindow*/if (isProcessing()) {
      if (CompressOptions.forceSpaceBetweenCJKAndWestern && !(end || !(!currentIsPunctuation && allowPanguClose(context)))) {
        closeWindow(context, true);
      } else {
        closeWindow(context, false);
      }
      return groupEnd1$9("process compact");
    }
  };
  let recordLastElement$5 = (context, char, charWidth, charFontSize, charCode) => {
    Ecs.out(char, char => {
      let x = CompressOptions.fixCJKLeftQuote ? fixCjkLeftQuote(getCurrentX(context), context, charFontSize, charCode, charWidth) : getCurrentX(context);
      let y = getCurrentY(context) - (charFontSize - env$11._gridSize) / 2.0;
      Ecs.setPosition(char, x, y);
      Ecs.setSize(char, charWidth, charFontSize);
    });
    recordLastElement(context, char);
  };
  let processChar_ = (context, char, elementType, currentIsPunctuation, isValidEndChar) => {
    let isInvalidEndColumn = getCurrentCol(context) >= env$11._lastCol && currentIsPunctuation && (elementType === 10 || elementType === 12 || elementType === 15 || elementType === 4 || elementType === 17);
    let overflowCol = getCurrentCol(context) >= env$11._maxCol;
    let isEOL = overflowCol || isInvalidEndColumn;
    let charWidth = Ecs.out(char, Ecs.getWidth);
    let charCode = Ecs.out(char, Ecs.getCharCode);
    let charFontSize = Ecs.out(char, Ecs.getFontSize);
    if (isEOL && (!isValidEndChar || isInvalidEndColumn || context._isNeedForceWrap)) {
      debug4$9("overflowCol: %o\nisInvalidEndChar: %o\nisInvalidEndColumn: %o\nisNeedForceWrap: %o", overflowCol, !isValidEndChar, isInvalidEndColumn, context._isNeedForceWrap);
      lineWrap(context);
    }
    if (CompressOptions.forceGridAlignment) {
      buffOffsetX$1(context, charFontSize, overflowCol && isValidEndChar);
    }
    Ecs.out(char, char => {
      let x = CompressOptions.fixCJKLeftQuote ? fixCjkLeftQuote(getCurrentX(context), context, charFontSize, charCode, charWidth) : getCurrentX(context);
      let y = getCurrentY(context) - (charFontSize - env$11._gridSize) / 2.0;
      Ecs.setPosition(char, x, y);
      Ecs.setSize(char, charWidth, charFontSize);
    });
    recordLastElement(context, char);
    if (CompressOptions.forceGridAlignment) {
      consumeOffsetX$1(context);
    }
    let t = getCurrentRow(context) < env$11._maxRow ? "Continue" : "Break";
    setLastCharWidth(growRowHeight(process(context, char, charFontSize, charWidth, currentIsPunctuation, isEOL, isValidEndChar), charFontSize), charWidth);
    return t;
  };
  let processChar = (context, char, elementType, currentIsPunctuation, isValidEndChar) => {
    if (elementType === 5) {
      lineWrap(context);
      return "Continue";
    }
    let isInvalidEndColumn = getCurrentCol(context) >= env$11._lastCol && currentIsPunctuation && (elementType === 10 || elementType === 12 || elementType === 15 || elementType === 4 || elementType === 17);
    let overflowCol = getCurrentCol(context) >= env$11._maxCol;
    let isEOL = overflowCol || isInvalidEndColumn;
    let charWidth = Ecs.out(char, Ecs.getWidth);
    let charCode = Ecs.out(char, Ecs.getCharCode);
    let charFontSize = Ecs.out(char, Ecs.getFontSize);
    if (isEOL && (!isValidEndChar || isInvalidEndColumn || context._isNeedForceWrap)) {
      debug4$9("overflowCol: %o\nisInvalidEndChar: %o\nisInvalidEndColumn: %o\nisNeedForceWrap: %o", overflowCol, !isValidEndChar, isInvalidEndColumn, context._isNeedForceWrap);
      lineWrap(context);
    }
    if (CompressOptions.forceGridAlignment) {
      buffOffsetX$1(context, charFontSize, overflowCol && isValidEndChar);
    }
    Ecs.out(char, char => {
      let x = CompressOptions.fixCJKLeftQuote ? fixCjkLeftQuote(getCurrentX(context), context, charFontSize, charCode, charWidth) : getCurrentX(context);
      let y = getCurrentY(context) - (charFontSize - env$11._gridSize) / 2.0;
      Ecs.setPosition(char, x, y);
      Ecs.setSize(char, charWidth, charFontSize);
    });
    recordLastElement(context, char);
    if (CompressOptions.forceGridAlignment) {
      consumeOffsetX$1(context);
    }
    let t = getCurrentRow(context) < env$11._maxRow ? "Continue" : "Break";
    setLastCharWidth(growRowHeight(process(context, char, charFontSize, charWidth, currentIsPunctuation, isEOL, isValidEndChar), charFontSize), charWidth);
    return t;
  };
  let allowContinueCompactProcess = (elementType, isValidEndChar) => {
    if (isProcessing()) {
      if (elementType === 4) {
        return true;
      } else {
        return isValidEndChar;
      }
    } else {
      return false;
    }
  };
  let allowProcessCompactText = (context, elementType) => {
    switch (elementType) {
      case 9:
        return isProcessing();
      case 3:
      case 13:
      case 14:
      case 17:
        return true;
      default:
        let tmp;
        switch (elementType) {
          case 10:
          case 12:
          case 15:
            tmp = true;
            break;
          default:
            tmp = false;
        }
        if (tmp) {
          return nextIsWord(context);
        }
        if (!isProcessing()) {
          return false;
        }
        let tmp$1;
        switch (elementType) {
          case 10:
          case 11:
          case 12:
            tmp$1 = true;
            break;
          default:
            tmp$1 = false;
        }
        if (tmp$1) {
          return true;
        }
        switch (elementType) {
          case 15:
          case 16:
            return true;
          default:
            return false;
        }
    }
  };
  let each = (context, char, elementType, currentIsPunctuation, isValidEndChar) => {
    debug6$9("%o: %o (%s) width: %d(px), isValidEndChar: %s, nextIsWord: %s", char, Ecs.out(char, Ecs.getCharacter), ElementType$Std.print(Ecs.out(char, Ecs.getElementType)), Ecs.out(char, Ecs.getWidth), isValidEndChar, nextIsWord(context));
    let tmp = true;
    if (!(isProcessing() && (elementType === 4 || isValidEndChar))) {
      let tmp$1;
      switch (elementType) {
        case 9:
          tmp$1 = isProcessing();
          break;
        case 3:
        case 13:
        case 14:
        case 17:
          tmp$1 = true;
          break;
        default:
          let tmp$2;
          switch (elementType) {
            case 10:
            case 12:
            case 15:
              tmp$2 = true;
              break;
            default:
              tmp$2 = false;
          }
          if (tmp$2) {
            tmp$1 = nextIsWord(context);
          } else if (isProcessing()) {
            let tmp$3;
            switch (elementType) {
              case 10:
              case 11:
              case 12:
                tmp$3 = true;
                break;
              default:
                tmp$3 = false;
            }
            if (tmp$3) {
              tmp$1 = true;
            } else {
              let tmp$4;
              switch (elementType) {
                case 15:
                case 16:
                  tmp$4 = true;
                  break;
                default:
                  tmp$4 = false;
              }
              tmp$1 = tmp$4;
            }
          } else {
            tmp$1 = false;
          }
      }
      tmp = tmp$1;
    }
    if (tmp) {
      if (!isProcessing()) {
        openWindow(context._cursor);
        if (CompressOptions.forceSpaceBetweenCJKAndWestern) {
          if (currentIsPunctuation) {
            debug$19("[盘古] 由于是标点，不做处理");
          } else {
            debug$19("[盘古] 插入");
            processPanguBegin(context);
          }
        }
        groupCollapsed$19("处理紧凑模式");
      }
      return processWindow(context, char, Ecs.out(char, Ecs.getFontSize));
    }
    closeCompactWindow(context, currentIsPunctuation, false);
    if (elementType === 5) {
      lineWrap(context);
      return "Continue";
    }
    let isInvalidEndColumn = getCurrentCol(context) >= env$11._lastCol && currentIsPunctuation && (elementType === 10 || elementType === 12 || elementType === 15 || elementType === 4 || elementType === 17);
    let overflowCol = getCurrentCol(context) >= env$11._maxCol;
    let isEOL = overflowCol || isInvalidEndColumn;
    let charWidth = Ecs.out(char, Ecs.getWidth);
    let charCode = Ecs.out(char, Ecs.getCharCode);
    let charFontSize = Ecs.out(char, Ecs.getFontSize);
    if (isEOL && (!isValidEndChar || isInvalidEndColumn || context._isNeedForceWrap)) {
      debug4$9("overflowCol: %o\nisInvalidEndChar: %o\nisInvalidEndColumn: %o\nisNeedForceWrap: %o", overflowCol, !isValidEndChar, isInvalidEndColumn, context._isNeedForceWrap);
      lineWrap(context);
    }
    if (CompressOptions.forceGridAlignment) {
      buffOffsetX$1(context, charFontSize, overflowCol && isValidEndChar);
    }
    Ecs.out(char, char => {
      let x = CompressOptions.fixCJKLeftQuote ? fixCjkLeftQuote(getCurrentX(context), context, charFontSize, charCode, charWidth) : getCurrentX(context);
      let y = getCurrentY(context) - (charFontSize - env$11._gridSize) / 2.0;
      Ecs.setPosition(char, x, y);
      Ecs.setSize(char, charWidth, charFontSize);
    });
    recordLastElement(context, char);
    if (CompressOptions.forceGridAlignment) {
      consumeOffsetX$1(context);
    }
    let t = getCurrentRow(context) < env$11._maxRow ? "Continue" : "Break";
    setLastCharWidth(growRowHeight(process(context, char, charFontSize, charWidth, currentIsPunctuation, isEOL, isValidEndChar), charFontSize), charWidth);
    return t;
  };
  let Process = {
    closeCompactWindow: closeCompactWindow,
    recordLastElement: recordLastElement$5,
    processChar_: processChar_,
    processChar: processChar,
    allowContinueCompactProcess: allowContinueCompactProcess,
    allowProcessCompactText: allowProcessCompactText,
    each: each
  };
  let each$1 = (context, char, prevChar) => {
    let elementType = Ecs.out(char, Ecs.getElementType);
    let currentIsPunctuation = elementType !== 2 && elementType !== 14 && elementType !== 3 && elementType !== 17 && elementType !== 4 && elementType !== 5 && elementType !== 1 && elementType !== 0;
    if (CompressOptions.$$debugger && elementType === 0) {
      let str = "Invalid Element: " + char.toString();
      throw new RangeError(str);
    }
    context._prevChar = prevChar;
    processUncompress(context, elementType, currentIsPunctuation);
    let tmp = false;
    if (currentIsPunctuation) {
      let tmp$1;
      switch (elementType) {
        case 5:
        case 6:
        case 7:
        case 8:
        case 9:
        case 11:
        case 12:
        case 16:
        case 17:
          tmp$1 = true;
          break;
        default:
          tmp$1 = false;
      }
      tmp = tmp$1;
    }
    return each(context, char, elementType, currentIsPunctuation, tmp);
  };
  let output$6 = context => {
    closeCompactWindow(context, false, true);
    debug1$9("context", context);
    groupEnd$9();
    return flush(context);
  };
  return {
    CompressOptions: CompressOptions,
    Pipe: Pipe,
    Env: Env,
    Store: Store,
    Ecs: Ecs,
    Runtime: Runtime,
    Context: Context,
    CompactContext: CompactContext,
    ForceGridAlignment: ForceGridAlignment$1,
    Compress: Compress,
    env: env$11,
    compressOptions: compressOptions,
    ProcessLogger: ProcessLogger,
    Logger: Logger$8,
    Process: Process,
    each: each$1,
    output: output$6
  };
};
export { MakeProcess }; /* Array-Std Not a pure module */
//# sourceMappingURL=Process.bs.js.map