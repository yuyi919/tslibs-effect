export function* unsafe_map(iterable, fn) {
  for (let a of iterable) yield fn(a);
}
export let unsafe_every = (iterable, fn) => {
  for (let a of iterable) {
    if (!fn(a)) return false;
  }
  return true;
};
//# sourceMappingURL=Iterable.js.map
