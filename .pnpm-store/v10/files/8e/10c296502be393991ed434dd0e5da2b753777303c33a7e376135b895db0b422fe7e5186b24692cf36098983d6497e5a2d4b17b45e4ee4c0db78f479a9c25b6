// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Constant from "./constant";
import * as Primitive_option from "rescript/lib/es6/Primitive_option.js";

function fromIterator(prim) {
  return Constant.ARRAY_FROM(prim);
}

function fromArrayLike(prim) {
  return Constant.ARRAY_FROM(prim);
}

function fromArrayLikeWithMap(prim0, prim1) {
  return Constant.ARRAY_FROM(prim0, prim1);
}

function isArray(prim) {
  return Constant.IS_ARR(prim);
}

function get(prim0, prim1) {
  return prim0[prim1];
}

function set(prim0, prim1, prim2) {
  prim0[prim1] = prim2;
}

function length(prim) {
  return prim.length;
}

function makeUnsafe(prim) {
  return Array(prim);
}

function indexOfOpt(arr, item) {
  let index = arr.indexOf(item);
  if (index !== -1) {
    return index;
  }
  
}

function lastIndexOfOpt(arr, item) {
  let index = arr.lastIndexOf(item);
  if (index !== -1) {
    return index;
  }
  
}

function findIndexOpt(fa, finder) {
  let index = fa.findIndex(finder);
  if (index !== -1) {
    return index;
  }
  
}

function reduceI(arr, init, f) {
  return arr.reduce(f, init);
}

function reduce(arr, init, f) {
  return arr.reduce(f, init);
}

function reduceRight(arr, init, f) {
  return arr.reduceRight(f, init);
}

function reduceRightI(arr, init, f) {
  return arr.reduceRight(f, init);
}

function make(length, x) {
  if (length <= 0) {
    return [];
  } else {
    return Array(length).fill(x);
  }
}

function unsafe_fromInitializer(length, f) {
  let arr = Array(length);
  for (let i = 0; i < length; ++i) {
    arr[i] = f(i);
  }
  return arr;
}

function fromInitializer(length, f) {
  if (length <= 0) {
    return [];
  } else {
    return unsafe_fromInitializer(length, f);
  }
}

function lastIndex(a) {
  return a.length - 1 | 0;
}

function equalFromIndex(fa, fb, _i, eq, len) {
  while (true) {
    let i = _i;
    if (i === len) {
      return true;
    }
    if (!eq(fa[i], fb[i])) {
      return false;
    }
    _i = i + 1 | 0;
    continue;
  };
}

function equal(fa, fb, eq) {
  let len = fa.length;
  if (len === fb.length) {
    return equalFromIndex(fa, fb, 0, eq, len);
  } else {
    return false;
  }
}

function equalFromIndex$1(fa, fb, eq, start) {
  let len = fa.length;
  if (len === fb.length) {
    return equalFromIndex(fa, fb, start, eq, len);
  } else {
    return false;
  }
}

function compare(a, b, cmp) {
  let lenA = a.length;
  let b$1 = b.length;
  let param_1 = lenA === b$1 ? 0 : (
      lenA > b$1 ? 1 : -1
    );
  if (param_1 === 0) {
    let _i = 0;
    let len = lenA;
    while (true) {
      let i = _i;
      if (i === len) {
        return 0;
      }
      let c = cmp(a[i], b[i]);
      if (c !== 0) {
        return c;
      }
      _i = i + 1 | 0;
      continue;
    };
  } else {
    return 0;
  }
}

function shuffle(xs) {
  let len = length(xs);
  for (let i = 0; i < len; ++i) {
    let f = Math.random() * (len - i | 0);
    let j = (Math.floor(f) | 0) + i | 0;
    let tmp = get(xs, i);
    set(xs, i, get(xs, j));
    set(xs, j, tmp);
  }
}

let Shuffle = {
  shuffle: shuffle
};

function toShuffled(xs) {
  let result = makeUnsafe(length(xs));
  let len = length(xs);
  for (let i = 0; i < len; ++i) {
    set(result, i, get(xs, i));
  }
  for (let i$1 = 0; i$1 < len; ++i$1) {
    let f = Math.random() * (len - i$1 | 0);
    let j = (Math.floor(f) | 0) + i$1 | 0;
    let tmp = get(result, i$1);
    set(result, i$1, get(result, j));
    set(result, j, tmp);
  }
  return result;
}

let ToShuffled = {
  toShuffled: toShuffled
};

function filterMap(arr, f) {
  let l = arr.length;
  let r = Array(l);
  let j = 0;
  for (let i = 0; i < l; ++i) {
    let v = f(arr[i]);
    if (v !== undefined) {
      r[j] = Primitive_option.valFromOption(v);
      j = j + 1 | 0;
    }
    
  }
  r.length = j;
  return r;
}

function keepSome(__x) {
  return filterMap(__x, x => x);
}

function findMap(arr, f) {
  let _i = 0;
  while (true) {
    let i = _i;
    if (i === arr.length) {
      return;
    }
    let r = f(arr[i]);
    if (r !== undefined) {
      return r;
    }
    _i = i + 1 | 0;
    continue;
  };
}

function atN(arr, i) {
  if (i < 0) {
    return arr[arr.length + i | 0];
  } else {
    return arr[i];
  }
}

function atCyclic(xs, i) {
  let len = xs.length;
  if (len === 0) {
    throw {
      RE_EXN_ID: "Invalid_argument",
      _1: "StdArray.atCyclic: arrays.length == 0",
      Error: new Error()
    };
  }
  return xs[i !== 0 ? (
          i < 0 ? (len + i % len | 0) % len : i % len
        ) : 0];
}

function atLast(arr, i) {
  return arr[(arr.length - i | 0) + 1 | 0];
}

function last(a) {
  return a[a.length - 1 | 0];
}

function copyAllWithin(prim0, prim1) {
  return prim0.copyWithin(prim1);
}

function copyWithinToEnd(prim0, prim1, prim2) {
  return prim0.copyWithin(prim1, prim2);
}

function copyWithin(prim0, prim1, prim2, prim3) {
  return prim0.copyWithin(prim1, prim2, prim3);
}

function fillAll(prim0, prim1) {
  return prim0.fill(prim1);
}

function fillToEnd(prim0, prim1, prim2) {
  return prim0.fill(prim1, prim2);
}

function fill(prim0, prim1, prim2, prim3) {
  return prim0.fill(prim1, prim2, prim3);
}

function pop(prim) {
  return prim.pop();
}

function push(prim0, prim1) {
  prim0.push(prim1);
}

function reverse(prim) {
  prim.reverse();
}

function shift(prim) {
  return prim.shift();
}

function sort(prim0, prim1) {
  prim0.sort(prim1);
}

function withAt(prim0, prim1, prim2) {
  return prim0.with(prim1, prim2);
}

function unshift(prim0, prim1) {
  prim0.unshift(prim1);
}

function unshiftMany(prim0, prim1) {
  prim0.unshift(...prim1);
}

function concat(prim0, prim1) {
  return prim0.concat(prim1);
}

function flat(prim) {
  return prim.flat();
}

function includes(prim0, prim1) {
  return prim0.includes(prim1);
}

function indexOf(prim0, prim1) {
  return prim0.indexOf(prim1);
}

let indexOfOpt$1 = indexOfOpt;

function indexOfFrom(prim0, prim1, prim2) {
  return prim0.indexOf(prim1, prim2);
}

function joinWith(prim0, prim1) {
  return prim0.join(prim1);
}

function joinWithUnsafe(prim0, prim1) {
  return prim0.join(prim1);
}

function join(prim) {
  return prim.join();
}

function joinUnsafe(prim) {
  return prim.join();
}

function lastIndexOf(prim0, prim1) {
  return prim0.lastIndexOf(prim1);
}

let lastIndexOfOpt$1 = lastIndexOfOpt;

function lastIndexOfFrom(prim0, prim1, prim2) {
  return prim0.lastIndexOf(prim1, prim2);
}

function slice(prim0, prim1, prim2) {
  return prim0.slice(prim1, prim2);
}

function sliceToEnd(prim0, prim1) {
  return prim0.slice(prim1);
}

function copy(prim) {
  return prim.slice(0);
}

function toString(prim) {
  return prim.toString();
}

function toLocaleString(prim) {
  return prim.toLocaleString();
}

function every(prim0, prim1) {
  return prim0.every(prim1);
}

function everyI(prim0, prim1) {
  return prim0.every(prim1);
}

function filter(prim0, prim1) {
  return prim0.filter(prim1);
}

function filterI(prim0, prim1) {
  return prim0.filter(prim1);
}

function find(prim0, prim1) {
  return prim0.find(prim1);
}

function findI(prim0, prim1) {
  return prim0.find(prim1);
}

function findIndex(prim0, prim1) {
  return prim0.findIndex(prim1);
}

function findIndexI(prim0, prim1) {
  return prim0.findIndex(prim1);
}

function forEach(prim0, prim1) {
  prim0.forEach(prim1);
}

function forEachI(prim0, prim1) {
  prim0.forEach(prim1);
}

function map(prim0, prim1) {
  return prim0.map(prim1);
}

function mapI(prim0, prim1) {
  return prim0.map(prim1);
}

let reduce$1 = reduce;

let reduceI$1 = reduceI;

let reduceRight$1 = reduceRight;

let reduceRightI$1 = reduceRightI;

function some(prim0, prim1) {
  return prim0.some(prim1);
}

function someI(prim0, prim1) {
  return prim0.some(prim1);
}

let findIndexOpt$1 = findIndexOpt;

function toReversed(prim) {
  return prim.toReversed();
}

function toSorted(prim0, prim1) {
  return prim0.toSorted(prim1);
}

let toShuffled$1 = ToShuffled.toShuffled;

let shuffle$1 = Shuffle.shuffle;

function flatMap(prim0, prim1) {
  return prim0.flatMap(prim1);
}

function at(prim0, prim1) {
  return prim0.at(prim1);
}

export {
  fromIterator,
  fromArrayLike,
  fromArrayLikeWithMap,
  make,
  unsafe_fromInitializer,
  fromInitializer,
  equal,
  equalFromIndex$1 as equalFromIndex,
  compare,
  isArray,
  lastIndex,
  copyAllWithin,
  copyWithinToEnd,
  copyWithin,
  fillAll,
  fillToEnd,
  fill,
  pop,
  push,
  reverse,
  shift,
  sort,
  withAt,
  unshift,
  unshiftMany,
  concat,
  flat,
  includes,
  indexOf,
  indexOfOpt$1 as indexOfOpt,
  indexOfFrom,
  joinWith,
  joinWithUnsafe,
  join,
  joinUnsafe,
  lastIndexOf,
  lastIndexOfOpt$1 as lastIndexOfOpt,
  lastIndexOfFrom,
  slice,
  sliceToEnd,
  copy,
  toString,
  toLocaleString,
  every,
  everyI,
  filter,
  filterI,
  find,
  findI,
  findIndex,
  findIndexI,
  forEach,
  forEachI,
  map,
  mapI,
  reduce$1 as reduce,
  reduceI$1 as reduceI,
  reduceRight$1 as reduceRight,
  reduceRightI$1 as reduceRightI,
  some,
  someI,
  findIndexOpt$1 as findIndexOpt,
  toReversed,
  toSorted,
  filterMap,
  keepSome,
  toShuffled$1 as toShuffled,
  shuffle$1 as shuffle,
  flatMap,
  findMap,
  at,
  atN,
  atLast,
  atCyclic,
  last,
}
/*  Not a pure module */
