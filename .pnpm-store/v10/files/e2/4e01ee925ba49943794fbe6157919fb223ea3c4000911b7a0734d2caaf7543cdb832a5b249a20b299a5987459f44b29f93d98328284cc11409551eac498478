// Generated by ReScript, PLEASE EDIT WITH CARE


function toString(o) {
  return o;
}

function $$return(param, t) {
  return t;
}

function switchTo(param, t) {
  return t;
}

function compute(a, cb) {
  return cb(a);
}

function switchWith(param, cb) {
  return cb();
}

function tap(a, cb) {
  cb();
  return a;
}

function effect(a, cb) {
  cb(a);
  return a;
}

function charCodeAt(str, int) {
  return str.codePointAt(int);
}

function charAt(str, int) {
  return str.codePointAt(int);
}

function charStrAt(str, int) {
  return str[int];
}

function is32Bit(str, int) {
  return str.codePointAt(int) > 65535;
}

function test(prim0, prim1) {
  return prim0.test(prim1);
}

function getStringLength(str) {
  let realLength = 0;
  let charCode = 0;
  for (let i = 0, i_finish = str.length; i < i_finish; ++i) {
    charCode = str.codePointAt(i);
    realLength = charCode >= 0 && charCode <= 128 ? realLength + 1 | 0 : realLength + 2 | 0;
  }
  return realLength;
}

export {
  toString,
  $$return,
  switchTo,
  compute,
  switchWith,
  tap,
  effect,
  charCodeAt,
  charAt,
  charStrAt,
  is32Bit,
  test,
  getStringLength,
}
/* No side effect */
