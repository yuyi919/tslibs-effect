/**
 * @since 2.0.0
 */import { pipe } from "./function.js";
import { map } from "./IO.js"; /**
                               * Returns a random number between 0 (inclusive) and 1 (exclusive). This is a direct wrapper around JavaScript's
                               * `Math.random()`.
                               *
                               * @since 2.0.0
                               */
export let random = () => Math.random(); /**
                                         * Takes a range specified by `low` (the first argument) and `high` (the second), and returns a random integer uniformly
                                         * distributed in the closed interval `[low, high]`. It is unspecified what happens if `low > high`, or if either of
                                         * `low` or `high` is not an integer.
                                         *
                                         * @since 2.0.0
                                         */
export let randomInt = (low, high) => map(n => Math.floor((high - low + 1) * n + low))(random); /**
                                                                                                * Returns a random number between a minimum value (inclusive) and a maximum value (exclusive). It is unspecified what
                                                                                                * happens if `maximum < minimum`.
                                                                                                *
                                                                                                * @since 2.0.0
                                                                                                */
export let randomRange = (min, max) => map(n => (max - min) * n + min)(random); /**
                                                                                * Returns a random boolean value with an equal chance of being `true` or `false`
                                                                                *
                                                                                * @since 2.0.0
                                                                                */
export let randomBool = /*#__PURE__*/map(n => n < .5)(random); /**
                                                               * Returns a random element of a `ReadonlyNonEmptyArray`.
                                                               *
                                                               * @since 2.10.0
                                                               */
export let randomElem = as => map(i => as[i])(randomInt(0, as.length - 1));
//# sourceMappingURL=Random.js.map