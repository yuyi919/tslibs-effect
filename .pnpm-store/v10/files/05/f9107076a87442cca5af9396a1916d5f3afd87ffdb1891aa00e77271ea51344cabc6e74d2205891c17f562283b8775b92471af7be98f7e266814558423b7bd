// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Array$Std from "../../Array.bs.js";
import * as Logger$Std from "./Logger.bs.js";
import * as IContext$Std from "./IContext.bs.js";
import * as ElementType$Std from "./ElementType.bs.js";

function getOffsetXFromFixCjkQuote(flag_STDWIDTH, fixCJKLeftQuote, lastIsPunctuation, charFontSize, charCode, charWidth) {
  let offset = !lastIsPunctuation && /* '“' */8220 === charCode ? charFontSize / 2.0 : 0.0;
  if (fixCJKLeftQuote && /* '“' */8220 === charCode && (!flag_STDWIDTH || charWidth === charFontSize)) {
    return offset - charFontSize / 2.0;
  } else {
    return offset;
  }
}

function MakeForceGridAlignment(Runtime) {
  let logger = console;
  let $$debugger = Runtime.CompressOptions.$$debugger;
  let groupCollapsed = Runtime.CompressOptions.groupCollapsed;
  let enable = process.env.NODE_ENV !== "production";
  let label = "main";
  let LoggerOption = {
    logger: logger,
    $$debugger: $$debugger,
    groupCollapsed: groupCollapsed,
    enable: enable,
    label: label
  };
  let toArgs = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return [
        "%c%s%c " + match,
        "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;",
        label,
        ""
      ].concat(args.slice(1));
    }
    return [
      "%c%s%c %s",
      "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;",
      label,
      ""
    ].concat(args);
  };
  let logger$1 = logger;
  let enable$1 = enable;
  let debuggerGroupCollapsed = groupCollapsed;
  let debug = a => {
    if (enable$1) {
      logger$1.debug(a);
      return;
    }
    
  };
  let debug1 = (a, a2) => {
    if (enable$1) {
      logger$1.debug(a, a2);
      return;
    }
    
  };
  let debug2 = (a, a2, a3) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3);
      return;
    }
    
  };
  let debug3 = (a, a2, a3, a4) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3, a4);
      return;
    }
    
  };
  let debug4 = (a, a2, a3, a4, a5) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3, a4, a5);
      return;
    }
    
  };
  let debug5 = (a, a2, a3, a4, a5, a6) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3, a4, a5, a6);
      return;
    }
    
  };
  let debug6 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
    
  };
  let banner = a => {
    if (enable$1) {
      logger$1.banner(a);
      return;
    }
    
  };
  let banner1 = (a, a2) => {
    if (enable$1) {
      logger$1.banner(a, a2);
      return;
    }
    
  };
  let banner2 = (a, a2, a3) => {
    if (enable$1) {
      logger$1.banner(a, a2, a3);
      return;
    }
    
  };
  let banner3 = (a, a2, a3, a4) => {
    if (enable$1) {
      logger$1.banner(a, a2, a3, a4);
      return;
    }
    
  };
  let banner4 = (a, a2, a3, a4, a5) => {
    if (enable$1) {
      logger$1.banner(a, a2, a3, a4, a5);
      return;
    }
    
  };
  let banner5 = (a, a2, a3, a4, a5, a6) => {
    if (enable$1) {
      logger$1.banner(a, a2, a3, a4, a5, a6);
      return;
    }
    
  };
  let log = a => {
    if (enable$1) {
      logger$1.log(a);
      return;
    }
    
  };
  let log1 = (a, a2) => {
    if (enable$1) {
      logger$1.log(a, a2);
      return;
    }
    
  };
  let log2 = (a, a2, a3) => {
    if (!enable$1) {
      return;
    }
    let args = toArgs(label, [
      a,
      a2,
      a3
    ]);
    logger$1.log.apply(null, args);
  };
  let log3 = (a, a2, a3, a4) => {
    if (enable$1) {
      logger$1.log(a, a2, a3, a4);
      return;
    }
    
  };
  let log4 = (a, a2, a3, a4, a5) => {
    if (enable$1) {
      logger$1.log(a, a2, a3, a4, a5);
      return;
    }
    
  };
  let log5 = (a, a2, a3, a4, a5, a6) => {
    if (enable$1) {
      logger$1.log(a, a2, a3, a4, a5, a6);
      return;
    }
    
  };
  let groupCollapsed$1 = a => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a);
      } else {
        logger$1.group(a);
      }
      return;
    }
    
  };
  let groupCollapsed1 = (a, a1) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1);
      } else {
        logger$1.group(a, a1);
      }
      return;
    }
    
  };
  let groupCollapsed2 = (a, a1, a2) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2);
      } else {
        logger$1.group(a, a1, a2);
      }
      return;
    }
    
  };
  let groupCollapsed3 = (a, a1, a2, a3) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3);
      } else {
        logger$1.group(a, a1, a2, a3);
      }
      return;
    }
    
  };
  let groupCollapsed4 = (a, a1, a2, a3, a4) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger$1.group(a, a1, a2, a3, a4);
      }
      return;
    }
    
  };
  let groupCollapsed5 = (a, a1, a2, a3, a4, a5) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger$1.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
    
  };
  let groupCollapsed6 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger$1.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
    
  };
  let groupCollapsed7 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger$1.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
    
  };
  let groupEnd = () => {
    if (enable$1) {
      logger$1.groupEnd();
      return;
    }
    
  };
  let groupEnd1 = a => {
    if (enable$1) {
      logger$1.groupEnd(a);
      return;
    }
    
  };
  let groupEnd2 = (a, a1) => {
    if (enable$1) {
      logger$1.groupEnd(a, a1);
      return;
    }
    
  };
  let groupEnd3 = (a, a1, a2) => {
    if (enable$1) {
      logger$1.groupEnd(a, a1, a2);
      return;
    }
    
  };
  let Logger = {
    toArgs: toArgs,
    logger: logger$1,
    enable: enable$1,
    debuggerGroupCollapsed: debuggerGroupCollapsed,
    debug: debug,
    debug1: debug1,
    debug2: debug2,
    debug3: debug3,
    debug4: debug4,
    debug5: debug5,
    debug6: debug6,
    banner: banner,
    banner1: banner1,
    banner2: banner2,
    banner3: banner3,
    banner4: banner4,
    banner5: banner5,
    log: log,
    log1: log1,
    log2: log2,
    log3: log3,
    log4: log4,
    log5: log5,
    groupCollapsed: groupCollapsed$1,
    groupCollapsed1: groupCollapsed1,
    groupCollapsed2: groupCollapsed2,
    groupCollapsed3: groupCollapsed3,
    groupCollapsed4: groupCollapsed4,
    groupCollapsed5: groupCollapsed5,
    groupCollapsed6: groupCollapsed6,
    groupCollapsed7: groupCollapsed7,
    groupEnd: groupEnd,
    groupEnd1: groupEnd1,
    groupEnd2: groupEnd2,
    groupEnd3: groupEnd3
  };
  let Runtime_env = Runtime.env;
  let Runtime_CompressOptions = Runtime.CompressOptions;
  let CompressOptions = Runtime_CompressOptions;
  let compressOptions_inlineCompression = CompressOptions.inlineCompression;
  let compressOptions_forceGridAlignment = CompressOptions.forceGridAlignment;
  let compressOptions_westernCharacterFirst = CompressOptions.westernCharacterFirst;
  let compressOptions_forceSpaceBetweenCJKAndWestern = CompressOptions.forceSpaceBetweenCJKAndWestern;
  let compressOptions_fixCJKLeftQuote = CompressOptions.fixCJKLeftQuote;
  let compressOptions = {
    inlineCompression: compressOptions_inlineCompression,
    forceGridAlignment: compressOptions_forceGridAlignment,
    westernCharacterFirst: compressOptions_westernCharacterFirst,
    forceSpaceBetweenCJKAndWestern: compressOptions_forceSpaceBetweenCJKAndWestern,
    fixCJKLeftQuote: compressOptions_fixCJKLeftQuote
  };
  let initlize = (env, options, firstChar, hasLeftQuote, paddingLeft) => {
    env._firstChar = firstChar;
    env._hasLeftQuote = hasLeftQuote;
    env._paddingLeft = paddingLeft;
    env._gridSize = options.gridSize;
    env._forceSpace = CompressOptions.forceSpaceBetweenCJKAndWestern ? env._gridSize / 4.0 : 0.0;
    env._spaceWidth = env._gridSize * 0.35;
    env._xInterval = options.xInterval;
    env._yInterval = options.yInterval;
    env._maxCol = options.column;
    env._lastCol = env._maxCol - 1.0;
    env._maxRow = options.row;
    let value = options.wrapWidth;
    env._maxWidth = value !== undefined ? value : env._maxCol * env._gridSize;
    env._letterSpacing = options.letterSpacing;
    env._gridWidth = env._gridSize + env._xInterval;
    return env;
  };
  let IEnv_env = Runtime_env;
  let IEnv = {
    CompressOptions: CompressOptions,
    env: IEnv_env,
    compressOptions: compressOptions,
    initlize: initlize
  };
  let CompressOptions$1 = Runtime.CompressOptions;
  let Config = {
    $$debugger: CompressOptions$1.$$debugger
  };
  let $$return = (param, t) => t;
  let switchTo = (param, t) => t;
  let tapIf = (context, exp, fn) => {
    if (exp(context)) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let tapIfConst = (context, exp, fn) => {
    if (exp) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let boolean = (context, exp) => [
    exp(context) ? "True" : "False",
    context
  ];
  let onTrue = (context, fn) => {
    if (context[0] === "False") {
      return context;
    } else {
      return [
        "True",
        fn(context[1])
      ];
    }
  };
  let onFalse = (context, fn) => {
    if (context[0] === "False") {
      return [
        "False",
        fn(context[1])
      ];
    } else {
      return context;
    }
  };
  let unwrap = context => context[1];
  let computeIfConst = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIf = (context, exp, fn) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIfElse = (context, exp, fn, else$p) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return else$p(context);
    }
  };
  let pipeIfConst = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let switchIf = (context, exp, else$p, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let check = (context, if$p, else$p, fn) => {
    if (if$p(context)) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let pipe = (context, fn) => fn(context);
  let computeIfElse = (context, exp, fn, fn2) => {
    if (exp) {
      return fn(context);
    } else {
      return fn2(context);
    }
  };
  let action = (context, fn) => {
    fn(context);
    return context;
  };
  let actionU = (context, fn) => {
    fn(context);
    return context;
  };
  let actionWith = (context, withCtx, fn) => {
    fn(withCtx);
    return context;
  };
  let tap = (context, fn) => {
    fn();
    return context;
  };
  let debugTap = (context, fn) => {
    if (Config.$$debugger) {
      fn();
      return context;
    } else {
      return context;
    }
  };
  let debug$1 = (context, fn) => {
    if (Config.$$debugger) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let ignore = (context, withCtx) => context;
  let call = (context, arg) => context(arg);
  let rev = (b, fn, a) => fn(a, b);
  let Pipe = {
    $$return: $$return,
    switchTo: switchTo,
    tapIf: tapIf,
    tapIfConst: tapIfConst,
    boolean: boolean,
    onTrue: onTrue,
    onFalse: onFalse,
    unwrap: unwrap,
    computeIfConst: computeIfConst,
    pipeIf: pipeIf,
    pipeIfElse: pipeIfElse,
    pipeIfConst: pipeIfConst,
    switchIf: switchIf,
    check: check,
    pipe: pipe,
    computeIfElse: computeIfElse,
    action: action,
    actionU: actionU,
    actionWith: actionWith,
    tap: tap,
    debugTap: debugTap,
    debug: debug$1,
    ignore: ignore,
    call: call,
    rev: rev
  };
  let env = Runtime.env;
  let context = Runtime.Store.context;
  let _buf = {
    seqs: [],
    areaWidth: 0.0,
    areaHeight: 0.0,
    width: 0.0,
    height: 0.0,
    lastLineIndex: 0,
    lineHeights: context._lineHeights,
    status: context._status
  };
  let setContentWidth = (context, width) => {
    context._contentWidth = width;
    return context;
  };
  let getContentWidth = context => context._contentWidth;
  let setCurrentX = (context, x) => {
    context._currentX = x;
    return context;
  };
  let getCurrentX = context => context._currentX;
  let setCurrentY = (context, y) => {
    context._currentY = y;
    return context;
  };
  let getCurrentY = context => context._currentY;
  let setRowHeight = (context, rowHeight) => {
    context._currentRowHeight = rowHeight;
    return context;
  };
  let getRowHeight = context => context._currentRowHeight;
  let storeLineHeights = context => {
    context._lineHeights.push(context._currentRowHeight);
    return context;
  };
  let setCurrentCol = (context, col) => {
    context._currentCol = col;
    return context;
  };
  let setCurrentColU = (context, col) => {
    context._currentCol = col;
  };
  let getCurrentCol = context => context._currentCol;
  let setCurrentRow = (context, row) => {
    context._currentRow = row;
    return context;
  };
  let getCurrentRow = context => context._currentRow;
  let finish = param => {};
  let syncContentWidth = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    return context;
  };
  let growRowHeight = (context, elementHeight) => {
    if (elementHeight > context._currentRowHeight) {
      context._currentRowHeight = elementHeight;
    }
    return context;
  };
  let resetCurrent = context => {
    context._currentX = 0.0;
    let context$1 = context;
    context$1._currentY = 0.0;
    let context$2 = context$1;
    context$2._currentCol = 0.0;
    let context$3 = context$2;
    context$3._currentRow = 1;
    return context$3;
  };
  let rollbackX = (context, x) => {
    debug3("rollbackX: %o - %o => %o", context._currentX, x, context._currentX - x);
    context._currentX = context._currentX - x;
    return context;
  };
  let moveCursorX = (context, x) => {
    debug3("[cursor] moveX: %o + %o => %o", context._currentX, x, context._currentX + x);
    context._currentX = context._currentX + x;
    return context;
  };
  let consumeXIn = (x, context) => {
    debug3("[cursor] moveX: %o + %o => %o", context._currentX, x, context._currentX + x);
    context._currentX = context._currentX + x;
    return x;
  };
  let growCurrentY = (context, y) => {
    context._currentY = context._currentY + y;
    return context;
  };
  let rollbackCol = (context, col) => {
    context._currentCol = context._currentCol - col;
    return context;
  };
  let consumeCol = (context, col) => {
    context._currentCol = context._currentCol + col;
    return context;
  };
  let consumeColU = (context, col) => {
    context._currentCol = context._currentCol + col;
  };
  let growCurrentRow = context => {
    context._currentY = context._currentY + (context._currentRowHeight + env._yInterval);
    let context$1 = context;
    context$1._currentRow = context._currentRow + 1 | 0;
    return context$1;
  };
  let setLastCharWidth = (context, charWidth) => {
    context._lastCharWidth = charWidth;
    return context;
  };
  let nextIsWord = context => {
    let kind = context._cursor < context._source.length ? Runtime.out(context._source[context._cursor + 1 | 0], Runtime.getElementType) : Runtime.out(context._END, Runtime.getElementType);
    switch (kind) {
      case 3 :
      case 12 :
        return true;
      default:
        return false;
    }
  };
  let lastIsNotPunctuation = context => {
    context._lastIsPunctuation = false;
    context._lastPunctuations = 0.0;
    return context;
  };
  let consumePunctuation = context => {
    context._lastIsPunctuation = true;
    context._lastPunctuations = context._lastPunctuations + 1.0;
    return context;
  };
  let needForceWrap = context => {
    context._isNeedForceWrap = true;
    return context;
  };
  let resetNeedForceWrap = context => {
    context._isNeedForceWrap = false;
    return context;
  };
  let recordLastElement = (context, char) => {
    context.rows[context._currentRow] = char;
  };
  let getRowSize = context => context.rows.length;
  let prepare = (context, source, options) => {
    let firstChar = source[0];
    let hasLeftQuote = Runtime.out(firstChar, Runtime.getElementType) === 10;
    let paddingLeft = hasLeftQuote ? Runtime.out(firstChar, Runtime.getWidth) + env._xInterval : 0.0;
    let env$1 = initlize(env, options, firstChar, hasLeftQuote, paddingLeft);
    context._currentX = 0.0;
    let context$1 = context;
    context$1._currentY = 0.0;
    let context$2 = context$1;
    context$2._currentCol = 0.0;
    let context$3 = context$2;
    context$3._currentRow = 1;
    let context$4 = context$3;
    context$4._lastIsPunctuation = false;
    context$4._lastPunctuations = 0.0;
    let context$5 = context$4;
    context$5._currentRowHeight = env$1._gridSize;
    let context$6 = context$5;
    context$6._lastCharWidth = 0.0;
    let context$7 = context$6;
    context$7._isNeedForceWrap = false;
    let context$8 = context$7;
    action((context$8._contentWidth = 0.0, context$8), context => {
      context._cursor = 0;
      context._lastIsWesternChar = CompressOptions$1.westernCharacterFirst;
      context._source = source;
      context.rows = [
        firstChar,
        firstChar
      ];
    });
    debug2("prepare", context, env$1);
  };
  let lineWrap = context => {
    log("lineWrap");
    /* lineWrap */
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let context$1 = context;
    context$1._currentCol = env._hasLeftQuote ? 1.0 : 0.0;
    let context$2 = context$1;
    context$2._currentX = env._paddingLeft;
    let context$3 = context$2;
    context$3._currentY = context$3._currentY + (context$3._currentRowHeight + env._yInterval);
    let context$4 = context$3;
    context$4._currentRow = context$3._currentRow + 1 | 0;
    let context$5 = context$4;
    context$5._lineHeights.push(context$5._currentRowHeight);
    let context$6 = context$5;
    context$6._currentRowHeight = env._gridSize;
    let context$7 = context$6;
    context$7._lastIsPunctuation = false;
    context$7._lastPunctuations = 0.0;
    let context$8 = context$7;
    context$8._lastCharWidth = 0.0;
    let context$9 = context$8;
    context$9._isNeedForceWrap = false;
  };
  let incrementFlush = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let lastLineIndex = context._currentRow;
    let overflowY = lastLineIndex > (context.rows.length - 1 | 0);
    let contentHeight = overflowY ? context._currentY : context._currentY + (context._lineHeights.push(context._currentRowHeight), context)._currentRowHeight;
    _buf.seqs = context.rows;
    _buf.areaWidth = context._contentWidth;
    _buf.areaHeight = contentHeight;
    _buf.width = env._maxWidth;
    _buf.height = contentHeight;
    _buf.lastLineIndex = overflowY ? lastLineIndex - 1 | 0 : lastLineIndex;
    _buf.lineHeights = context._lineHeights;
    _buf.status = "flushing";
    return _buf;
  };
  let flush = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let lastLineIndex = context._currentRow;
    let overflowY = lastLineIndex > (context.rows.length - 1 | 0);
    let contentHeight = overflowY ? context._currentY : context._currentY + (context._lineHeights.push(context._currentRowHeight), context)._currentRowHeight;
    _buf.seqs = context.rows;
    _buf.areaWidth = context._contentWidth;
    _buf.areaHeight = contentHeight;
    _buf.width = env._maxWidth;
    _buf.height = contentHeight;
    _buf.lastLineIndex = overflowY ? lastLineIndex - 1 | 0 : lastLineIndex;
    _buf.lineHeights = context._lineHeights;
    _buf.status = "closed";
    context._lineHeights = [];
    debug1("contentHeight: %o", contentHeight);
    return _buf;
  };
  let output = param => ({
    seqs: _buf.seqs,
    areaWidth: _buf.areaWidth,
    areaHeight: _buf.areaHeight,
    width: _buf.width,
    height: _buf.height,
    lastLineIndex: _buf.lastLineIndex,
    lineHeights: _buf.lineHeights,
    status: "closed"
  });
  let Context = {
    LoggerOption: LoggerOption,
    Logger: Logger,
    IEnv: IEnv,
    ECS: Runtime,
    CompressOptions: CompressOptions$1,
    Pipe: Pipe,
    env: env,
    context: context,
    _buf: _buf,
    setContentWidth: setContentWidth,
    getContentWidth: getContentWidth,
    setCurrentX: setCurrentX,
    getCurrentX: getCurrentX,
    setCurrentY: setCurrentY,
    getCurrentY: getCurrentY,
    setRowHeight: setRowHeight,
    getRowHeight: getRowHeight,
    storeLineHeights: storeLineHeights,
    setCurrentCol: setCurrentCol,
    setCurrentColU: setCurrentColU,
    getCurrentCol: getCurrentCol,
    setCurrentRow: setCurrentRow,
    getCurrentRow: getCurrentRow,
    finish: finish,
    syncContentWidth: syncContentWidth,
    growRowHeight: growRowHeight,
    resetCurrent: resetCurrent,
    rollbackX: rollbackX,
    moveCursorX: moveCursorX,
    consumeXIn: consumeXIn,
    growCurrentY: growCurrentY,
    rollbackCol: rollbackCol,
    consumeCol: consumeCol,
    consumeColU: consumeColU,
    growCurrentRow: growCurrentRow,
    setLastCharWidth: setLastCharWidth,
    nextIsWord: nextIsWord,
    lastIsNotPunctuation: lastIsNotPunctuation,
    consumePunctuation: consumePunctuation,
    needForceWrap: needForceWrap,
    resetNeedForceWrap: resetNeedForceWrap,
    recordLastElement: recordLastElement,
    getRowSize: getRowSize,
    prepare: prepare,
    lineWrap: lineWrap,
    incrementFlush: incrementFlush,
    flush: flush,
    output: output
  };
  let logger$2 = console;
  let $$debugger$1 = Runtime.CompressOptions.$$debugger;
  let groupCollapsed$2 = Runtime.CompressOptions.groupCollapsed;
  let enable$2 = process.env.NODE_ENV !== "production";
  let label$1 = "compact";
  let LoggerOption$1 = {
    logger: logger$2,
    $$debugger: $$debugger$1,
    groupCollapsed: groupCollapsed$2,
    enable: enable$2,
    label: label$1
  };
  let toArgs$1 = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return [
        "%c%s%c " + match,
        "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;",
        label,
        ""
      ].concat(args.slice(1));
    }
    return [
      "%c%s%c %s",
      "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;",
      label,
      ""
    ].concat(args);
  };
  let logger$3 = logger$2;
  let enable$3 = enable$2;
  let debuggerGroupCollapsed$1 = groupCollapsed$2;
  let debug$2 = a => {
    if (enable$3) {
      logger$3.debug(a);
      return;
    }
    
  };
  let debug1$1 = (a, a2) => {
    if (enable$3) {
      logger$3.debug(a, a2);
      return;
    }
    
  };
  let debug2$1 = (a, a2, a3) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3);
      return;
    }
    
  };
  let debug3$1 = (a, a2, a3, a4) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3, a4);
      return;
    }
    
  };
  let debug4$1 = (a, a2, a3, a4, a5) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3, a4, a5);
      return;
    }
    
  };
  let debug5$1 = (a, a2, a3, a4, a5, a6) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3, a4, a5, a6);
      return;
    }
    
  };
  let debug6$1 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
    
  };
  let banner$1 = a => {
    if (enable$3) {
      logger$3.banner(a);
      return;
    }
    
  };
  let banner1$1 = (a, a2) => {
    if (enable$3) {
      logger$3.banner(a, a2);
      return;
    }
    
  };
  let banner2$1 = (a, a2, a3) => {
    if (enable$3) {
      logger$3.banner(a, a2, a3);
      return;
    }
    
  };
  let banner3$1 = (a, a2, a3, a4) => {
    if (enable$3) {
      logger$3.banner(a, a2, a3, a4);
      return;
    }
    
  };
  let banner4$1 = (a, a2, a3, a4, a5) => {
    if (enable$3) {
      logger$3.banner(a, a2, a3, a4, a5);
      return;
    }
    
  };
  let banner5$1 = (a, a2, a3, a4, a5, a6) => {
    if (enable$3) {
      logger$3.banner(a, a2, a3, a4, a5, a6);
      return;
    }
    
  };
  let log$1 = a => {
    if (enable$3) {
      logger$3.log(a);
      return;
    }
    
  };
  let log1$1 = (a, a2) => {
    if (enable$3) {
      logger$3.log(a, a2);
      return;
    }
    
  };
  let log2$1 = (a, a2, a3) => {
    if (!enable$3) {
      return;
    }
    let args = toArgs$1(label$1, [
      a,
      a2,
      a3
    ]);
    logger$3.log.apply(null, args);
  };
  let log3$1 = (a, a2, a3, a4) => {
    if (enable$3) {
      logger$3.log(a, a2, a3, a4);
      return;
    }
    
  };
  let log4$1 = (a, a2, a3, a4, a5) => {
    if (enable$3) {
      logger$3.log(a, a2, a3, a4, a5);
      return;
    }
    
  };
  let log5$1 = (a, a2, a3, a4, a5, a6) => {
    if (enable$3) {
      logger$3.log(a, a2, a3, a4, a5, a6);
      return;
    }
    
  };
  let groupCollapsed$3 = a => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a);
      } else {
        logger$3.group(a);
      }
      return;
    }
    
  };
  let groupCollapsed1$1 = (a, a1) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1);
      } else {
        logger$3.group(a, a1);
      }
      return;
    }
    
  };
  let groupCollapsed2$1 = (a, a1, a2) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2);
      } else {
        logger$3.group(a, a1, a2);
      }
      return;
    }
    
  };
  let groupCollapsed3$1 = (a, a1, a2, a3) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3);
      } else {
        logger$3.group(a, a1, a2, a3);
      }
      return;
    }
    
  };
  let groupCollapsed4$1 = (a, a1, a2, a3, a4) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger$3.group(a, a1, a2, a3, a4);
      }
      return;
    }
    
  };
  let groupCollapsed5$1 = (a, a1, a2, a3, a4, a5) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger$3.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
    
  };
  let groupCollapsed6$1 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger$3.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
    
  };
  let groupCollapsed7$1 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger$3.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
    
  };
  let groupEnd$1 = () => {
    if (enable$3) {
      logger$3.groupEnd();
      return;
    }
    
  };
  let groupEnd1$1 = a => {
    if (enable$3) {
      logger$3.groupEnd(a);
      return;
    }
    
  };
  let groupEnd2$1 = (a, a1) => {
    if (enable$3) {
      logger$3.groupEnd(a, a1);
      return;
    }
    
  };
  let groupEnd3$1 = (a, a1, a2) => {
    if (enable$3) {
      logger$3.groupEnd(a, a1, a2);
      return;
    }
    
  };
  let Logger$1 = {
    toArgs: toArgs$1,
    logger: logger$3,
    enable: enable$3,
    debuggerGroupCollapsed: debuggerGroupCollapsed$1,
    debug: debug$2,
    debug1: debug1$1,
    debug2: debug2$1,
    debug3: debug3$1,
    debug4: debug4$1,
    debug5: debug5$1,
    debug6: debug6$1,
    banner: banner$1,
    banner1: banner1$1,
    banner2: banner2$1,
    banner3: banner3$1,
    banner4: banner4$1,
    banner5: banner5$1,
    log: log$1,
    log1: log1$1,
    log2: log2$1,
    log3: log3$1,
    log4: log4$1,
    log5: log5$1,
    groupCollapsed: groupCollapsed$3,
    groupCollapsed1: groupCollapsed1$1,
    groupCollapsed2: groupCollapsed2$1,
    groupCollapsed3: groupCollapsed3$1,
    groupCollapsed4: groupCollapsed4$1,
    groupCollapsed5: groupCollapsed5$1,
    groupCollapsed6: groupCollapsed6$1,
    groupCollapsed7: groupCollapsed7$1,
    groupEnd: groupEnd$1,
    groupEnd1: groupEnd1$1,
    groupEnd2: groupEnd2$1,
    groupEnd3: groupEnd3$1
  };
  let Config$1 = {
    $$debugger: Runtime.CompressOptions.$$debugger
  };
  let $$return$1 = (param, t) => t;
  let switchTo$1 = (param, t) => t;
  let tapIf$1 = (context, exp, fn) => {
    if (exp(context)) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let tapIfConst$1 = (context, exp, fn) => {
    if (exp) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let boolean$1 = (context, exp) => [
    exp(context) ? "True" : "False",
    context
  ];
  let onTrue$1 = (context, fn) => {
    if (context[0] === "False") {
      return context;
    } else {
      return [
        "True",
        fn(context[1])
      ];
    }
  };
  let onFalse$1 = (context, fn) => {
    if (context[0] === "False") {
      return [
        "False",
        fn(context[1])
      ];
    } else {
      return context;
    }
  };
  let unwrap$1 = context => context[1];
  let computeIfConst$1 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIf$1 = (context, exp, fn) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIfElse$1 = (context, exp, fn, else$p) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return else$p(context);
    }
  };
  let pipeIfConst$1 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let switchIf$1 = (context, exp, else$p, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let check$1 = (context, if$p, else$p, fn) => {
    if (if$p(context)) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let pipe$1 = (context, fn) => fn(context);
  let computeIfElse$1 = (context, exp, fn, fn2) => {
    if (exp) {
      return fn(context);
    } else {
      return fn2(context);
    }
  };
  let action$1 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionU$1 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionWith$1 = (context, withCtx, fn) => {
    fn(withCtx);
    return context;
  };
  let tap$1 = (context, fn) => {
    fn();
    return context;
  };
  let debugTap$1 = (context, fn) => {
    if (Config$1.$$debugger) {
      fn();
      return context;
    } else {
      return context;
    }
  };
  let debug$3 = (context, fn) => {
    if (Config$1.$$debugger) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let ignore$1 = (context, withCtx) => context;
  let call$1 = (context, arg) => context(arg);
  let rev$1 = (b, fn, a) => fn(a, b);
  let Pipe$1 = {
    $$return: $$return$1,
    switchTo: switchTo$1,
    tapIf: tapIf$1,
    tapIfConst: tapIfConst$1,
    boolean: boolean$1,
    onTrue: onTrue$1,
    onFalse: onFalse$1,
    unwrap: unwrap$1,
    computeIfConst: computeIfConst$1,
    pipeIf: pipeIf$1,
    pipeIfElse: pipeIfElse$1,
    pipeIfConst: pipeIfConst$1,
    switchIf: switchIf$1,
    check: check$1,
    pipe: pipe$1,
    computeIfElse: computeIfElse$1,
    action: action$1,
    actionU: actionU$1,
    actionWith: actionWith$1,
    tap: tap$1,
    debugTap: debugTap$1,
    debug: debug$3,
    ignore: ignore$1,
    call: call$1,
    rev: rev$1
  };
  let env$1 = Runtime.env;
  let processOffset = (context, start, end, offset, isMultLine) => {
    let source = context._source;
    if (!(!isMultLine && offset > 0.0)) {
      return;
    }
    debug1$1("currentX_offset", offset);
    for (let i = start; i <= end; ++i) {
      Runtime.addX(Runtime.unwrap(source[i]), offset);
    }
  };
  let pipeOffset = (context, isMultLine, start, end, currentX, currentX_before, forceSpace, allow) => {
    let offset = allow ? (currentX - currentX_before + forceSpace) / 2.0 - forceSpace : (currentX - currentX_before) / 2;
    let source = context._source;
    if (!(!isMultLine && offset > 0.0)) {
      return;
    }
    debug1$1("currentX_offset", offset);
    for (let i = start; i <= end; ++i) {
      Runtime.addX(Runtime.unwrap(source[i]), offset);
    }
  };
  let getOffsetX = (context, currentX_before, isMultLine, startElementIndex, endElementIndex, allowPangu) => {
    let forceSpace = env$1._forceSpace;
    let gridWidth = env$1._gridWidth;
    let currentX = tap$1((action$1(Math.ceil(currentX_before / gridWidth), x => setCurrentX(context, x)), getCurrentCol(context) * gridWidth), () => debug$2("forceGridAlignment"));
    if (allowPangu) {
      if (currentX >= currentX_before + forceSpace) {
        return currentX;
      }
      let t = currentX + gridWidth;
      return action$1((consumeCol(context, 1.0), t), __x => {
        let offset = (__x - currentX_before + forceSpace) / 2.0 - forceSpace;
        let source = context._source;
        if (!(!isMultLine && offset > 0.0)) {
          return;
        }
        debug1$1("currentX_offset", offset);
        for (let i = startElementIndex; i <= endElementIndex; ++i) {
          Runtime.addX(Runtime.unwrap(source[i]), offset);
        }
      });
    }
    if (currentX >= currentX_before) {
      return currentX;
    }
    let t$1 = currentX + gridWidth;
    return action$1((consumeCol(context, 1.0), t$1), __x => {
      let offset = (__x - currentX_before) / 2;
      let source = context._source;
      if (!(!isMultLine && offset > 0.0)) {
        return;
      }
      debug1$1("currentX_offset", offset);
      for (let i = startElementIndex; i <= endElementIndex; ++i) {
        Runtime.addX(Runtime.unwrap(source[i]), offset);
      }
    });
  };
  let consumeOffsetX = context => {
    let offsetX = context._buf_offsetX;
    if (offsetX !== 0.0) {
      consumeXIn(pipeIfConst$1(action$1(offsetX, __x => debug1$1("consumeOffsetX", __x)), offsetX > env$1._gridSize, offsetX => {
        rollbackCol(context, 1.0);
        return offsetX - env$1._gridSize;
      }), context);
      return;
    }
    
  };
  let buffOffsetX = (context, fontSize, overflow) => {
    let gridSize = env$1._gridSize;
    let gridWidth = env$1._gridWidth;
    if (fontSize === gridSize) {
      return;
    }
    debug$2("gridAlignment");
    let offset_x = (fontSize - gridSize) / 2.0;
    context._buf_offsetX = pipe$1(consumeCol(context, offset_x > 0.0 ? Math.ceil(offset_x * 2.0 / gridWidth) : 0.0), context => {
      let offset_x$1 = ((Math.ceil(offset_x * 2.0 / gridWidth) + 1.0) * gridWidth - fontSize) / 2.0;
      let t = overflow ? (lineWrap(context), offset_x$1) * 2.0 : offset_x$1;
      moveCursorX(context, offset_x$1);
      return t;
    });
  };
  return {
    Context: Context,
    LoggerOption: LoggerOption$1,
    Logger: Logger$1,
    ECS: Runtime,
    Pipe: Pipe$1,
    env: env$1,
    processOffset: processOffset,
    pipeOffset: pipeOffset,
    getOffsetX: getOffsetX,
    consumeOffsetX: consumeOffsetX,
    buffOffsetX: buffOffsetX
  };
}

function MakeCompress(Runtime) {
  let CompressOptions = Runtime.CompressOptions;
  let logger = console;
  let $$debugger = Runtime.CompressOptions.$$debugger;
  let groupCollapsed = Runtime.CompressOptions.groupCollapsed;
  let enable = process.env.NODE_ENV !== "production";
  let label = "main";
  let LoggerOption = {
    logger: logger,
    $$debugger: $$debugger,
    groupCollapsed: groupCollapsed,
    enable: enable,
    label: label
  };
  let toArgs = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return [
        "%c%s%c " + match,
        "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;",
        label,
        ""
      ].concat(args.slice(1));
    }
    return [
      "%c%s%c %s",
      "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;",
      label,
      ""
    ].concat(args);
  };
  let logger$1 = logger;
  let enable$1 = enable;
  let debuggerGroupCollapsed = groupCollapsed;
  let debug = a => {
    if (enable$1) {
      logger$1.debug(a);
      return;
    }
    
  };
  let debug1 = (a, a2) => {
    if (enable$1) {
      logger$1.debug(a, a2);
      return;
    }
    
  };
  let debug2 = (a, a2, a3) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3);
      return;
    }
    
  };
  let debug3 = (a, a2, a3, a4) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3, a4);
      return;
    }
    
  };
  let debug4 = (a, a2, a3, a4, a5) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3, a4, a5);
      return;
    }
    
  };
  let debug5 = (a, a2, a3, a4, a5, a6) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3, a4, a5, a6);
      return;
    }
    
  };
  let debug6 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable$1) {
      logger$1.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
    
  };
  let banner = a => {
    if (enable$1) {
      logger$1.banner(a);
      return;
    }
    
  };
  let banner1 = (a, a2) => {
    if (enable$1) {
      logger$1.banner(a, a2);
      return;
    }
    
  };
  let banner2 = (a, a2, a3) => {
    if (enable$1) {
      logger$1.banner(a, a2, a3);
      return;
    }
    
  };
  let banner3 = (a, a2, a3, a4) => {
    if (enable$1) {
      logger$1.banner(a, a2, a3, a4);
      return;
    }
    
  };
  let banner4 = (a, a2, a3, a4, a5) => {
    if (enable$1) {
      logger$1.banner(a, a2, a3, a4, a5);
      return;
    }
    
  };
  let banner5 = (a, a2, a3, a4, a5, a6) => {
    if (enable$1) {
      logger$1.banner(a, a2, a3, a4, a5, a6);
      return;
    }
    
  };
  let log = a => {
    if (enable$1) {
      logger$1.log(a);
      return;
    }
    
  };
  let log1 = (a, a2) => {
    if (enable$1) {
      logger$1.log(a, a2);
      return;
    }
    
  };
  let log2 = (a, a2, a3) => {
    if (!enable$1) {
      return;
    }
    let args = toArgs(label, [
      a,
      a2,
      a3
    ]);
    logger$1.log.apply(null, args);
  };
  let log3 = (a, a2, a3, a4) => {
    if (enable$1) {
      logger$1.log(a, a2, a3, a4);
      return;
    }
    
  };
  let log4 = (a, a2, a3, a4, a5) => {
    if (enable$1) {
      logger$1.log(a, a2, a3, a4, a5);
      return;
    }
    
  };
  let log5 = (a, a2, a3, a4, a5, a6) => {
    if (enable$1) {
      logger$1.log(a, a2, a3, a4, a5, a6);
      return;
    }
    
  };
  let groupCollapsed$1 = a => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a);
      } else {
        logger$1.group(a);
      }
      return;
    }
    
  };
  let groupCollapsed1 = (a, a1) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1);
      } else {
        logger$1.group(a, a1);
      }
      return;
    }
    
  };
  let groupCollapsed2 = (a, a1, a2) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2);
      } else {
        logger$1.group(a, a1, a2);
      }
      return;
    }
    
  };
  let groupCollapsed3 = (a, a1, a2, a3) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3);
      } else {
        logger$1.group(a, a1, a2, a3);
      }
      return;
    }
    
  };
  let groupCollapsed4 = (a, a1, a2, a3, a4) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger$1.group(a, a1, a2, a3, a4);
      }
      return;
    }
    
  };
  let groupCollapsed5 = (a, a1, a2, a3, a4, a5) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger$1.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
    
  };
  let groupCollapsed6 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger$1.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
    
  };
  let groupCollapsed7 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable$1) {
      if (debuggerGroupCollapsed) {
        logger$1.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger$1.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
    
  };
  let groupEnd = () => {
    if (enable$1) {
      logger$1.groupEnd();
      return;
    }
    
  };
  let groupEnd1 = a => {
    if (enable$1) {
      logger$1.groupEnd(a);
      return;
    }
    
  };
  let groupEnd2 = (a, a1) => {
    if (enable$1) {
      logger$1.groupEnd(a, a1);
      return;
    }
    
  };
  let groupEnd3 = (a, a1, a2) => {
    if (enable$1) {
      logger$1.groupEnd(a, a1, a2);
      return;
    }
    
  };
  let Logger = {
    toArgs: toArgs,
    logger: logger$1,
    enable: enable$1,
    debuggerGroupCollapsed: debuggerGroupCollapsed,
    debug: debug,
    debug1: debug1,
    debug2: debug2,
    debug3: debug3,
    debug4: debug4,
    debug5: debug5,
    debug6: debug6,
    banner: banner,
    banner1: banner1,
    banner2: banner2,
    banner3: banner3,
    banner4: banner4,
    banner5: banner5,
    log: log,
    log1: log1,
    log2: log2,
    log3: log3,
    log4: log4,
    log5: log5,
    groupCollapsed: groupCollapsed$1,
    groupCollapsed1: groupCollapsed1,
    groupCollapsed2: groupCollapsed2,
    groupCollapsed3: groupCollapsed3,
    groupCollapsed4: groupCollapsed4,
    groupCollapsed5: groupCollapsed5,
    groupCollapsed6: groupCollapsed6,
    groupCollapsed7: groupCollapsed7,
    groupEnd: groupEnd,
    groupEnd1: groupEnd1,
    groupEnd2: groupEnd2,
    groupEnd3: groupEnd3
  };
  let Runtime_env = Runtime.env;
  let Runtime_CompressOptions = Runtime.CompressOptions;
  let CompressOptions$1 = Runtime_CompressOptions;
  let compressOptions_inlineCompression = CompressOptions$1.inlineCompression;
  let compressOptions_forceGridAlignment = CompressOptions$1.forceGridAlignment;
  let compressOptions_westernCharacterFirst = CompressOptions$1.westernCharacterFirst;
  let compressOptions_forceSpaceBetweenCJKAndWestern = CompressOptions$1.forceSpaceBetweenCJKAndWestern;
  let compressOptions_fixCJKLeftQuote = CompressOptions$1.fixCJKLeftQuote;
  let compressOptions = {
    inlineCompression: compressOptions_inlineCompression,
    forceGridAlignment: compressOptions_forceGridAlignment,
    westernCharacterFirst: compressOptions_westernCharacterFirst,
    forceSpaceBetweenCJKAndWestern: compressOptions_forceSpaceBetweenCJKAndWestern,
    fixCJKLeftQuote: compressOptions_fixCJKLeftQuote
  };
  let initlize = (env, options, firstChar, hasLeftQuote, paddingLeft) => {
    env._firstChar = firstChar;
    env._hasLeftQuote = hasLeftQuote;
    env._paddingLeft = paddingLeft;
    env._gridSize = options.gridSize;
    env._forceSpace = CompressOptions$1.forceSpaceBetweenCJKAndWestern ? env._gridSize / 4.0 : 0.0;
    env._spaceWidth = env._gridSize * 0.35;
    env._xInterval = options.xInterval;
    env._yInterval = options.yInterval;
    env._maxCol = options.column;
    env._lastCol = env._maxCol - 1.0;
    env._maxRow = options.row;
    let value = options.wrapWidth;
    env._maxWidth = value !== undefined ? value : env._maxCol * env._gridSize;
    env._letterSpacing = options.letterSpacing;
    env._gridWidth = env._gridSize + env._xInterval;
    return env;
  };
  let IEnv_env = Runtime_env;
  let IEnv = {
    CompressOptions: CompressOptions$1,
    env: IEnv_env,
    compressOptions: compressOptions,
    initlize: initlize
  };
  let CompressOptions$2 = Runtime.CompressOptions;
  let Config = {
    $$debugger: CompressOptions$2.$$debugger
  };
  let $$return = (param, t) => t;
  let switchTo = (param, t) => t;
  let tapIf = (context, exp, fn) => {
    if (exp(context)) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let tapIfConst = (context, exp, fn) => {
    if (exp) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let boolean = (context, exp) => [
    exp(context) ? "True" : "False",
    context
  ];
  let onTrue = (context, fn) => {
    if (context[0] === "False") {
      return context;
    } else {
      return [
        "True",
        fn(context[1])
      ];
    }
  };
  let onFalse = (context, fn) => {
    if (context[0] === "False") {
      return [
        "False",
        fn(context[1])
      ];
    } else {
      return context;
    }
  };
  let unwrap = context => context[1];
  let computeIfConst = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIf = (context, exp, fn) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIfElse = (context, exp, fn, else$p) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return else$p(context);
    }
  };
  let pipeIfConst = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let switchIf = (context, exp, else$p, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let check = (context, if$p, else$p, fn) => {
    if (if$p(context)) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let pipe = (context, fn) => fn(context);
  let computeIfElse = (context, exp, fn, fn2) => {
    if (exp) {
      return fn(context);
    } else {
      return fn2(context);
    }
  };
  let action = (context, fn) => {
    fn(context);
    return context;
  };
  let actionU = (context, fn) => {
    fn(context);
    return context;
  };
  let actionWith = (context, withCtx, fn) => {
    fn(withCtx);
    return context;
  };
  let tap = (context, fn) => {
    fn();
    return context;
  };
  let debugTap = (context, fn) => {
    if (Config.$$debugger) {
      fn();
      return context;
    } else {
      return context;
    }
  };
  let debug$1 = (context, fn) => {
    if (Config.$$debugger) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let ignore = (context, withCtx) => context;
  let call = (context, arg) => context(arg);
  let rev = (b, fn, a) => fn(a, b);
  let Pipe = {
    $$return: $$return,
    switchTo: switchTo,
    tapIf: tapIf,
    tapIfConst: tapIfConst,
    boolean: boolean,
    onTrue: onTrue,
    onFalse: onFalse,
    unwrap: unwrap,
    computeIfConst: computeIfConst,
    pipeIf: pipeIf,
    pipeIfElse: pipeIfElse,
    pipeIfConst: pipeIfConst,
    switchIf: switchIf,
    check: check,
    pipe: pipe,
    computeIfElse: computeIfElse,
    action: action,
    actionU: actionU,
    actionWith: actionWith,
    tap: tap,
    debugTap: debugTap,
    debug: debug$1,
    ignore: ignore,
    call: call,
    rev: rev
  };
  let env = Runtime.env;
  let context = Runtime.Store.context;
  let _buf = {
    seqs: [],
    areaWidth: 0.0,
    areaHeight: 0.0,
    width: 0.0,
    height: 0.0,
    lastLineIndex: 0,
    lineHeights: context._lineHeights,
    status: context._status
  };
  let setContentWidth = (context, width) => {
    context._contentWidth = width;
    return context;
  };
  let getContentWidth = context => context._contentWidth;
  let setCurrentX = (context, x) => {
    context._currentX = x;
    return context;
  };
  let getCurrentX = context => context._currentX;
  let setCurrentY = (context, y) => {
    context._currentY = y;
    return context;
  };
  let getCurrentY = context => context._currentY;
  let setRowHeight = (context, rowHeight) => {
    context._currentRowHeight = rowHeight;
    return context;
  };
  let getRowHeight = context => context._currentRowHeight;
  let storeLineHeights = context => {
    context._lineHeights.push(context._currentRowHeight);
    return context;
  };
  let setCurrentCol = (context, col) => {
    context._currentCol = col;
    return context;
  };
  let setCurrentColU = (context, col) => {
    context._currentCol = col;
  };
  let getCurrentCol = context => context._currentCol;
  let setCurrentRow = (context, row) => {
    context._currentRow = row;
    return context;
  };
  let getCurrentRow = context => context._currentRow;
  let finish = param => {};
  let syncContentWidth = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    return context;
  };
  let growRowHeight = (context, elementHeight) => {
    if (elementHeight > context._currentRowHeight) {
      context._currentRowHeight = elementHeight;
    }
    return context;
  };
  let resetCurrent = context => {
    context._currentX = 0.0;
    let context$1 = context;
    context$1._currentY = 0.0;
    let context$2 = context$1;
    context$2._currentCol = 0.0;
    let context$3 = context$2;
    context$3._currentRow = 1;
    return context$3;
  };
  let rollbackX = (context, x) => {
    debug3("rollbackX: %o - %o => %o", context._currentX, x, context._currentX - x);
    context._currentX = context._currentX - x;
    return context;
  };
  let moveCursorX = (context, x) => {
    debug3("[cursor] moveX: %o + %o => %o", context._currentX, x, context._currentX + x);
    context._currentX = context._currentX + x;
    return context;
  };
  let consumeXIn = (x, context) => {
    debug3("[cursor] moveX: %o + %o => %o", context._currentX, x, context._currentX + x);
    context._currentX = context._currentX + x;
    return x;
  };
  let growCurrentY = (context, y) => {
    context._currentY = context._currentY + y;
    return context;
  };
  let rollbackCol = (context, col) => {
    context._currentCol = context._currentCol - col;
    return context;
  };
  let consumeCol = (context, col) => {
    context._currentCol = context._currentCol + col;
    return context;
  };
  let consumeColU = (context, col) => {
    context._currentCol = context._currentCol + col;
  };
  let growCurrentRow = context => {
    context._currentY = context._currentY + (context._currentRowHeight + env._yInterval);
    let context$1 = context;
    context$1._currentRow = context._currentRow + 1 | 0;
    return context$1;
  };
  let setLastCharWidth = (context, charWidth) => {
    context._lastCharWidth = charWidth;
    return context;
  };
  let nextIsWord = context => {
    let kind = context._cursor < context._source.length ? Runtime.out(context._source[context._cursor + 1 | 0], Runtime.getElementType) : Runtime.out(context._END, Runtime.getElementType);
    switch (kind) {
      case 3 :
      case 12 :
        return true;
      default:
        return false;
    }
  };
  let lastIsNotPunctuation = context => {
    context._lastIsPunctuation = false;
    context._lastPunctuations = 0.0;
    return context;
  };
  let consumePunctuation = context => {
    context._lastIsPunctuation = true;
    context._lastPunctuations = context._lastPunctuations + 1.0;
    return context;
  };
  let needForceWrap = context => {
    context._isNeedForceWrap = true;
    return context;
  };
  let resetNeedForceWrap = context => {
    context._isNeedForceWrap = false;
    return context;
  };
  let recordLastElement = (context, char) => {
    context.rows[context._currentRow] = char;
  };
  let getRowSize = context => context.rows.length;
  let prepare = (context, source, options) => {
    let firstChar = source[0];
    let hasLeftQuote = Runtime.out(firstChar, Runtime.getElementType) === 10;
    let paddingLeft = hasLeftQuote ? Runtime.out(firstChar, Runtime.getWidth) + env._xInterval : 0.0;
    let env$1 = initlize(env, options, firstChar, hasLeftQuote, paddingLeft);
    context._currentX = 0.0;
    let context$1 = context;
    context$1._currentY = 0.0;
    let context$2 = context$1;
    context$2._currentCol = 0.0;
    let context$3 = context$2;
    context$3._currentRow = 1;
    let context$4 = context$3;
    context$4._lastIsPunctuation = false;
    context$4._lastPunctuations = 0.0;
    let context$5 = context$4;
    context$5._currentRowHeight = env$1._gridSize;
    let context$6 = context$5;
    context$6._lastCharWidth = 0.0;
    let context$7 = context$6;
    context$7._isNeedForceWrap = false;
    let context$8 = context$7;
    action((context$8._contentWidth = 0.0, context$8), context => {
      context._cursor = 0;
      context._lastIsWesternChar = CompressOptions$2.westernCharacterFirst;
      context._source = source;
      context.rows = [
        firstChar,
        firstChar
      ];
    });
    debug2("prepare", context, env$1);
  };
  let lineWrap = context => {
    log("lineWrap");
    /* lineWrap */
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let context$1 = context;
    context$1._currentCol = env._hasLeftQuote ? 1.0 : 0.0;
    let context$2 = context$1;
    context$2._currentX = env._paddingLeft;
    let context$3 = context$2;
    context$3._currentY = context$3._currentY + (context$3._currentRowHeight + env._yInterval);
    let context$4 = context$3;
    context$4._currentRow = context$3._currentRow + 1 | 0;
    let context$5 = context$4;
    context$5._lineHeights.push(context$5._currentRowHeight);
    let context$6 = context$5;
    context$6._currentRowHeight = env._gridSize;
    let context$7 = context$6;
    context$7._lastIsPunctuation = false;
    context$7._lastPunctuations = 0.0;
    let context$8 = context$7;
    context$8._lastCharWidth = 0.0;
    let context$9 = context$8;
    context$9._isNeedForceWrap = false;
  };
  let incrementFlush = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let lastLineIndex = context._currentRow;
    let overflowY = lastLineIndex > (context.rows.length - 1 | 0);
    let contentHeight = overflowY ? context._currentY : context._currentY + (context._lineHeights.push(context._currentRowHeight), context)._currentRowHeight;
    _buf.seqs = context.rows;
    _buf.areaWidth = context._contentWidth;
    _buf.areaHeight = contentHeight;
    _buf.width = env._maxWidth;
    _buf.height = contentHeight;
    _buf.lastLineIndex = overflowY ? lastLineIndex - 1 | 0 : lastLineIndex;
    _buf.lineHeights = context._lineHeights;
    _buf.status = "flushing";
    return _buf;
  };
  let flush = context => {
    if (context._currentX > context._contentWidth) {
      context._contentWidth = context._currentX;
    }
    let lastLineIndex = context._currentRow;
    let overflowY = lastLineIndex > (context.rows.length - 1 | 0);
    let contentHeight = overflowY ? context._currentY : context._currentY + (context._lineHeights.push(context._currentRowHeight), context)._currentRowHeight;
    _buf.seqs = context.rows;
    _buf.areaWidth = context._contentWidth;
    _buf.areaHeight = contentHeight;
    _buf.width = env._maxWidth;
    _buf.height = contentHeight;
    _buf.lastLineIndex = overflowY ? lastLineIndex - 1 | 0 : lastLineIndex;
    _buf.lineHeights = context._lineHeights;
    _buf.status = "closed";
    context._lineHeights = [];
    debug1("contentHeight: %o", contentHeight);
    return _buf;
  };
  let output = param => ({
    seqs: _buf.seqs,
    areaWidth: _buf.areaWidth,
    areaHeight: _buf.areaHeight,
    width: _buf.width,
    height: _buf.height,
    lastLineIndex: _buf.lastLineIndex,
    lineHeights: _buf.lineHeights,
    status: "closed"
  });
  let logger$2 = console;
  let groupCollapsed$2 = Runtime.CompressOptions.groupCollapsed;
  let enable$2 = process.env.NODE_ENV !== "production";
  let ILogger_label = "compact";
  let toArgs$1 = (label, args) => {
    if (typeof label !== "object" && typeof label !== "string") {
      return args;
    }
    if (typeof label !== "string") {
      return args;
    }
    let match = args[0];
    if (match !== undefined && typeof match === "string") {
      return [
        "%c%s%c " + match,
        "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;",
        label,
        ""
      ].concat(args.slice(1));
    }
    return [
      "%c%s%c %s",
      "background:#00bbee;border-radius:0.2em;color:white;font-weight:bold;padding:2px 0.5em;margin-right:4px;",
      label,
      ""
    ].concat(args);
  };
  let logger$3 = logger$2;
  let enable$3 = enable$2;
  let debuggerGroupCollapsed$1 = groupCollapsed$2;
  let debug$2 = a => {
    if (enable$3) {
      logger$3.debug(a);
      return;
    }
    
  };
  let debug1$1 = (a, a2) => {
    if (enable$3) {
      logger$3.debug(a, a2);
      return;
    }
    
  };
  let debug2$1 = (a, a2, a3) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3);
      return;
    }
    
  };
  let debug3$1 = (a, a2, a3, a4) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3, a4);
      return;
    }
    
  };
  let debug4$1 = (a, a2, a3, a4, a5) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3, a4, a5);
      return;
    }
    
  };
  let debug5$1 = (a, a2, a3, a4, a5, a6) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3, a4, a5, a6);
      return;
    }
    
  };
  let debug6$1 = (a, a2, a3, a4, a5, a6, a7) => {
    if (enable$3) {
      logger$3.debug(a, a2, a3, a4, a5, a6, a7);
      return;
    }
    
  };
  let banner$1 = a => {
    if (enable$3) {
      logger$3.banner(a);
      return;
    }
    
  };
  let banner1$1 = (a, a2) => {
    if (enable$3) {
      logger$3.banner(a, a2);
      return;
    }
    
  };
  let banner2$1 = (a, a2, a3) => {
    if (enable$3) {
      logger$3.banner(a, a2, a3);
      return;
    }
    
  };
  let banner3$1 = (a, a2, a3, a4) => {
    if (enable$3) {
      logger$3.banner(a, a2, a3, a4);
      return;
    }
    
  };
  let banner4$1 = (a, a2, a3, a4, a5) => {
    if (enable$3) {
      logger$3.banner(a, a2, a3, a4, a5);
      return;
    }
    
  };
  let banner5$1 = (a, a2, a3, a4, a5, a6) => {
    if (enable$3) {
      logger$3.banner(a, a2, a3, a4, a5, a6);
      return;
    }
    
  };
  let log$1 = a => {
    if (enable$3) {
      logger$3.log(a);
      return;
    }
    
  };
  let log1$1 = (a, a2) => {
    if (enable$3) {
      logger$3.log(a, a2);
      return;
    }
    
  };
  let log2$1 = (a, a2, a3) => {
    if (!enable$3) {
      return;
    }
    let args = toArgs$1(ILogger_label, [
      a,
      a2,
      a3
    ]);
    logger$3.log.apply(null, args);
  };
  let log3$1 = (a, a2, a3, a4) => {
    if (enable$3) {
      logger$3.log(a, a2, a3, a4);
      return;
    }
    
  };
  let log4$1 = (a, a2, a3, a4, a5) => {
    if (enable$3) {
      logger$3.log(a, a2, a3, a4, a5);
      return;
    }
    
  };
  let log5$1 = (a, a2, a3, a4, a5, a6) => {
    if (enable$3) {
      logger$3.log(a, a2, a3, a4, a5, a6);
      return;
    }
    
  };
  let groupCollapsed$3 = a => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a);
      } else {
        logger$3.group(a);
      }
      return;
    }
    
  };
  let groupCollapsed1$1 = (a, a1) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1);
      } else {
        logger$3.group(a, a1);
      }
      return;
    }
    
  };
  let groupCollapsed2$1 = (a, a1, a2) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2);
      } else {
        logger$3.group(a, a1, a2);
      }
      return;
    }
    
  };
  let groupCollapsed3$1 = (a, a1, a2, a3) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3);
      } else {
        logger$3.group(a, a1, a2, a3);
      }
      return;
    }
    
  };
  let groupCollapsed4$1 = (a, a1, a2, a3, a4) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3, a4);
      } else {
        logger$3.group(a, a1, a2, a3, a4);
      }
      return;
    }
    
  };
  let groupCollapsed5$1 = (a, a1, a2, a3, a4, a5) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3, a4, a5);
      } else {
        logger$3.group(a, a1, a2, a3, a4, a5);
      }
      return;
    }
    
  };
  let groupCollapsed6$1 = (a, a1, a2, a3, a4, a5, a6) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3, a4, a5, a6);
      } else {
        logger$3.group(a, a1, a2, a3, a4, a5, a6);
      }
      return;
    }
    
  };
  let groupCollapsed7$1 = (a, a1, a2, a3, a4, a5, a6, a7) => {
    if (enable$3) {
      if (debuggerGroupCollapsed$1) {
        logger$3.groupCollapsed(a, a1, a2, a3, a4, a5, a6, a7);
      } else {
        logger$3.group(a, a1, a2, a3, a4, a5, a6, a7);
      }
      return;
    }
    
  };
  let groupEnd$1 = () => {
    if (enable$3) {
      logger$3.groupEnd();
      return;
    }
    
  };
  let groupEnd1$1 = a => {
    if (enable$3) {
      logger$3.groupEnd(a);
      return;
    }
    
  };
  let groupEnd2$1 = (a, a1) => {
    if (enable$3) {
      logger$3.groupEnd(a, a1);
      return;
    }
    
  };
  let groupEnd3$1 = (a, a1, a2) => {
    if (enable$3) {
      logger$3.groupEnd(a, a1, a2);
      return;
    }
    
  };
  let Config$1 = {
    $$debugger: Runtime.CompressOptions.$$debugger
  };
  let $$return$1 = (param, t) => t;
  let switchTo$1 = (param, t) => t;
  let tapIf$1 = (context, exp, fn) => {
    if (exp(context)) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let tapIfConst$1 = (context, exp, fn) => {
    if (exp) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let boolean$1 = (context, exp) => [
    exp(context) ? "True" : "False",
    context
  ];
  let onTrue$1 = (context, fn) => {
    if (context[0] === "False") {
      return context;
    } else {
      return [
        "True",
        fn(context[1])
      ];
    }
  };
  let onFalse$1 = (context, fn) => {
    if (context[0] === "False") {
      return [
        "False",
        fn(context[1])
      ];
    } else {
      return context;
    }
  };
  let unwrap$1 = context => context[1];
  let computeIfConst$1 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIf$1 = (context, exp, fn) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return context;
    }
  };
  let pipeIfElse$1 = (context, exp, fn, else$p) => {
    if (exp(context)) {
      return fn(context);
    } else {
      return else$p(context);
    }
  };
  let pipeIfConst$1 = (context, exp, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return context;
    }
  };
  let switchIf$1 = (context, exp, else$p, fn) => {
    if (exp) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let check$1 = (context, if$p, else$p, fn) => {
    if (if$p(context)) {
      return fn(context);
    } else {
      return else$p;
    }
  };
  let pipe$1 = (context, fn) => fn(context);
  let computeIfElse$1 = (context, exp, fn, fn2) => {
    if (exp) {
      return fn(context);
    } else {
      return fn2(context);
    }
  };
  let action$1 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionU$1 = (context, fn) => {
    fn(context);
    return context;
  };
  let actionWith$1 = (context, withCtx, fn) => {
    fn(withCtx);
    return context;
  };
  let tap$1 = (context, fn) => {
    fn();
    return context;
  };
  let debugTap$1 = (context, fn) => {
    if (Config$1.$$debugger) {
      fn();
      return context;
    } else {
      return context;
    }
  };
  let debug$3 = (context, fn) => {
    if (Config$1.$$debugger) {
      fn(context);
      return context;
    } else {
      return context;
    }
  };
  let ignore$1 = (context, withCtx) => context;
  let call$1 = (context, arg) => context(arg);
  let rev$1 = (b, fn, a) => fn(a, b);
  let env$1 = Runtime.env;
  let isCompressableChar = char => {
    if (char >= 8252) {
      if (char >= 11834) {
        if (char !== 65374) {
          return char >= 11835;
        } else {
          return false;
        }
      } else if (char !== 8263) {
        return char >= 8253;
      } else {
        return false;
      }
    } else if (char > 8212 || char < 95) {
      return char !== 8230;
    } else {
      return !(char > 8211 || char < 96);
    }
  };
  let process = (context, char, charFontSize, charWidth, currentIsPunctuation, isEOL, isValidEndChar) => {
    let match = isCompressableChar(Runtime.out(char, Runtime.getCharCode));
    let match$1 = match ? (
        isEOL && isValidEndChar ? "EOLCompression" : (
            CompressOptions.inlineCompression && currentIsPunctuation ? "InlineCompression" : "NoCompression"
          )
      ) : "NoCompression";
    switch (match$1) {
      case "EOLCompression" :
        groupCollapsed$3("EOLCompression");
        return tap$1(tapIf$1(consumeCol(moveCursorX(context, charFontSize / 2.0), 0.5), context => getCurrentCol(context) % 1.0 !== 0.5, needForceWrap), () => groupEnd$1());
      case "InlineCompression" :
        let match$2 = Runtime.out(char, Runtime.getElementType);
        let tmp;
        let exit = 0;
        switch (match$2) {
          case 6 :
          case 7 :
          case 9 :
          case 10 :
          case 11 :
            tmp = true;
            break;
          case 15 :
          case 16 :
            exit = 1;
            break;
          default:
            tmp = false;
        }
        if (exit === 1) {
          let match$3 = Runtime.out(char, Runtime.getCharCode);
          tmp = match$3 !== 34 ? match$3 !== 39 : false;
        }
        if (tmp) {
          consumePunctuation(context);
          groupCollapsed$3("InlineCompression");
          return tap$1(consumeCol(moveCursorX(context, charWidth - charFontSize / 2.0 + env$1._xInterval * (
            context._lastIsPunctuation ? 1.0 : 0.0
          )), charWidth / charFontSize - 0.5), () => groupEnd$1());
        } else {
          return consumeCol(moveCursorX(tapIfConst$1(context, context._lastIsPunctuation, lastIsNotPunctuation), charWidth + env$1._xInterval), charWidth / charFontSize);
        }
      case "NoCompression" :
        return consumeCol(moveCursorX(tapIfConst$1(context, context._lastIsPunctuation, lastIsNotPunctuation), charWidth + env$1._xInterval), charWidth / charFontSize);
    }
  };
  let processUncompress = (context, elementType, currentIsPunctuation) => {
    if (CompressOptions.inlineCompression && context._lastIsPunctuation && !currentIsPunctuation) {
      groupCollapsed2$1("processUncompress", context._lastPunctuations, ElementType$Std.print(elementType));
      return finish(tap$1(lastIsNotPunctuation(consumeCol(moveCursorX(context, context._lastCharWidth / 2.0 + env$1._xInterval), 0.5)), () => groupEnd$1()));
    }
    
  };
  let fixCjkLeftQuote = (currentX, context, charFontSize, charCode, charWidth) => {
    let flag_STDWIDTH = env$1._flag_STDWIDTH;
    let offset = !context._lastIsPunctuation && /* '“' */8220 === charCode ? charFontSize / 2.0 : 0.0;
    return currentX + (
      CompressOptions.fixCJKLeftQuote && /* '“' */8220 === charCode && (!flag_STDWIDTH || charWidth === charFontSize) ? offset - charFontSize / 2.0 : offset
    );
  };
  return {
    process: process,
    processUncompress: processUncompress,
    fixCjkLeftQuote: fixCjkLeftQuote
  };
}

export {
  getOffsetXFromFixCjkQuote,
  MakeForceGridAlignment,
  MakeCompress,
}
/* Array-Std Not a pure module */
