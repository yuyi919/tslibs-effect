// Generated by ReScript, PLEASE EDIT WITH CARE


function first(__x) {
  return __x[0];
}

function last(arr) {
  return arr[arr.length - 1 | 0];
}

function get(arr, i) {
  return arr[i];
}

function set(arr, i, v) {
  arr[i] = v;
}

function forEach(arr, each) {
  for (let i = 0, i_finish = arr.length; i < i_finish; ++i) {
    each(arr[i], i);
  }
}

function _reduceI(arr, b, f) {
  let r = b;
  for (let i = 0, i_finish = arr.length; i < i_finish; ++i) {
    r = f(r, arr[i], i);
  }
  return r;
}

function reduceI(arr, b, f) {
  let r = b;
  for (let i = 0, i_finish = arr.length; i < i_finish; ++i) {
    r = f(r, arr[i], i);
  }
  return r;
}

function reduce(arr, b, each) {
  let _b = b;
  let _i = 0;
  while (true) {
    let i = _i;
    let b$1 = _b;
    if (i === arr.length) {
      return b$1;
    }
    _i = i + 1 | 0;
    _b = each(b$1, arr[i]);
    continue;
  };
}

function OptsInline($star) {
  let forEach = (arr, each) => {
    for (let i = 0, i_finish = arr.length; i < i_finish; ++i) {
      each(arr[i], i);
    }
  };
  let reduceI = (arr, b, each) => {
    let _b = b;
    let _i = 0;
    while (true) {
      let i = _i;
      let b$1 = _b;
      if (i === arr.length) {
        return b$1;
      }
      _i = i + 1 | 0;
      _b = each(b$1, arr[i], i);
      continue;
    };
  };
  return {
    forEach: forEach,
    reduceI: reduceI
  };
}

function forEachStep(arr, fn, step) {
  let _i = 0;
  while (true) {
    let i = _i;
    if (i >= arr.length) {
      return;
    }
    fn(arr, i);
    _i = i + step | 0;
    continue;
  };
}

function forEachStep2(fa, f, step) {
  let i = 0;
  while (i < fa.length) {
    f(fa, i);
    i = i + step | 0;
  };
}

export {
  first,
  last,
  get,
  set,
  forEach,
  _reduceI,
  reduceI,
  reduce,
  OptsInline,
  forEachStep,
  forEachStep2,
}
/* No side effect */
