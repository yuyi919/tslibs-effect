// @@bs.config({flags: ["-bs-no-cross-module-opt"]})

@inline
let eq = (a: int, b: int) => a === b

@inline
let div2 = (v: int) => v->Types.unsafeCoerce / 2
@inline
let mul2 = v => v * 2
@inline
let mul = (v, mul) => v * mul
@inline
let div = (v, div) => v / div

@inline
let div_float = (v: int, div: int) => v->Float.fromInt /. div->Float.fromInt

@inline
let minusminus = v => v - 1
@inline
let plusplus = v => v + 1
@inline
let diff = (v, v2) => v - v2
@inline
let sub = (v, v2) => v - v2
@inline
let rev_sub = (v2, v) => sub(v, v2)
@inline
let add = (v, v2) => v + v2

@val external ceil: float => int = "Math.ceil"

external unsafe_toString: int => string = "%identity"
external unsafe: 'a => int = "%identity"
external unsafe_to_float: int => float = "%identity"

// let logand = Int32.logand
// let logor = Int32.logor
// let logxor = Int32.logxor
// let shift_left = Int32.shift_left
// let shift_right = Int32.shift_right
// let shift_right_logical = Int32.shift_right_logical
let m = Float.mod
external neg: int => int = "%negint"
external add: (int, int) => int = "%addint"
external sub: (int, int) => int = "%subint"
external mul: (int, int) => int = "%mulint"
external div: (int, int) => int = "%divint"
external unsafeDiv: (int, int) => int = "%divfloat"
external rem: (int, int) => int = "%modint"
external mod: (int, int) => int = "%modfloat"
external modf: int => (int, int) = "%modf_float"
external logand: (int, int) => int = "%andint"
external logor: (int, int) => int = "%orint"
external logxor: (int, int) => int = "%xorint"
external shift_left: (int, int) => int = "%lslint"
external shift_right: (int, int) => int = "%asrint"
external shift_right_logical: (int, int) => int = "%lsrint"
// external of_int: int => int = "%identity"
// external to_int: int => int = "%identity"
external of_float: float => int = "?int_of_float"
external fromFloat: float => int = "?int_of_float"
external to_float: int => float = "?int_to_float"
external bits_of_float: float => int = "?int_bits_of_float"
external float_of_bits: int => float = "?int_float_of_bits"

external force: int => int = "?int_of_float"

@inline
let get: ref<int> => int = r => r.contents

@inline
let min_int = 0x80000000l
let max_int = 0x7FFFFFFFl
