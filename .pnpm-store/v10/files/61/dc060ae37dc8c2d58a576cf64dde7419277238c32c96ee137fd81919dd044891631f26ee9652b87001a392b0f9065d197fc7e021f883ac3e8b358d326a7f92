external covariant: 'a => 'b = "%identity"

@genType
type covariant<'a, 'b> = 'a => 'b

external invariant: 'a => 'a = "%identity"

@genType
type invariant<'a> = 'a => 'a

type objectProto = {toString: unit => string}

external asUnk: 't => unknown = "%identity"

external unsafeCoerce: 'a => 'b = "%identity"

// external unsafe_string: (string, int, int) => string = "?md5_string"

// let string = str => unsafe_string(str, 0, String.length(str))

// let c = Genlex.make_lexer(list{"*", "/"}, Stream.of_string("fooba*rcesd"))

// let rec toarray = (c, list) => {
//   switch Stream.peek(c) {
//   | Some(_) => toarray(c, list{...list, Stream.next(c)})
//   | None => list
//   }
// }

// @genType
// let c = toarray(c, list{})->List.toArray->Identity.map(a => {
//   Js.log(a)
//   a
// })

// @genType
// let d = Lexing.from_string("abc")
// let yy = Parsing.yyparse

// @noalloc external seeded_hash_param: (int, int, int, 'a) => int = "?hash"
// /* external old_hash_param :
//  int -> int -> 'a -> int = "caml_hash_univ_param" [@@noalloc] */

// let hash = x => seeded_hash_param(10, 100, 0, x)

// let cc = hash("app")
