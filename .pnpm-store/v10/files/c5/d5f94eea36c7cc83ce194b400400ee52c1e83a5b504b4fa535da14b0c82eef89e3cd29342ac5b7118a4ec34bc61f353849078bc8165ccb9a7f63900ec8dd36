import type * as fp from "fp-ts";
import { Kind, Kind2, URIS, URIS2 } from "fp-ts/HKT";
import { Zero2 } from "fp-ts/Zero";
import * as core from "./core";
/**
 * @category instances
 * @since 0.6.0
 */
export declare const URI = "Parser";
/**
 * @category instances
 * @since 0.6.0
 */
export type URI = typeof URI;
declare module "fp-ts/HKT" {
    interface URItoKind2<E, A> {
        [URI]: core.Parser<E, A>;
    }
}
/**
 * @category instances
 * @since 0.6.7
 */
export declare const Functor: fp.functor.Functor2<URI>;
/**
 * @category instances
 * @since 0.6.7
 */
export declare const Apply: fp.apply.Apply2<URI>;
/**
 * @category instances
 * @since 0.6.7
 */
export declare const Applicative: fp.applicative.Applicative2<URI>;
/**
 * @category instances
 * @since 0.6.7
 */
export declare const Monad: fp.monad.Monad2<URI>;
/**
 * @category instances
 * @since 0.6.7
 */
export declare const getMonad2C: <I>() => fp.monad.Monad2C<URI, I>;
/**
 * @category instances
 * @since 0.6.11
 */
export declare const ChainRec: fp.chainRec.ChainRec2<URI>;
/**
 * @category instances
 * @since 0.6.7
 */
export declare const Alt: fp.alt.Alt2<URI>;
/**
 * @category instances
 * @since 0.6.7
 */
export declare const Alternative: fp.alternative.Alternative2<URI>;
/**
 * @category instances
 * @since 0.6.7
 */
export declare const Zero: Zero2<URI>;
/**
 * @category instances
 * @since 0.6.7
 */
export declare const parser: fp.monad.Monad2<URI> & fp.alternative.Alternative2<URI>;
/**
 * @category instances
 * @since 0.6.7
 */
export declare const getSemigroup: <I, A>(S: fp.semigroup.Semigroup<A>) => fp.semigroup.Semigroup<core.Parser<I, A>>;
/**
 * @category instances
 * @since 0.6.0
 */
export declare const getMonoid: <I, A>(monoid: fp.monoid.Monoid<A>) => fp.monoid.Monoid<core.Parser<I, A>>;
export declare function getApplyF<URI extends URIS2>(M: fp.apply.Apply2<URI>): <I, E, A, B>(fab: core.Parser<I, Kind2<URI, E, (a: A) => B>>, fa: core.Parser<I, Kind2<URI, E, A>>) => core.Parser<I, Kind2<URI, E, B>>;
export declare function getApplyF<URI extends URIS>(M: fp.apply.Apply1<URI>): <I, A, B>(fab: core.Parser<I, Kind<URI, (a: A) => B>>, fa: core.Parser<I, Kind<URI, A>>) => core.Parser<I, Kind<URI, B>>;
//# sourceMappingURL=URI.d.ts.map