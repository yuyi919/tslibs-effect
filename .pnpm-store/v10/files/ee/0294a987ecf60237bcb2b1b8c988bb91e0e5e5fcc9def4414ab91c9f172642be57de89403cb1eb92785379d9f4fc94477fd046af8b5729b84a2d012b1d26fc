import * as core from "./core.js";
import { liftMap2 } from "./liftmap.js";

/**
 * @category instances
 * @since 0.6.0
 */
export let URI = "Parser";
/**
 * @category instances
 * @since 0.6.0
 */ /**
 * @category instances
 * @since 0.6.7
 */
export let Functor = {
  URI,
  map: core.mapF,
};
/**
 * @category instances
 * @since 0.6.7
 */
export let Apply = {
  URI,
  map: core.mapF,
  ap: core.apF,
};
/**
 * @category instances
 * @since 0.6.7
 */
export let Applicative = {
  URI,
  map: core.mapF,
  ap: core.apF,
  of: core.of,
};
/**
 * @category instances
 * @since 0.6.7
 */
export let Monad = {
  URI,
  map: core.mapF,
  ap: core.apF,
  of: core.of,
  chain: core.chainF,
};
/**
 * @category instances
 * @since 0.6.7
 */
export let getMonad2C = () => Monad;
/**
 * @category instances
 * @since 0.6.11
 */
export let ChainRec = {
  URI,
  map: core.mapF,
  ap: core.apF,
  chain: core.chainF,
  chainRec: core.chainRecF,
};
/**
 * @category instances
 * @since 0.6.7
 */
export let Alt = {
  URI,
  map: core.mapF,
  alt: core.altF,
};
/**
 * @category instances
 * @since 0.6.7
 */
export let Alternative = {
  URI,
  map: core.mapF,
  of: core.of,
  ap: core.apF,
  alt: core.altF,
  zero: core.fail,
};
/**
 * @category instances
 * @since 0.6.7
 */
export let Zero = {
  URI,
  zero: core.fail,
};
/**
 * @category instances
 * @since 0.6.7
 */
export let parser = {
  URI,
  map: core.mapF,
  of: core.of,
  ap: core.apF,
  chain: core.chainF,
  alt: core.altF,
  zero: core.fail,
};
/**
 * @category instances
 * @since 0.6.7
 */
export let getSemigroup = (semigroup) => ({
  concat: (x, y) => liftMap2(x, y, semigroup.concat),
});
/**
 * @category instances
 * @since 0.6.0
 */
export let getMonoid = (monoid) => ({
  ...getSemigroup(monoid),
  empty: core.of(monoid.empty),
});
export let getApplyF = (M) => (fab, fa) => liftMap2(fab, fa, M.ap);
//# sourceMappingURL=URI.js.map
