// Generated by ReScript, PLEASE EDIT WITH CARE
let first = __x => __x[0];
let last = arr => arr[arr.length - 1 | 0];
let get = (arr, i) => arr[i];
let set = (arr, i, v) => {
  arr[i] = v;
};
let forEach = (arr, each) => {
  for (let i = 0, i_finish = arr.length; i < i_finish; ++i) {
    each(arr[i], i);
  }
};
let _reduceI = (arr, b, f) => {
  let r = b;
  for (let i = 0, i_finish = arr.length; i < i_finish; ++i) {
    r = f(r, arr[i], i);
  }
  return r;
};
let reduceI = (arr, b, f) => {
  let r = b;
  for (let i = 0, i_finish = arr.length; i < i_finish; ++i) {
    r = f(r, arr[i], i);
  }
  return r;
};
let reduce = (arr, b, each) => {
  let _b = b;
  let _i = 0;
  while (true) {
    let i = _i;
    let b$1 = _b;
    if (i === arr.length) {
      return b$1;
    }
    _i = i + 1 | 0;
    _b = each(b$1, arr[i]);
    continue;
  }
  ;
};
let OptsInline = $star => {
  let forEach = (arr, each) => {
    for (let i = 0, i_finish = arr.length; i < i_finish; ++i) {
      each(arr[i], i);
    }
  };
  let reduceI = (arr, b, each) => {
    let _b = b;
    let _i = 0;
    while (true) {
      let i = _i;
      let b$1 = _b;
      if (i === arr.length) {
        return b$1;
      }
      _i = i + 1 | 0;
      _b = each(b$1, arr[i], i);
      continue;
    }
    ;
  };
  return {
    forEach: forEach,
    reduceI: reduceI
  };
};
let forEachStep = (arr, fn, step) => {
  let _i = 0;
  while (true) {
    let i = _i;
    if (i >= arr.length) {
      return;
    }
    fn(arr, i);
    _i = i + step | 0;
    continue;
  }
  ;
};
let forEachStep2 = (fa, f, step) => {
  let i = 0;
  while (i < fa.length) {
    f(fa, i);
    i = i + step | 0;
  }
  ;
};
export { first, last, get, set, forEach, _reduceI, reduceI, reduce, OptsInline, forEachStep, forEachStep2 }; /* No side effect */
//# sourceMappingURL=ArrayUtil.bs.js.map