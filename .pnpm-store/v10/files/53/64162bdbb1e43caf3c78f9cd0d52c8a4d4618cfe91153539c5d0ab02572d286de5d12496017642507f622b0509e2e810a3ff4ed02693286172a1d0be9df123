/**
 * @since 2.0.0
 */ import { dual } from "./internal/helper.js";
import * as NonNullable from "./NonNullable.js";

/**
 * @category model
 * @since 2.0.0
 */ /**
 * Inverts the ordering of the input `Ordering`.
 *
 * @param o - The input `Ordering`.
 *
 * @example
 * import { reverse } from "effect/Ordering"
 *
 * assert.deepStrictEqual(reverse(1), -1)
 * assert.deepStrictEqual(reverse(-1), 1)
 * assert.deepStrictEqual(reverse(0), 0)
 *
 * @since 2.0.0
 */
export let reverse = (o) => (o === -1 ? 1 : o === 1 ? -1 : 0);
/**
 *
 */ /**
 * onLessThen
 */ /**
 * onEquals
 */ /**
 * onGreaterThen
 */ /**
 * Depending on the `Ordering` parameter given to it, returns a value produced by one of the 3 functions provided as parameters.
 *
 * @param self - The `Ordering` parameter to match against.
 * @param onLessThan - A function that will be called if the `Ordering` parameter is `-1`.
 * @param onEqual - A function that will be called if the `Ordering` parameter is `0`.
 * @param onGreaterThan - A function that will be called if the `Ordering` parameter is `1`.
 *
 * @example
 *
 * const toMessage = Ordering.match({
 *   lt: constant('less than'),
 *   eq: constant('equal'),
 *   gt: constant('greater than')
 * })
 *
 * assert.deepStrictEqual(toMessage(-1), "less than")
 * assert.deepStrictEqual(toMessage(0), "equal")
 * assert.deepStrictEqual(toMessage(1), "greater than")
 *
 * @category pattern matching
 * @since 2.0.0
 */
export let match = /*#__PURE__*/ dual(2, (self, option) =>
  self < 0 ? option.lt() : self === 0 ? option.eq() : option.gt(),
);
/**
 * @category combining
 * @since 2.0.0
 */
export let combine = /*#__PURE__*/ dual(2, (self, that) =>
  self !== 0 ? self : that,
);
/**
 * @category combining
 * @since 2.0.0
 */
export let combineMany = /*#__PURE__*/ dual(2, (self, collection) =>
  self !== 0 ? self : (collection.find((ordering) => ordering !== 0) ?? 0),
);
export let combineManyIterable = /*#__PURE__*/ dual(2, (self, collection) =>
  self !== 0
    ? self
    : (NonNullable.findIterableMap(collection, (ordering) =>
        NonNullable.nullWhen(ordering, 0),
      ) ?? 0),
);
/**
 * @category combining
 * @since 2.0.0
 */
export let combineAll = (collection) => combineMany(0, collection);
/**
 * @category combining
 * @since 2.0.0
 */
export let combineAllIterable = (collection) =>
  combineManyIterable(0, collection);
//# sourceMappingURL=Ordering.js.map
