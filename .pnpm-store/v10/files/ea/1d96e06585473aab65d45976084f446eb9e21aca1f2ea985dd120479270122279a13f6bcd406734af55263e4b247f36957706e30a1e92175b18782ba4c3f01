import { ERROR } from "./constant/index.js";
import { tryCatch } from "./helper.js";

let __key_stackTraceLimit = "stackTraceLimit";
export let setStackTraceLimit = (size) => (ERROR[__key_stackTraceLimit] = size);
export let getStackTraceLimit = () => ERROR[__key_stackTraceLimit];
export let getStack = (size) => {
  let limit = getStackTraceLimit();
  setStackTraceLimit(size);
  let creationError = ERROR();
  setStackTraceLimit(limit);
  return creationError.stack;
};
export { tryCatch };
//# sourceMappingURL=Error.js.map
