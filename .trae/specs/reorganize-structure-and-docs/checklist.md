- [x] 目录重组后：`src/` 的对外入口与内部实现边界清晰，且在 `README.md` 中有对应说明
- [x] 对外导入路径兼容策略明确：默认不引入 **BREAKING**；如有必须的破坏性变更，提供迁移说明
- [x] 未引入逻辑语义变更：仅发生文件移动与必要的 `import/export` 路径调整
- [x] `bun test src` 通过
- [x] `tsc --build` 通过，且发布入口与子路径仍与 `package.json#exports` 匹配
- [x] `README.md` 覆盖：项目目标、使用示例、导出/对齐策略、目录结构、维护与 agent 约束、开发命令
